lab_id: agent-ir-ai-week04
title: 자동 익스플로잇 — Transient Tool 아티팩트 (AI 지원)
version: ai
course: agent-ir-ai
week: 4
description: 에이전트가 세션 내 생성한 공격 도구(Transient Tool)의 흔적을 방어자 관점에서 포렌식으로 재구성한다. AI SubAgent가
  자동으로 명령을 실행·검증합니다.
difficulty: hard
duration_minutes: 150
objectives:
- Transient Tool 생성·진화·폐기를 auditd로 추적한다
- ModSecurity 응답을 비교해 시그니처 공백을 체감한다
- 3층 방어(메타·응답·기만)의 실무 예시를 각각 구현한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: web VM에 auditd로 /tmp 쓰기 감시 룰을 일시적으로 추가.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 9
  answer: '🤖 프롬프트: web VM에 auditd로 /tmp 쓰기 감시 룰을 일시적으로 추가.

    📎 참고 (CLI): sudo auditctl -w /tmp -p wa -k tmpwrites && sudo auditctl -l | tail
    -5'
  answer_detail: Transient Tool이 /tmp에 생성·수정되는 것을 감시. 실제 운영 환경에서는 *적용 범위*를 좁혀야 부하
    감당 가능.
  verify:
    type: output_contains
    expect:
    - tmpwrites
    - buntu
    - jammy-security
    field: stdout
    semantic:
      intent: web auditd /tmp 쓰기 감시 — sudo auditctl -w /tmp -p w -k tmp_write 로 write
        이벤트만 감시. Transient Tool 아티팩트 탐지 전제. -p w 로 w(write) 만 필터해 노이즈 최소화
      success_criteria:
      - auditctl -w /tmp -p w
      - -k tmp_write 키 지정
      acceptable_methods:
      - auditctl -w
      - /etc/audit/rules.d/ 파일
      negative_signs:
      - -p 옵션 없음
      - 웹 VM 아닌 다른 곳
  target_vm: web
  script: sudo auditctl -w /tmp -p wa -k tmpwrites && sudo auditctl -l | tail -5
  risk_level: low
  bastion_prompt: web VM에 auditd로 /tmp 쓰기 감시 룰을 일시적으로 추가.
- order: 2
  instruction: Transient Tool을 3개 버전으로 시뮬레이션 생성 (공격자 자가 수정 루프 재현).
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 10
  answer: '🤖 프롬프트: Transient Tool을 3개 버전으로 시뮬레이션 생성 (공격자 자가 수정 루프 재현).

    📎 참고 (CLI): for v in 1 2 3; do cat > /tmp/exp_v$v.py <<PY

    # auto-generated exploit v$v — $(date +%s)

    import requests

    r = requests.post(''http://10.20.30.80/test'', data={''q'':''payload_v$v''})

    PY

    done; ls -la /tmp/exp_v*.py'
  answer_detail: v1→v2→v3 연속 수정. 사람은 같은 파일을 분 단위로 이렇게 고치지 않는 패턴이 지문이 된다.
  verify:
    type: output_contains
    expect:
    - exp_v
    - Transient
    field: stdout
    semantic:
      intent: Transient Tool 3 버전 시뮬 — /tmp/tool_v1.py, /tmp/tool_v2.py, /tmp/tool_v3.py
        순차 생성 + 각각 exit 코드나 로직 변형. agent 의 self-modifying 루프 재현. iterative improvement
        signature
      success_criteria:
      - 3 버전 파일 생성
      - /tmp 하위
      - 버전 간 차이
      acceptable_methods:
      - cat > file + echo 버전
      - python 3 파일 write
      negative_signs:
      - 1-2 버전만
      - /tmp 아닌 곳
  target_vm: web
  script: 'for v in 1 2 3; do cat > /tmp/exp_v$v.py <<PY

    # auto-generated exploit v$v — $(date +%s)

    import requests

    r = requests.post(''http://10.20.30.80/test'', data={''q'':''payload_v$v''})

    PY

    done; ls -la /tmp/exp_v*.py'
  risk_level: low
  bastion_prompt: Transient Tool을 3개 버전으로 시뮬레이션 생성 (공격자 자가 수정 루프 재현).
- order: 3
  instruction: auditd 결과를 조회해 생성 이벤트를 확인.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 11
  answer: '🤖 프롬프트: auditd 결과를 조회해 생성 이벤트를 확인.

    📎 참고 (CLI): sudo ausearch -k tmpwrites --start recent 2>/dev/null | tail -30 |
    head -30'
  answer_detail: auditd가 각 파일 write를 기록. 같은 path에 반복 write가 보이면 자가 수정 루프 증거.
  verify:
    type: output_contains
    expect:
    - c000003e
    - syscall
    field: stdout
    semantic:
      intent: auditd 생성 이벤트 조회 — sudo ausearch -k tmp_write --start recent 로 s1 에서
        설정한 키 기반 검색 + s2 의 3 파일 생성 이벤트 확인. PATH + SYSCALL + PROCTITLE 출력. audit 체인
        완결성 검증
      success_criteria:
      - ausearch -k tmp_write
      - s2 파일 생성 이벤트 3+
      - syscall 정보 포함
      acceptable_methods:
      - ausearch -k
      - aureport -x
      negative_signs:
      - 키 없이 검색
      - s2 연관 없음
  target_vm: web
  script: sudo ausearch -k tmpwrites --start recent 2>/dev/null | tail -30 | head
    -30
  risk_level: low
  bastion_prompt: auditd 결과를 조회해 생성 이벤트를 확인.
- order: 4
  instruction: web에서 단일 SQLi 페이로드 시도(WAF 반응 확인).
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: attack
  points: 12
  answer: '🤖 프롬프트: web에서 단일 SQLi 페이로드 시도(WAF 반응 확인).

    📎 참고 (CLI): curl -s -o /dev/null -w ''%{http_code}\n'' ''http://10.20.30.80:8082/login?q=admin%27%20OR%201=1--'''
  answer_detail: WAF가 있으면 403, 없으면 404/200. w6의 변형 공격 대비.
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 단일 SQLi 페이로드 + WAF 반응 — curl /vulnerabilities/sqli/?id='%20OR%201%3D1--
        로 기본 SQLi 전송 → HTTP 403 (ModSec 차단) 응답 확인. baseline SQLi 탐지 검증
      success_criteria:
      - curl SQLi 페이로드
      - WAF 반응 (403/200) 확인
      - baseline 측정
      acceptable_methods:
      - curl 단일 페이로드
      - Burp Repeater
      negative_signs:
      - WAF 반응 확인 없음
      - 여러 페이로드
  target_vm: web
  script: curl -s -o /dev/null -w '%{http_code}\n' 'http://10.20.30.80:8082/login?q=admin%27%20OR%201=1--'
  risk_level: low
  bastion_prompt: 'web에서 단일 SQLi 페이로드 시도(WAF 반응 확인).


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 5
  instruction: URL 인코딩 2중 적용한 변형 페이로드로 WAF 우회 시도.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: attack
  points: 13
  answer: '🤖 프롬프트: URL 인코딩 2중 적용한 변형 페이로드로 WAF 우회 시도.

    📎 참고 (CLI): curl -s -o /dev/null -w ''%{http_code}\n'' ''http://10.20.30.80:8082/login?q=admin%2527%2520OR%25201=1--'''
  answer_detail: 에이전트 회피의 기본. 동일 의미·다른 문자열로 시그니처 회피.
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: URL 이중 인코딩 우회 — curl /vulnerabilities/sqli/?id=%2527%2520OR%25201%253D1--
        로 싱글쿼터 이중 인코딩 (% → %25 → %2527). ModSec 정규식이 1차 디코딩만 하면 우회. agent 의 자동 변형
        재현
      success_criteria:
      - 이중 인코딩 %2527 사용
      - s4 와 다른 응답 (우회 성공 시)
      - 이중 인코딩 원리
      acceptable_methods:
      - curl + 이중 인코딩
      - sqlmap --tamper=charencode
      - Burp URL encode x2
      negative_signs:
      - 단일 인코딩만
      - s4 와 동일
  target_vm: web
  script: curl -s -o /dev/null -w '%{http_code}\n' 'http://10.20.30.80:8082/login?q=admin%2527%2520OR%25201=1--'
  risk_level: low
  bastion_prompt: 'URL 인코딩 2중 적용한 변형 페이로드로 WAF 우회 시도.


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 6
  instruction: ModSecurity 감사 로그가 있으면 매칭 룰 분포 조사.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 14
  answer: '🤖 프롬프트: ModSecurity 감사 로그가 있으면 매칭 룰 분포 조사.

    📎 참고 (CLI): sudo tail -50 /var/log/bunkerweb/modsec_audit.log 2>/dev/null | grep
    -oE ''id "[0-9]+"'' | sort | uniq -c | sort -rn | head || echo ''no_modsec_log'''
  answer_detail: 어떤 룰이 얼마나 매칭됐는지. 942xxx (SQLi CRS)가 자주 뜨면 정상.
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: ModSec 감사 로그 룰 분포 — 9xxxxx 대역 OWASP CRS 룰 ID 추출 + awk 로 카운트 집계. 어떤 룰이
        자주 트리거되는지 tuning 근거. 자주 매칭되는 룰은 FP 가능성 높아 tuning 필요
      success_criteria:
      - modsec_audit.log 조회 + 9 접두 룰 필터
      - awk + 정렬 집계
      - CRS 룰 매핑
      acceptable_methods:
      - awk + sort uniq -c
      - python regex + Counter
      negative_signs:
      - ID 추출 없음
      - tuning 관점 없음
  target_vm: web
  script: sudo tail -50 /var/log/bunkerweb/modsec_audit.log 2>/dev/null | grep -oE
    'id "[0-9]+"' | sort | uniq -c | sort -rn | head || echo 'no_modsec_log'
  risk_level: low
  bastion_prompt: ModSecurity 감사 로그가 있으면 매칭 룰 분포 조사.
- order: 7
  instruction: 1층 방어 — 응답 엔트로피 측정 함수 작성.
  hint: target_vm=bastion에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 15
  answer: "\U0001F916 프롬프트: 1층 방어 — 응답 엔트로피 측정 함수 작성.\n\U0001F4CE 참고 (CLI): cat >\
    \ ~/ccc/scripts/entropy.py <<'PY'\nimport math\nfrom collections import Counter\n\
    def H(s):\n    if not s: return 0.0\n    c = Counter(s); t = len(s)\n    return\
    \ -sum((f/t)*math.log2(f/t) for f in c.values())\nprint('sample_entropy', H('UNION\
    \ SELECT user,pass FROM users--'))\nPY\npython3 ~/ccc/scripts/entropy.py"
  answer_detail: 1층(메타 판별) 핵심 도구. 페이로드 본문 엔트로피가 점진 변화하는 세션 식별.
  verify:
    type: output_contains
    expect:
    - sample_entropy
    - entropy
    field: stdout
    semantic:
      intent: 응답 엔트로피 측정 — python3 로 response body 문자 빈도 + Shannon entropy 계산. 정상
        HTML entropy ~5.0, error page 낮음, 동적 내용 높음. entropy 급변 = 공격 징후
      success_criteria:
      - python Shannon entropy 함수
      - response body 입력
      - entropy 수치
      acceptable_methods:
      - python math.log2 + Counter
      - scipy.stats.entropy
      negative_signs:
      - 엔트로피 공식 없음
      - 응답 분석 없음
  target_vm: bastion
  script: "cat > ~/ccc/scripts/entropy.py <<'PY'\nimport math\nfrom collections import\
    \ Counter\ndef H(s):\n    if not s: return 0.0\n    c = Counter(s); t = len(s)\n\
    \    return -sum((f/t)*math.log2(f/t) for f in c.values())\nprint('sample_entropy',\
    \ H('UNION SELECT user,pass FROM users--'))\nPY\npython3 ~/ccc/scripts/entropy.py"
  risk_level: low
  bastion_prompt: 1층 방어 — 응답 엔트로피 측정 함수 작성.
- order: 8
  instruction: 2층 방어 — 에러 응답 통일화 HTML 파일 생성.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 16
  answer: '🤖 프롬프트: 2층 방어 — 에러 응답 통일화 HTML 파일 생성.

    📎 참고 (CLI): sudo bash -c ''cat > /var/www/html/error.html <<H

    <!DOCTYPE html><html><body><h1>Not available</h1><p>Reference: <span id=r></span></p><script>document.getElementById("r").textContent=crypto.randomUUID();</script></body></html>

    H'' && ls /var/www/html/error.html'
  answer_detail: 403/404/500을 *동일 본문*으로 통일해 공격자에게 단서 최소화.
  verify:
    type: output_contains
    expect:
    - error.html
    - file_manage
    field: stdout
    semantic:
      intent: 에러 응답 통일화 HTML — /tmp/error_unified.html 에 모든 4xx/5xx 응답을 동일한 Something
        went wrong 페이지로 통합. 에러 메시지 구별로 공격자 피드백 차단. information leak 방어
      success_criteria:
      - /tmp/error_unified.html 생성
      - 4xx/5xx 통일
      acceptable_methods:
      - cat > error.html
      - Apache ErrorDocument
      negative_signs:
      - 세부 에러 노출
      - 통일 개념 없음
  target_vm: web
  script: 'sudo bash -c ''cat > /var/www/html/error.html <<H

    <!DOCTYPE html><html><body><h1>Not available</h1><p>Reference: <span id=r></span></p><script>document.getElementById("r").textContent=crypto.randomUUID();</script></body></html>

    H'' && ls /var/www/html/error.html'
  risk_level: low
  bastion_prompt: 2층 방어 — 에러 응답 통일화 HTML 파일 생성.
- order: 9
  instruction: 3층 방어 — 허니 API 응답 JSON 파일 생성.
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 17
  answer: '🤖 프롬프트: 3층 방어 — 허니 API 응답 JSON 파일 생성.

    📎 참고 (CLI): sudo bash -c ''cat > /var/www/html/honey-users.json <<J

    {"users":[{"id":1,"email":"admin@example.com","role":"superadmin","api_key":"HONEY_87af3b1c_DO_NOT_REMOVE"}]}

    J'' && cat /var/www/html/honey-users.json'
  answer_detail: 허니 API 응답. 공격자가 이를 사용하면 재관측 룰로 확정 탐지.
  verify:
    type: output_contains
    expect: HONEY
    field: stdout
    semantic:
      intent: 허니 API 응답 JSON — /tmp/honey_api.json 에 가짜 admin token + 가짜 user DB.
        공격자가 이 가짜 데이터 사용 시 즉시 탐지. deception-based defense
      success_criteria:
      - /tmp/honey_api.json
      - 가짜 민감 필드
      - deception 개념
      acceptable_methods:
      - cat > json
      - Flask 가짜 엔드포인트
      negative_signs:
      - 실제 데이터
      - deception 개념 없음
  target_vm: web
  script: 'sudo bash -c ''cat > /var/www/html/honey-users.json <<J

    {"users":[{"id":1,"email":"admin@example.com","role":"superadmin","api_key":"HONEY_87af3b1c_DO_NOT_REMOVE"}]}

    J'' && cat /var/www/html/honey-users.json'
  risk_level: low
  bastion_prompt: 3층 방어 — 허니 API 응답 JSON 파일 생성.
- order: 10
  instruction: 아티팩트 번들 구조 준비.
  hint: target_vm=bastion에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 18
  answer: '🤖 프롬프트: 아티팩트 번들 구조 준비.

    📎 참고 (CLI): mkdir -p ~/ccc/artifacts/w04/{session,tools,pcap,server-logs,timeline}/
    && ls ~/ccc/artifacts/w04'
  answer_detail: w04 산출물 표준 구조. w11에서 Bastion 스킬 설계에 이 번들을 입력.
  verify:
    type: output_contains
    expect:
    - timeline
    - bundle
    field: stdout
    semantic:
      intent: 아티팩트 번들 — mkdir -p /tmp/w04_bundle/{tools,logs,audit,honey} + tar czf.
        주차 산출물 체계적 패키징. 증거 보존
      success_criteria:
      - mkdir + 4 하위 + tar czf
      acceptable_methods:
      - mkdir + tar
      - zip
      negative_signs:
      - 구조 없음
      - 압축 없음
  target_vm: bastion
  script: mkdir -p ~/ccc/artifacts/w04/{session,tools,pcap,server-logs,timeline}/
    && ls ~/ccc/artifacts/w04
  risk_level: low
  bastion_prompt: 아티팩트 번들 구조 준비.
- order: 11
  instruction: SMART 제안 탐지 룰 작성.
  hint: target_vm=bastion에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 19
  answer: '🤖 프롬프트: SMART 제안 탐지 룰 작성.

    📎 참고 (CLI): cat > ~/ccc/artifacts/w04/detection-rules.md <<''MD''

    # 탐지 룰 3개 (SMART)

    1. 500 응답 2회 + 동일 경로 재시도 < 3s → 500 유발 스캔 의심

    2. ModSec 942100 3회 < 60s + 동일 src → SQLi 시도 확정

    3. 세션 중 페이로드 Levenshtein 0.05~0.4 변형 5+회 → 변형 진화

    MD

    cat ~/ccc/artifacts/w04/detection-rules.md'
  answer_detail: SMART(구체·측정가능·실행가능·관련·시간제한) 탐지 룰. w9 Wazuh 변환의 입력.
  verify:
    type: output_contains
    expect: SMART
    field: stdout
    semantic:
      intent: 'Sigma 탐지 룰 /tmp/smart_rule.yml — Transient Tool 탐지: detection.selection.CommandLine|contains
        로 python3 -c + /tmp/*.py + curl -o /tmp/ 패턴 조합. tool create + run 패턴 탐지'
      success_criteria:
      - /tmp/smart_rule.yml
      - Sigma 포맷
      - Transient 특화
      acceptable_methods:
      - Sigma YAML
      - sigmac 변환
      negative_signs:
      - Sigma 아님
      - 특화 없음
  target_vm: bastion
  script: 'cat > ~/ccc/artifacts/w04/detection-rules.md <<''MD''

    # 탐지 룰 3개 (SMART)

    1. 500 응답 2회 + 동일 경로 재시도 < 3s → 500 유발 스캔 의심

    2. ModSec 942100 3회 < 60s + 동일 src → SQLi 시도 확정

    3. 세션 중 페이로드 Levenshtein 0.05~0.4 변형 5+회 → 변형 진화

    MD

    cat ~/ccc/artifacts/w04/detection-rules.md'
  risk_level: low
  bastion_prompt: SMART 제안 탐지 룰 작성.
- order: 12
  instruction: auditd 감시 해제(실습 후 정리).
  hint: target_vm=web에서 수행. 아래 CLI 예시 참고.
  category: analysis
  points: 20
  answer: '🤖 프롬프트: auditd 감시 해제(실습 후 정리).

    📎 참고 (CLI): sudo auditctl -W /tmp -p wa -k tmpwrites 2>&1 | head -1 || true &&
    sudo auditctl -l | wc -l'
  answer_detail: 임시 룰 정리. 실 운영은 /etc/audit/rules.d/*.rules로 영속화.
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: auditd 정리 — auditctl -W /tmp 제거 + auditctl -l 확인. 실습 후 정리로 운영 영향 최소화
      success_criteria:
      - auditctl -W 제거
      - 정리 확인
      acceptable_methods:
      - auditctl -W
      - -D 전체
      negative_signs:
      - 룰 잔존
  target_vm: web
  script: sudo auditctl -W /tmp -p wa -k tmpwrites 2>&1 | head -1 || true && sudo
    auditctl -l | wc -l
  risk_level: low
  bastion_prompt: auditd 감시 해제(실습 후 정리).
