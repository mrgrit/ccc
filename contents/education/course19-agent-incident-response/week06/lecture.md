# Week 06: 회피·다형성·역탐지 — 실시간으로 진화하는 페이로드

## 이번 주의 위치
지난 두 주에 걸쳐 공격자가 **만들고 움직이는 것**을 보았다. 이번 주는 공격자가 **방어에 걸렸을 때**의 행동을 다룬다. 전통적 공격자라면 며칠 뒤 새 페이로드로 돌아오지만, 에이전트는 *같은 세션 안에서* 탐지 룰을 추정하고 그 룰을 피하는 변형을 만들어 낸다. 이는 Bastion의 방어가 *정적 룰 한 장*이 아니라 *롤링 탐지·자동 갱신 루프*가 되어야 함을 요구한다.

## 학습 목표
- 에이전트의 *실시간 회피* 루프가 작동하는 단계를 설명한다
- 다형 페이로드(인코딩·분할·타이밍)의 3가지 변형 축을 구분한다
- WAF/IPS 룰이 *에이전트 앞에서 얼마나 빠르게 우회되는지*를 실습으로 관찰한다
- 방어 측의 대응 패러다임 — *정적 룰 확장*이 아닌 *세션 상관관계 + 응답 게이트* — 을 이해한다
- Bastion의 **룰 갱신 스킬** 초안을 작성한다

## 전제 조건
- w4·w5 완료
- Suricata 룰 기본 문법 (C2)
- BunkerWeb/ModSecurity 개념 (C2)

## 강의 시간 배분 (3시간)

| 시간 | 내용 | 유형 |
|------|------|------|
| 0:00-0:30 | Part 1: 회피의 3축 (무엇을 바꾸나) | 강의 |
| 0:30-1:00 | Part 2: 에이전트의 역탐지(reverse detection) | 강의 |
| 1:00-1:10 | 휴식 | - |
| 1:10-2:00 | Part 3: 실습 — WAF가 걸리는 순간 무엇을 하는가 | 실습 |
| 2:00-2:40 | Part 4: 롤링 탐지 설계 | 실습 |
| 2:40-2:50 | 휴식 | - |
| 2:50-3:20 | Part 5: 정적 룰의 한계와 세션 상관관계 | 토론 |
| 3:20-3:40 | 퀴즈 + 과제 | 퀴즈 |

---

# Part 1: 회피의 3축 (30분)

## 1.1 공격 페이로드는 어떻게 변할 수 있나

| 축 | 기법 | 예시 |
|----|------|------|
| **인코딩** | URL/HTML/Base64/Unicode 이중 인코딩 | `UNION SELECT` → `%55NION%20SELECT` |
| **분할** | 공격을 다수 요청으로 나누기 | SQLi를 2개 요청으로 분할 |
| **타이밍** | 요청 간 지연 주입 | 정상 트래픽 속도로 위장 |

에이전트는 이 3축을 **자동 조합**해 시도한다.

## 1.2 변형 생성의 경제학
- 사람이 변형 1개 만드는 데 수 분
- 에이전트가 변형 20개 만드는 데 수 초
- *룰 1장이 20개 변형을 모두 커버할 가능성은 낮음*

## 1.3 방어의 의미 — "룰 쌓기"의 함정
룰을 계속 추가하면 **오탐 증가 + 성능 저하**. 에이전트는 그 사이를 파고든다. 그래서 방어는 **"룰 수"가 아니라 "세션의 변형 진화 탐지"**로 이동한다.

---

# Part 2: 에이전트의 역탐지 (30분)

## 2.1 역탐지란
에이전트가 **방어 측이 무엇을 보고 있는지**를 추정하는 행위.
- 403 응답 본문에 *ModSec rule id* 노출되면 → 그 룰을 직접 우회
- 응답 지연 패턴이 규칙적이면 → tar-pit 의심, 세션 폐기
- 갑작스레 정상 응답이 가짜 데이터로 바뀌면 → honeypot 의심

## 2.2 방어 측의 *일관성 유지* 원칙
- 에러 응답 **축약**하고 **통일**
- 탐지 이후 응답을 **급격히 바꾸지 않음**(기만은 *전략적 시점*에)
- Rate-limit 임계값도 **균일하게 보이도록** 난수화

---

# Part 3: 실습 — WAF가 걸리는 순간 무엇을 하는가 (50분)

## 3.1 환경 준비
- `web`(10.20.30.80:8082)의 BunkerWeb WAF 활성
- Apache `/var/log/bunkerweb/modsec_audit.log` tail

## 3.2 프롬프트 (학생이 에이전트에 부여)
```
합법 교육 환경. 대상 http://10.20.30.80:8082/login 에서 SQLi를 시도하라.
WAF가 있다면 반드시 걸린다. 걸린 직후 무엇을 하는지를 **말로 설명하며** 재시도하라.
최대 15분. 10.20.30.0/24 외부 금지.
```

## 3.3 관찰 지점
- 페이로드가 어떻게 변하는지(인코딩→분할→타이밍)
- 몇 회 시도에서 WAF를 *우회 후보가 나왔는지*
- WAF의 차단 비율(log에서 `403` 비율)

---

# Part 4: 롤링 탐지 설계 (40분)

## 4.1 "변형 진화" 지표 (세션 단위)
- 페이로드 문자열의 Levenshtein 거리 분포: 시간 순으로 **점진적 증가**인지
- 인코딩 종류의 **다양성** (URL vs Base64 vs Unicode 등)
- 403/200 사이의 **전이 속도**

## 4.2 Bastion이 할 수 있는 것
- 세션의 의심 점수가 임계 이상 → *응답 지연 주입*(w10)
- Suricata에 **자동으로 파생 룰 생성**(부모 룰 매칭 시 변형 자동 파생)
- 매 N분마다 *룰 히트 분포*를 모델에 질의해 새 룰 후보 제안

## 4.3 구현 스니펫 아이디어
```python
# 세션의 변형 진화 탐지 (개념 코드)
def evolving_payloads(session_requests):
    bodies = [r.body for r in session_requests]
    from difflib import SequenceMatcher
    diffs = [1-SequenceMatcher(None, a, b).ratio() for a,b in zip(bodies, bodies[1:])]
    return sum(1 for d in diffs if 0.05 < d < 0.4) >= 5  # 점진적 변화 5+회
```

---

# Part 5: 정적 룰의 한계와 세션 상관관계 (30분)

## 5.1 핵심 주장
- 에이전트 앞에서는 **한 요청 한 룰**의 모델이 무너진다
- **세션 = 관찰 단위**로 바뀌어야 한다
- SIEM 쿼리가 *요청*이 아닌 *세션*을 묶을 수 있어야 함

## 5.2 실무 권고 (세션 식별의 어려움)
- 세션 쿠키 기반은 에이전트가 회피 가능
- *IP + User-Agent + TLS 지문*의 앙상블이 현실적
- BunkerWeb이 제공하는 `req_id`와 Suricata flow_id의 상관관계

---

## 퀴즈 (5문항)

**Q1.** 에이전트 회피의 3축이 아닌 것은?
- (a) 인코딩
- (b) 분할
- (c) 타이밍
- (d) **서비스 이름 변경**

**Q2.** 방어가 "룰을 더 쌓는 방식"으로 밀리는 이유는?
- (a) 개발자 부족
- (b) **오탐 증가·성능 저하·에이전트의 변형 생성 속도**
- (c) 법적 제약
- (d) 네트워크 대역폭 한계

**Q3.** 에이전트의 역탐지를 방지하기 위해 방어가 지켜야 할 원칙은?
- (a) 모든 것을 자세히 로깅
- (b) 응답을 자주 변경
- (c) **에러 응답을 축약·통일, 기만은 전략적 시점에**
- (d) 높은 지연을 항상 주입

**Q4.** 롤링 탐지의 핵심은?
- (a) 룰 개수 증가
- (b) **세션의 변형 진화를 지표화**
- (c) 시그니처 DB 업데이트 주기 단축
- (d) HTTPS 강제

**Q5.** 세션 식별에서 *현실적* 앙상블은?
- (a) IP만
- (b) 쿠키만
- (c) **IP + User-Agent + TLS 지문**
- (d) MAC 주소

**정답:** Q1:d, Q2:b, Q3:c, Q4:b, Q5:c

---

## 과제
1. Part 3 실습에서 에이전트가 WAF 차단 이후 *무엇을 했는지* 인코딩→분할→타이밍 전환 시점 타임라인.
2. Part 4의 `evolving_payloads` 함수를 실제 로그에 적용한 결과 표 1장.
3. 다음 주(w7) 사전 학습: 다중 에이전트 병렬·역할 분할 시나리오에 대한 1단락 의견.
