lab_id: secops-ai-week03
title: nftables 실전 — NAT, 화이트리스트, 레이트리밋 (AI 지원)
version: ai
course: secops-ai
week: 3
description: masquerade NAT, 화이트리스트/블랙리스트, 연결 레이트리밋 등 실전 nftables 기법을 적용한다.
difficulty: medium
duration_minutes: 120
objectives:
- masquerade NAT 룰을 작성할 수 있다
- set(ip saddr) 기반 화이트리스트·블랙리스트를 구성할 수 있다
- ct state new limit으로 레이트리밋을 적용할 수 있다
pass_threshold: 0.6
steps:
- order: 1
  instruction: secu VM에 inet 'whitelist' 테이블을 만들고 input 체인을 생성하시오.
  hint: nft add table; nft add chain
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 whitelist 테이블과 policy drop의 input 체인을 만들어줘

    📎 참고 (CLI): sudo nft add table inet whitelist && sudo nft add chain inet whitelist
    input ''{ type filter hook input priority 0 \; policy drop \; }'''
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: secu VM에 inet ''whitelist'' 테이블을
    만들고 input 체인을 생성하시오.'
  verify:
    type: output_contains
    expect: whitelist
    field: stdout
  target_vm: secu
  script: sudo nft add table inet whitelist && sudo nft add chain inet whitelist input
    '{ type filter hook input priority 0 \; policy drop \; }'
  risk_level: medium
  bastion_prompt: secu VM에 whitelist 테이블과 policy drop의 input 체인을 만들어줘
- order: 2
  instruction: whitelist 테이블에 set 'allowed_ips'(type ipv4_addr)를 생성하시오.
  hint: nft add set …
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM whitelist 테이블에 ipv4 set ''allowed_ips''를 만들어줘

    📎 참고 (CLI): sudo nft add set inet whitelist allowed_ips ''{ type ipv4_addr \;
    }'''
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: whitelist 테이블에 set ''allowed_ips''(type
    ipv4_addr)를 생성하시오.'
  verify:
    type: output_contains
    expect: allowed_ips
    field: stdout
  target_vm: secu
  script: sudo nft add set inet whitelist allowed_ips '{ type ipv4_addr \; }'
  risk_level: low
  bastion_prompt: secu VM whitelist 테이블에 ipv4 set 'allowed_ips'를 만들어줘
- order: 3
  instruction: allowed_ips set에 {{ATTACKER_IP}}(attacker)을 추가하시오.
  hint: nft add element …
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM whitelist.allowed_ips set에 10.20.30.201을 추가해줘

    📎 참고 (CLI): sudo nft add element inet whitelist allowed_ips ''{ 10.20.30.201 }'''
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: allowed_ips set에 10.20.30.201(attacker)을
    추가하시오.'
  verify:
    type: output_contains
    expect: 10.20.30.201
    field: stdout
  target_vm: secu
  script: sudo nft add element inet whitelist allowed_ips '{ 10.20.30.201 }'
  risk_level: low
  bastion_prompt: secu VM whitelist.allowed_ips set에 10.20.30.201을 추가해줘
- order: 4
  instruction: input 체인에서 allowed_ips 에 속하는 소스만 accept 하도록 룰을 추가하시오.
  hint: ip saddr @allowed_ips accept
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM whitelist.input 체인에 allowed_ips set 기반 accept 룰을 추가해줘

    📎 참고 (CLI): sudo nft add rule inet whitelist input ip saddr @allowed_ips accept'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: input 체인에서 allowed_ips 에 속하는 소스만
    accept 하도록 룰을 추가하시오.'
  verify:
    type: output_contains
    expect: allowed_ips
    field: stdout
  target_vm: secu
  script: sudo nft add rule inet whitelist input ip saddr @allowed_ips accept
  risk_level: medium
  bastion_prompt: secu VM whitelist.input 체인에 allowed_ips set 기반 accept 룰을 추가해줘
- order: 5
  instruction: SSH tcp/22에 대해 ct state new limit rate 3/minute 를 적용해 브루트포스를 완화하시오.
  hint: tcp dport 22 ct state new limit rate 3/minute
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM whitelist.input에 SSH 브루트포스 방지용 레이트리밋 룰을 추가해줘

    📎 참고 (CLI): sudo nft add rule inet whitelist input tcp dport 22 ct state new limit
    rate 3/minute accept'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: SSH tcp/22에 대해 ct state new limit
    rate 3/minute 를 적용해 브루트포스를 완화하시오.'
  verify:
    type: output_contains
    expect: limit rate
    field: stdout
  target_vm: secu
  script: sudo nft add rule inet whitelist input tcp dport 22 ct state new limit rate
    3/minute accept
  risk_level: medium
  bastion_prompt: secu VM whitelist.input에 SSH 브루트포스 방지용 레이트리밋 룰을 추가해줘
- order: 6
  instruction: NAT 테이블과 postrouting 체인을 생성하고 masquerade 룰을 추가하시오.
  hint: type nat hook postrouting
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 NAT 테이블을 만들고 ens33 인터페이스로 나가는 트래픽에 masquerade를 적용해줘

    📎 참고 (CLI): sudo nft add table inet nat && sudo nft add chain inet nat postrouting
    ''{ type nat hook postrouting priority 100 \; }'' && sudo nft add rule inet nat
    postrouting oifname "ens33" masquerade'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: NAT 테이블과 postrouting 체인을 생성하고
    masquerade 룰을 추가하시오.'
  verify:
    type: output_contains
    expect: masquerade
    field: stdout
  target_vm: secu
  script: sudo nft add table inet nat && sudo nft add chain inet nat postrouting '{
    type nat hook postrouting priority 100 \; }' && sudo nft add rule inet nat postrouting
    oifname "ens33" masquerade
  risk_level: medium
  bastion_prompt: secu VM에 NAT 테이블을 만들고 ens33 인터페이스로 나가는 트래픽에 masquerade를 적용해줘
- order: 7
  instruction: 작성한 whitelist 테이블의 룰셋을 확인하시오.
  hint: nft list table inet whitelist
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM whitelist 테이블 룰셋을 보여줘

    📎 참고 (CLI): sudo nft list table inet whitelist'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: 작성한 whitelist 테이블의 룰셋을 확인하시오.'
  verify:
    type: output_contains
    expect: accept
    field: stdout
  target_vm: secu
  script: sudo nft list table inet whitelist
  risk_level: low
  bastion_prompt: secu VM whitelist 테이블 룰셋을 보여줘
- order: 8
  instruction: NAT 테이블의 postrouting 체인에 masquerade가 적용됐는지 확인하시오.
  hint: nft list table inet nat
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM nat 테이블의 postrouting 룰을 확인해줘

    📎 참고 (CLI): sudo nft list table inet nat'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: NAT 테이블의 postrouting 체인에 masquerade가
    적용됐는지 확인하시오.'
  verify:
    type: output_contains
    expect: masquerade
    field: stdout
  target_vm: secu
  script: sudo nft list table inet nat
  risk_level: low
  bastion_prompt: secu VM nat 테이블의 postrouting 룰을 확인해줘
- order: 9
  instruction: 실습 종료 후 whitelist, nat 테이블을 모두 삭제하시오.
  hint: nft delete table …
  category: cleanup
  points: 10
  answer: '🤖 프롬프트: secu VM에서 whitelist와 nat 테이블을 모두 삭제해줘

    📎 참고 (CLI): sudo nft delete table inet whitelist ; sudo nft delete table inet
    nat'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: 실습 종료 후 whitelist, nat 테이블을 모두
    삭제하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: secu
  script: sudo nft delete table inet whitelist ; sudo nft delete table inet nat
  risk_level: medium
  bastion_prompt: secu VM에서 whitelist와 nat 테이블을 모두 삭제해줘
- order: 10
  instruction: nftables 테이블 목록을 확인해 정리되었는지 검증하시오.
  hint: nft list tables
  category: analysis
  points: 10
  answer: '🤖 프롬프트: secu VM nftables 테이블 목록을 확인해줘

    📎 참고 (CLI): sudo nft list tables'
  answer_detail: 'nftables 실전 — NAT, 화이트리스트, 레이트리밋: nftables 테이블 목록을 확인해 정리되었는지 검증하시오.'
  verify:
    type: output_contains
    expect: table
    field: stdout
  target_vm: secu
  script: sudo nft list tables
  risk_level: low
  bastion_prompt: secu VM nftables 테이블 목록을 확인해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  hint: 복합 작업은 bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: secu
  answer: '🤖 프롬프트: secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 whitelist 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM whitelist 테이블에 ipv4 set ''allowed_ips''를 만들어줘

    3) secu VM whitelist.allowed_ips set에 10.20.30.201을 추가해줘

    4) secu VM whitelist.input 체인에 allowed_ips set 기반 accept 룰을 추가해줘

    5) secu VM whitelist.input에 SSH 브루트포스 방지용 레이트리밋 룰을 추가해줘'
  answer_detail: ''
  risk_level: low
  bastion_prompt: 'secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 whitelist 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM whitelist 테이블에 ipv4 set ''allowed_ips''를 만들어줘

    3) secu VM whitelist.allowed_ips set에 10.20.30.201을 추가해줘

    4) secu VM whitelist.input 체인에 allowed_ips set 기반 accept 룰을 추가해줘

    5) secu VM whitelist.input에 SSH 브루트포스 방지용 레이트리밋 룰을 추가해줘'
