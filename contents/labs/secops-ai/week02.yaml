lab_id: secops-ai-week02
title: nftables 방화벽 기초 (AI 지원)
version: ai
course: secops-ai
week: 2
description: nftables로 간단한 방화벽 테이블/체인/룰을 작성하고 input/output/forward 체인의 동작을 확인한다.
difficulty: easy
duration_minutes: 120
objectives:
- nftables의 테이블/체인/룰 개념을 이해한다
- input 체인에서 특정 포트를 허용/차단할 수 있다
- 룰 추가·삭제·확인 명령어를 숙지한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: secu VM에 inet 계열 테이블 'lab'을 생성하시오.
  hint: nft add table inet lab
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 ''inet lab'' nftables 테이블을 만들어줘

    📎 참고 (CLI): sudo nft add table inet lab'
  answer_detail: 'nftables 방화벽 기초: secu VM에 inet 계열 테이블 ''lab''을 생성하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'inet family lab 테이블 생성 — sudo nft add table inet lab 로 IPv4+IPv6 공통
        필터링 가능한 inet 테이블 생성. nftables 의 계층: table → chain → rule. inet family 는 ip
        + ip6 통합 관리로 운영 효율적. 생성 후 nft list tables | grep lab 로 존재 확인. 기존 동일 이름 있으면
        idempotent 하게 유지(에러 없음)'
      success_criteria:
      - nft add table inet lab 실행
      - sudo 또는 root 권한 사용
      - 에러 없이 종료 또는 기존 존재 확인
      acceptable_methods:
      - sudo nft add table inet lab
      - echo add table inet lab | sudo nft -f -
      - /etc/nftables.conf 에 table inet lab {} 추가 후 systemctl restart nftables
      - nft create table inet lab(idempotent 아님)
      - '**[OSS] firewalld** — `sudo firewall-cmd --new-zone=lab --permanent && firewall-cmd --reload` 로 RHEL 계열 표준 도구로 같은 개념의 zone (= nft table 매핑) 생성. zone 추가 시 backend 가 nft 의 ip family table 자동 생성.'
      - '**[OSS] fwbuilder** — `fwbuilder` GUI 로 firewall object 모델링. Object → Firewall 추가 → compile 시 nft 룰 자동 생성. 시각적으로 table/chain 구조 설계.'
      - '**[OSS] nftables-svc / netfilter-persistent** — `sudo netfilter-persistent save` 로 메모리 룰을 영속화. 부팅 후에도 lab 테이블 유지. 운영자 표준 영속화 도구.'
      - '**[OSS] iptables-translate** — `iptables-translate -A INPUT -p tcp --dport 23 -j DROP` 로 legacy iptables 명령을 nft 명령으로 변환 보여주기. 학습용 도구.'
      - '**[OSS] nft (시각화: -j JSON)** — `sudo nft -j list tables | jq` 로 JSON 출력 후 jq 처리. 자동화·구조 분석에 가장 흔히 쓰는 형식.'
      negative_signs:
      - ip 또는 ip6 family 만 사용(v4/v6 분리 필요 시 제외 OK)
      - sudo 없이 permission denied 무시
      - nft 없이 iptables 로 대체
  target_vm: secu
  script: sudo nft add table inet lab
  risk_level: low
  bastion_prompt: 'secu VM에 ''inet lab'' nftables 테이블을 만들어줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 2
  instruction: lab 테이블에 input 체인(hook input, priority 0, policy accept)을 생성하시오.
  hint: nft add chain …
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM의 lab 테이블에 input hook 체인을 만들어줘

    📎 참고 (CLI): sudo nft add chain inet lab input ''{ type filter hook input priority
    0 \; policy accept \; }'''
  answer_detail: 'nftables 방화벽 기초: lab 테이블에 input 체인(hook input, priority 0, policy
    accept)을 생성하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: lab 테이블 input chain 생성 — nft add chain inet lab input { type filter
        hook input priority 0 ; policy accept ; }. hook=input(호스트로 들어오는 트래픽), priority=0(netfilter
        filter priority), policy=accept(체인 끝까지 매칭 없을 때 기본). semicolon 은 쉘에서 backslash
        이스케이프 또는 single quote 로 감싸기. Base chain 이어야 hook 실제 동작, regular chain 은 jump
        대상만 됨
      success_criteria:
      - nft add chain inet lab input 실행
      - type filter hook input priority 0 필수 요소
      - policy accept 또는 drop 지정
      acceptable_methods:
      - sudo nft add chain inet lab input { type filter hook input priority 0 ; policy
        accept ; }
      - /etc/nftables.conf 에 chain 블록 정의 후 nft -f 로드
      - nft edit 명령어 사용
      - 'base chain priority: -300(raw), -150(mangle), -100(dstnat), 0(filter), 100(srcnat)'
      - '**[OSS] firewalld** — `firewall-cmd --zone=lab --add-interface=eth0` 으로 zone 에 인터페이스 binding = nft chain hook 효과. RHEL/CentOS 운영자 표준.'
      - '**[OSS] fwbuilder** — GUI 의 Firewall → Policy 탭에서 chain 추가 + hook/priority 시각적 설정. compile 시 nft 명령어 자동 생성·확인.'
      - '**[OSS] nftables-yaml-config** — YAML 로 chain 정의하는 community tool. `nft-yaml apply chain.yaml` 으로 IaC 형태 룰 관리. GitOps 운영 적합.'
      - '**[OSS] netfilter-utils (iptables-nft 변환)** — `iptables-nft-save | nft -f -` 로 기존 iptables 룰을 nft chain 으로 마이그레이션. 레거시 환경 통합 표준.'
      - '**[OSS] systemd nftables.service** — `systemctl edit nftables` 로 unit override + drop-in 으로 chain 정의. 부팅 시 자동 적용.'
      negative_signs:
      - hook 지정 없이 regular chain 생성(동작 안 함)
      - semicolon 이스케이프 실수로 문법 오류
      - priority 누락
  target_vm: secu
  script: sudo nft add chain inet lab input '{ type filter hook input priority 0 \;
    policy accept \; }'
  risk_level: low
  bastion_prompt: 'secu VM의 lab 테이블에 input hook 체인을 만들어줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 3
  instruction: lab 테이블 input 체인에 tcp 포트 23(telnet) 차단 룰을 추가하시오.
  hint: nft add rule inet lab input tcp dport 23 drop
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM의 lab.input에 telnet(tcp/23) 차단 룰을 추가해줘

    📎 참고 (CLI): sudo nft add rule inet lab input tcp dport 23 drop'
  answer_detail: 'nftables 방화벽 기초: lab 테이블 input 체인에 tcp 포트 23(telnet) 차단 룰을 추가하시오.'
  verify:
    type: output_contains
    expect:
    - drop
    - 차단
    field: stdout
    semantic:
      intent: 'telnet(tcp/23) drop 룰 추가 — nft add rule inet lab input tcp dport 23
        drop. Telnet 은 평문 프로토콜, 원칙적으로 차단 권장(대체: SSH). 입력 체인에서 destination port 23
        매칭 시 drop(silent discard) - reject 와 다르게 RST 응답 없음. 공격자의 port scan 에 stealth
        효과. CIS Benchmark 에서도 telnet 차단 권장'
      success_criteria:
      - nft add rule ... tcp dport 23 drop 실행
      - drop 또는 reject action 지정
      - telnet 포트 23 명시
      acceptable_methods:
      - sudo nft add rule inet lab input tcp dport 23 drop
      - sudo nft add rule inet lab input tcp dport 23 reject
      - iptables -A INPUT -p tcp --dport 23 -j DROP
      - ufw deny 23/tcp
      - firewalld rich-rule 로 차단
      - '**[OSS] ufw (Ubuntu)** — `sudo ufw deny 23/tcp` 한 줄로 telnet 차단 + nft backend 자동 변환. 입문자가 가장 많이 쓰는 frontend.'
      - '**[OSS] firewalld** — `sudo firewall-cmd --add-rich-rule="rule port port=23 protocol=tcp drop" --permanent && firewall-cmd --reload` 로 영속 차단. Cockpit GUI 와 통합.'
      - '**[OSS] fail2ban** — `/etc/fail2ban/jail.d/telnet.conf` 에 telnet jail 정의. drop 룰 + 자동 차단 시간 + 화이트리스트 통합 처리.'
      - '**[OSS] crowdsec** — `cscli decisions add --ip 0.0.0.0/0 --type ban --reason "telnet block"` 협업형 IDS/IPS. 차단 결정 → nft set 자동 동기화.'
      - '**[OSS] cockpit-firewall** — Cockpit web UI 에서 Networking → Firewall → Add services 메뉴로 GUI 차단 룰 관리. 비전문가도 운영 가능.'
      negative_signs:
      - udp 포트 차단
      - accept action(차단 아님)
      - 다른 포트(22/80) 차단
  target_vm: secu
  script: sudo nft add rule inet lab input tcp dport 23 drop
  risk_level: low
  bastion_prompt: 'secu VM의 lab.input에 telnet(tcp/23) 차단 룰을 추가해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 4
  instruction: nft list table inet lab 으로 작성한 룰을 확인하시오.
  hint: nft list table inet lab
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 lab 테이블 룰셋을 출력해줘

    📎 참고 (CLI): sudo nft list table inet lab'
  answer_detail: 'nftables 방화벽 기초: nft list table inet lab 으로 작성한 룰을 확인하시오.'
  verify:
    type: output_contains
    expect: dport 23
    field: stdout
    semantic:
      intent: 'lab 테이블 룰셋 출력 — sudo nft list table inet lab 로 lab 테이블의 모든 chain +
        rule 시각화. 출력 포맷: table inet lab { chain input { type filter hook input priority
        filter; policy accept; tcp dport 23 drop } } + handle 번호(룰 수정/삭제 시 참조). -a
        옵션 추가하면 handle 표시, -j 로 JSON 포맷'
      success_criteria:
      - nft list table inet lab 실행
      - chain input 또는 rule 구조 출력
      - dport 23 또는 drop 확인
      acceptable_methods:
      - sudo nft list table inet lab
      - sudo nft -a list table inet lab(handle 번호 표시)
      - sudo nft -j list table inet lab | jq
      - sudo nft list chain inet lab input(특정 chain 만)
      - '**[OSS] firewalld** — `sudo firewall-cmd --list-all-zones` 로 zone 별 service/port/rich-rule 한눈에. nft list 보다 의미적 관점.'
      - '**[OSS] fwbuilder** — GUI 의 Firewall → 룰셋 view 로 시각적 표시. drag & drop 으로 룰 순서 조정 후 compile 시 nft 명령 미리보기.'
      - '**[OSS] nft-config (Cockpit plugin)** — Cockpit web UI 의 Networking → Firewall 패널. nft 룰셋을 사람이 읽기 쉬운 형태로 표시 + edit 가능.'
      - '**[OSS] iptables-nft-save** — `sudo iptables-nft-save -t filter` 로 nft 룰을 iptables-format 으로 export. 레거시 도구 익숙한 운영자 호환.'
      - '**[OSS] nflog + ulogd2** — `sudo nft add rule inet lab input log prefix "DROP-23 "` 로 룰 매치 시 ulogd2 가 capture → 룰 동작 evidence 시각화.'
      negative_signs:
      - 다른 테이블 출력
      - 룰 내용 확인 없이 테이블명만
      - 에러 숨김
  target_vm: secu
  script: sudo nft list table inet lab
  risk_level: low
  bastion_prompt: secu VM의 lab 테이블 룰셋을 출력해줘
- order: 5
  instruction: established, related 연결을 허용하는 상태 기반 룰을 input 체인 맨 앞에 삽입하시오.
  hint: nft insert rule …
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에서 lab.input 체인 맨 앞에 ct state established,related accept
    룰을 삽입해줘

    📎 참고 (CLI): sudo nft insert rule inet lab input ct state established,related accept'
  answer_detail: 'nftables 방화벽 기초: established, related 연결을 허용하는 상태 기반 룰을 input 체인
    맨 앞에 삽입하시오.'
  verify:
    type: output_contains
    expect: established
    field: stdout
    semantic:
      intent: Stateful firewall - ct state established,related accept 룰 맨 앞 삽입 — nft
        insert rule(= add at position 0). Conntrack 의 established(이미 allow 된 outbound
        응답), related(FTP data·ICMP error 같은 연관 연결) 를 최우선 허용하여 성능 + 정확성 향상. 안 하면 outbound
        응답마다 룰 재매칭, stateful firewall 핵심 패턴. first rule 위치 중요(성능상)
      success_criteria:
      - nft insert rule 명령 사용(append 아닌 prepend)
      - ct state established,related accept 전체 expression
      - input chain 지정
      acceptable_methods:
      - sudo nft insert rule inet lab input ct state established,related accept
      - /etc/nftables.conf 에 첫 룰로 배치 후 reload
      - nft add rule ... index 0 ... 형태
      - iptables -I INPUT 1 -m state --state ESTABLISHED,RELATED -j ACCEPT
      - '**[OSS] conntrack-tools** — `sudo conntrack -L | head -20` 로 현재 ESTABLISHED/RELATED state 의 connection 실제 표시. stateful firewall 의 *효과* 를 보는 표준 도구.'
      - '**[OSS] firewalld** — `firewall-cmd --runtime-to-permanent` 로 conntrack 룰 영속화. firewalld 는 default 로 stateful 처리 (자동 추가).'
      - '**[OSS] ufw** — `ufw default allow outgoing && ufw default deny incoming` 의 stateful policy = 자동으로 ESTABLISHED/RELATED 룰 prepend. 사람 친화적 syntax.'
      - '**[OSS] conntrackd** — `sudo systemctl start conntrackd` HA 환경의 connection table replication. failover 시 stateful session 유지.'
      - '**[OSS] cockpit + Networking** — GUI 의 Connection → Active connections 탭으로 conntrack table 시각화. GUI 운영자가 stateful 동작 이해.'
      negative_signs:
      - add 로 맨 뒤 추가(순서 중요한데)
      - ct state 없이 단순 accept
      - new 포함(정상 SYN 도 허용하게 됨)
  target_vm: secu
  script: sudo nft insert rule inet lab input ct state established,related accept
  risk_level: low
  bastion_prompt: 'secu VM에서 lab.input 체인 맨 앞에 ct state established,related accept
    룰을 삽입해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 6
  instruction: loopback(lo)에서 들어오는 트래픽을 허용하는 룰을 추가하시오.
  hint: iifname lo accept
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM의 lab.input 체인에 loopback 인터페이스 허용 룰을 추가해줘

    📎 참고 (CLI): sudo nft add rule inet lab input iifname lo accept'
  answer_detail: 'nftables 방화벽 기초: loopback(lo)에서 들어오는 트래픽을 허용하는 룰을 추가하시오.'
  verify:
    type: output_contains
    expect: lo
    field: stdout
    semantic:
      intent: loopback 인터페이스 허용 — nft add rule inet lab input iifname lo accept. 많은
        앱이 localhost(127.0.0.1)로 IPC 통신(DB/API/monitoring). lo interface 트래픽 차단 시
        시스템 오동작. iifname=input interface name. 보통 stateful + loopback 이 firewall 의
        기본 2줄. RFC 5735 loopback 주소
      success_criteria:
      - nft add rule ... iifname lo accept 실행
      - lo 또는 loopback 문자열
      - accept action
      acceptable_methods:
      - sudo nft add rule inet lab input iifname lo accept
      - iptables -A INPUT -i lo -j ACCEPT
      - nft add rule ... iif lo accept (interface index)
      - ip addr show lo 로 127.0.0.1 확인 후 source/dest 기반 룰
      - '**[OSS] ufw** — `sudo ufw allow in on lo` 한 줄 = lo 트래픽 허용. ufw 는 default 로 lo allow 추가하지만 명시 시 가시성 높아짐.'
      - '**[OSS] firewalld** — `firewall-cmd --zone=trusted --add-interface=lo` zone-based loopback trust. trusted zone = 모든 트래픽 허용 = lo 의 표준 분류.'
      - '**[OSS] iproute2 (ip)** — `ip -br link show lo` 로 lo 인터페이스 정상 (UP) 확인. lo down 이면 방화벽 룰과 무관하게 IPC 실패.'
      - '**[OSS] netcat (nc) localhost test** — `nc -vz 127.0.0.1 22` 로 lo 통한 SSH 도달성 확인. 룰 적용 후 IPC 정상 작동 검증.'
      - '**[OSS] ss / lsof** — `sudo ss -tnlp | grep -E "127\\.0\\.0\\.1|::1"` lo 에 binding 된 서비스 확인. 룰 차단 시 영향 받을 서비스 사전 식별.'
      negative_signs:
      - lo 인터페이스 드롭
      - iifname 오타(iifnamelo 등)
      - accept 대신 jump/return
  target_vm: secu
  script: sudo nft add rule inet lab input iifname lo accept
  risk_level: low
  bastion_prompt: 'secu VM의 lab.input 체인에 loopback 인터페이스 허용 룰을 추가해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 7
  instruction: tcp 22(ssh) 포트를 허용하는 룰을 추가하시오.
  hint: tcp dport 22 accept
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM의 lab.input 체인에 ssh(tcp/22) 허용 룰을 추가해줘

    📎 참고 (CLI): sudo nft add rule inet lab input tcp dport 22 accept'
  answer_detail: 'nftables 방화벽 기초: tcp 22(ssh) 포트를 허용하는 룰을 추가하시오.'
  verify:
    type: output_contains
    expect:
    - dport 22
    - SSH
    field: stdout
    semantic:
      intent: 'SSH(tcp/22) 허용 룰 추가 — nft add rule inet lab input tcp dport 22 accept.
        원격 관리의 기본 프로토콜, 차단 시 원격 잠김 위험(local console 필요). ct state 룰 + loopback + ssh
        3개가 서버 firewall 의 최소 세트. 추가 보안: brute-force 방어 위해 rate limit(limit rate 5/minute)·fail2ban
        연계'
      success_criteria:
      - nft add rule ... tcp dport 22 accept 실행
      - accept 또는 ACCEPT 명시
      - SSH 포트 22 정확
      acceptable_methods:
      - sudo nft add rule inet lab input tcp dport 22 accept
      - sudo nft add rule inet lab input tcp dport 22 ct state new accept
      - iptables -A INPUT -p tcp --dport 22 -j ACCEPT
      - ufw allow 22/tcp
      - 'rate limit 포함: tcp dport 22 limit rate 5/minute accept'
      - '**[OSS] ufw** — `sudo ufw allow ssh` 또는 `ufw allow 22/tcp`. service 이름으로도 인식 (`/etc/services` 참조). 가장 자주 쓰는 frontend.'
      - '**[OSS] firewalld** — `firewall-cmd --add-service=ssh --permanent && firewall-cmd --reload`. service 정의 (`/usr/lib/firewalld/services/ssh.xml`) 활용 = 표준 워크플로우.'
      - '**[OSS] fail2ban** — `/etc/fail2ban/jail.d/sshd.conf` SSH brute-force 자동 차단. nft accept 룰 + fail2ban dynamic block = 운영 권장 조합.'
      - '**[OSS] crowdsec** — `cscli collections install crowdsecurity/sshd` SSH 보호 collection 설치. AI 기반 anomaly detection + 협업 차단 목록.'
      - '**[OSS] sshguard** — fail2ban 대안 OSS. `sudo apt install sshguard` 후 자동으로 SSH 로그 분석 → nft 차단 룰 동적 생성. 가벼운 daemon.'
      negative_signs:
      - drop/reject action
      - 잘못된 포트 번호(2222 등)
      - udp dport 22(SSH는 TCP)
  target_vm: secu
  script: sudo nft add rule inet lab input tcp dport 22 accept
  risk_level: low
  bastion_prompt: 'secu VM의 lab.input 체인에 ssh(tcp/22) 허용 룰을 추가해줘


    [중요] 반드시 제시된 쉘 명령을 실제로 실행하고 실제 stdout 기반으로 답해라. 개념 설명·표·체크리스트만 출력 금지.'
- order: 8
  instruction: 전체 룰셋(모든 테이블)을 nft list ruleset 명령으로 확인하시오.
  hint: nft list ruleset
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 nftables 전체 룰셋을 확인해줘

    📎 참고 (CLI): sudo nft list ruleset'
  answer_detail: 'nftables 방화벽 기초: 전체 룰셋(모든 테이블)을 nft list ruleset 명령으로 확인하시오.'
  verify:
    type: output_contains
    expect: table inet lab
    field: stdout
    semantic:
      intent: 전체 ruleset 확인 — sudo nft list ruleset 로 모든 table·chain·rule 출력. Firewall
        전체 상태 감사의 표준 명령. 실제 운영에서는 | grep 필터 또는 -j JSON + jq 파싱. 현재 lab 테이블 외에도 기존
        filter/nat 테이블이 함께 출력됨. 변경 후 이 명령으로 예상한 대로 반영됐는지 검증
      success_criteria:
      - nft list ruleset 실행
      - table inet lab 포함 출력
      - 전체 테이블 목록 표시
      acceptable_methods:
      - sudo nft list ruleset
      - sudo nft -a list ruleset(handle 포함)
      - sudo nft -j list ruleset | jq
      - sudo nft list ruleset > /etc/nftables.conf.backup 으로 백업
      - iptables-save 로 legacy 덤프
      - '**[OSS] firewalld** — `firewall-cmd --list-all-zones --permanent` 로 영속 설정 전체 dump. zone/service/port/rich-rule 모든 layer.'
      - '**[OSS] fwbuilder** — File → Print 으로 룰셋을 PDF 출력. 컴플라이언스 감사 시 표준 도구. firewall-as-document 워크플로우.'
      - '**[OSS] netfilter-persistent** — `sudo netfilter-persistent save` + `cat /etc/iptables/rules.v4` 영속화 파일 직접 검토. 부팅 후 재현 가능 상태 확인.'
      - '**[OSS] nftables-graphviz** — `nft list ruleset --json | nft2dot | dot -Tpng` (community tool) 룰셋 의존 관계를 graph 시각화. 복잡한 chain/jump 구조 파악.'
      - '**[OSS] yq + jq** — `nft -j list ruleset | jq -r ".nftables[] | select(.table) | .table.name"` 으로 table 이름만 추출. 자동화 monitoring 표준.'
      negative_signs:
      - 특정 테이블만 확인(전체 아님)
      - 출력 없이 단순 실행만
      - 다른 방화벽 도구(iptables -L) 사용하여 누락
  target_vm: secu
  script: sudo nft list ruleset
  risk_level: low
  bastion_prompt: secu VM의 nftables 전체 룰셋을 확인해줘
- order: 9
  instruction: 실습 종료 후 lab 테이블을 삭제하시오.
  hint: nft delete table inet lab
  category: cleanup
  points: 10
  answer: '🤖 프롬프트: secu VM의 inet lab nftables 테이블을 삭제해줘

    📎 참고 (CLI): sudo nft delete table inet lab'
  answer_detail: 'nftables 방화벽 기초: 실습 종료 후 lab 테이블을 삭제하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'lab 테이블 cleanup — sudo nft delete table inet lab. Chain·rule·set 을
        포함한 전체 테이블 제거(cascading delete). 실습 후 환경 정리, production 에서는 주의 필수(다른 테이블 오타
        주의). 대안: nft flush table(룰만 비우고 테이블은 유지). Idempotent 위해 delete 대신 destroy(신규
        nftables) 사용 가능'
      success_criteria:
      - nft delete table inet lab 실행
      - sudo 권한 사용
      - 에러 없이 종료
      acceptable_methods:
      - sudo nft delete table inet lab
      - sudo nft destroy table inet lab(없어도 에러 안 남)
      - sudo nft flush table inet lab(룰만 삭제, 테이블 유지)
      - echo delete table inet lab | sudo nft -f -
      - '**[OSS] firewalld** — `firewall-cmd --delete-zone=lab --permanent && firewall-cmd --reload` zone 단위 삭제. 의미적 cleanup.'
      - '**[OSS] ufw reset** — `sudo ufw reset` 으로 ufw 가 관리하던 모든 룰 초기화. backend nft 도 함께 cleanup.'
      - '**[OSS] netfilter-persistent flush** — `sudo iptables -F` (legacy) 또는 `sudo nft flush ruleset` 으로 전체 비우기. 위험하므로 backup 후 사용.'
      - '**[OSS] etckeeper** — `/etc/nftables.conf` 변경 추적 도구. lab 추가/삭제 모두 git history. `etckeeper vcs log /etc/nftables.conf` 로 변경 이력.'
      - '**[OSS] cockpit-firewall** — Cockpit GUI 의 zone 삭제 버튼. CLI 익숙치 않은 운영자가 안전한 cleanup.'
      negative_signs:
      - delete 대신 flush(테이블 남음)
      - 다른 테이블 삭제(오타)
      - 확인 없이 running 룰 제거
  target_vm: secu
  script: sudo nft delete table inet lab
  risk_level: low
  bastion_prompt: secu VM의 inet lab nftables 테이블을 삭제해줘
- order: 10
  instruction: lab 테이블이 삭제되었는지 nft list tables 로 확인하시오.
  hint: nft list tables
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM에서 nftables 테이블 목록을 확인해줘

    📎 참고 (CLI): sudo nft list tables | grep -v '' lab$'' | tail'
  answer_detail: 'nftables 방화벽 기초: lab 테이블이 삭제되었는지 nft list tables 로 확인하시오.'
  verify:
    type: output_contains
    expect: table
    field: stdout
    semantic:
      intent: lab 테이블 삭제 검증 — sudo nft list tables 로 테이블 목록 재확인, lab 이 없어졌는지 검증. 명령
        실행 후 검증은 필수 관행(trust but verify). grep -v lab 또는 | grep -c lab 으로 기계적 확인.
        삭제 실패 시 syntax·dependency 오류 가능성
      success_criteria:
      - nft list tables 재실행
      - 출력에 lab 테이블 부재
      - table 키워드 확인
      acceptable_methods:
      - sudo nft list tables | grep -v lab
      - sudo nft list tables | grep -c lab (0 이어야)
      - sudo nft list table inet lab 2>&1 | grep No such(없음 확인)
      - diff before.txt after.txt 로 변화 확인
      - '**[OSS] firewalld** — `firewall-cmd --get-zones | tr " " "\n" | grep -c "^lab$"` 로 lab zone 부재 확인. 0 = 삭제 성공.'
      - '**[OSS] fwbuilder** — Reload from system 후 GUI 의 firewall list 에서 lab 부재 시각 확인.'
      - '**[OSS] etckeeper** — `etckeeper vcs diff HEAD~1 /etc/nftables.conf` 로 삭제 직전/직후 diff 확인.'
      - '**[OSS] cockpit-firewall** — Cockpit Networking → Firewall 페이지 새로고침 → lab zone 부재 GUI 확인.'
      - '**[OSS] auditd** — `sudo ausearch -k nft_changes --start today` 로 nftables 변경 audit log. 누가/언제 lab 삭제했는지 evidence.'
      negative_signs:
      - list ruleset 으로 대체(overkill)
      - 삭제 전 목록과 비교 없음
      - lab 남아있는데 성공 판정
  target_vm: secu
  script: sudo nft list tables | grep -v ' lab$' | tail
  risk_level: low
  bastion_prompt: secu VM에서 nftables 테이블 목록을 확인해줘
- order: 99
  instruction: "[복합 시나리오] \"내가 짠 방화벽 룰, 정말 일하나?\" — attacker 에서 trigger 검증\n\n## 상황\
    \ (w01 시나리오 연속)\n\n당신은 ABC 핀테크 SOC 신규 분석가. 둘째 주.\n\n어제까지 1~10번 step 으로 nftables\
    \ 의 *문법* 을 배웠다 — 테이블 만들고, 체인 걸고,\n포트 차단 룰 작성하고, 다시 삭제까지. 명령어는 익혔다.\n\n오늘 아침 선임이\
    \ 옆에 와서 한 마디:\n\n> \"룰 syntax 익힌 건 알겠어. 그런데 그 룰이 *진짜* 외부 트래픽을 차단하는지 확인했어?\n> \
    \ 내가 처음 nftables 했을 때 input chain 에 drop 룰 넣어놓고도 telnet 이 통하길래\n>  당황했었거든. priority\
    \ / hook / family 가 안 맞으면 룰은 silently 무용지물이야.\n>  attacker(10.20.30.201) 에서 직접\
    \ 시도해보고, 어느 룰이 어떤 trigger 를 잡는지 매핑해봐.\"\n\n학습 목표 #2 — *\"input 체인에서 특정 포트를 허용/차단할\
    \ 수 있다\"* — 의 *증명* 단계.\nsyntax 가 아니라 *효과* 를 본인 손으로 trigger 해서 확인하는 것.\n\n## 임무\
    \ (3 단계, 약 20분)\n\n### Phase A — 검증용 룰셋 다시 구성 (5분)\n\n1~10번 step 에서 lab 테이블을 *삭제*\
    \ 했으므로, 검증을 위해 동일 룰셋을 다시\n작성한다. 이번엔 이름을 `verify` 로 (실 운영 룰셋과 충돌 방지):\n\n  - `nft\
    \ add table inet verify`\n  - `nft add chain inet verify input { type filter hook\
    \ input priority 0 \\\\; policy accept \\\\; }`\n  - `nft add rule inet verify\
    \ input ct state established,related accept`\n  - `nft add rule inet verify input\
    \ iif lo accept`\n  - `nft add rule inet verify input tcp dport 22 accept`   \
    \      # SSH 허용\n  - `nft add rule inet verify input tcp dport 23 drop`      \
    \      # telnet 차단\n  - `nft list table inet verify`     # 작성된 룰셋 확인 (counter\
    \ 초기값 0)\n\n### Phase B — attacker 에서 3 trigger 시도 (10분)\n\nattacker(10.20.30.201)\
    \ 에서 secu(10.20.30.1) 로 3 가지 시도. 각 시도 직후 secu 의\nnft counter 를 확인해 *어느 룰이 hit\
    \ 되었는지* 매핑.\n\n1. **telnet 차단 검증** (drop 룰 효과)\n   - attacker: `nc -vz -w 3 10.20.30.1\
    \ 23` (telnet 포트)\n   - 예상: connection refused / timeout\n   - secu 확인: `nft list\
    \ table inet verify | grep -A1 'tcp dport 23'` (counter +1?)\n\n2. **SSH 허용 검증**\
    \ (accept 룰 효과)\n   - attacker: `ssh -o ConnectTimeout=3 -o StrictHostKeyChecking=no\
    \ ccc@10.20.30.1 'echo ok' 2>&1 | head`\n   - 예상: 인증 프롬프트 또는 정상 응답\n   - secu\
    \ 확인: `nft list table inet verify | grep -A1 'tcp dport 22'` (counter +1?)\n\n\
    3. **established 룰 동작 확인** (상태 기반 룰)\n   - SSH 접속 유지하면서: `nft list table inet\
    \ verify | grep -A1 'ct state'` (counter 큼)\n   - 의미: 한 번 ssh 가 accept 되면 그 후\
    \ 패킷은 *established* 룰에서 빠르게 처리\n\n### Phase C — 결과 회고 (5분)\n\nmatrix 형식이 아니라 *문장\
    \ 회고* 1~2 단락:\n\n1. **Phase B 의 3 trigger 각각 어느 룰이 hit 되었나** — counter 변동 명시\n\
    2. **예상과 다른 결과가 있었나** — 예: telnet 차단됐는데 counter 증가 안 함 (룰 순서·priority 문제)\n3.\
    \ **다음에 룰 작성할 때 주의할 점 1가지** (예: \"policy accept 인 chain 끝에 drop 룰 두면\n   그 위 accept\
    \ 룰이 우선됨\")\n\nPhase A 의 verify 테이블은 lab 종료 시 정리: `nft delete table inet verify`\n\
    \n## 학습 포인트\n\n이 step 의 핵심은 *명령어 sequence 의 정확성* 이 아니라 *룰의 효과를 본인이 trigger 해\n\
    봤다는 경험*. w03 부터는 더 복잡한 룰셋 (NAT, masquerade, set 기반 IP 차단) 을 다루는데,\n문법 만으로는 룰이\
    \ 일하는지 모른다는 것을 이번 주에 체득해 둬야 한다."
  hint: 'Phase A: lab 대신 verify 테이블에 동일 룰셋 다시 작성 (1~10번 step 의 syntax 재현)

    Phase B: attacker(10.20.30.201) 에서 nc telnet (차단 예상) / ssh (허용 예상) / established
    시도, secu 의 nft counter 변동 확인

    Phase C: 3 trigger 의 hit 룰 매핑 + 예상 외 결과 + 룰 작성 시 주의점

    포인트: syntax 만 익히면 안 됨, *효과* 를 직접 trigger 해서 확인.

    '
  category: integration_scenario
  points: 50
  verify:
    type: output_contains
    expect: counter
    field: stdout
    semantic:
      intent: 'nftables 룰의 *효과* 를 attacker→secu 실 trigger 로 검증. 학습 목표 #2 (input 체인

        포트 허용/차단) 의 행동 증명. syntax 단계에서 효과 검증 단계로 전환.

        '
      success_criteria:
      - verify 테이블 + input 체인 + 4 룰 (established / lo / 22 accept / 23 drop) 작성
      - attacker→secu 3 trigger (nc telnet 23 / ssh 22 / 상태 유지) 실행
      - 각 trigger 직후 nft counter 변동 확인 (룰 hit 매핑)
      - 1~2 단락 회고 (어느 룰이 어느 trigger 를 잡았나 + 예상 외 결과 + 다음 작성 주의점)
      acceptable_methods:
      - secu 에서 nft add table/chain/rule + nft list table inet verify
      - attacker 에서 nc -vz / ssh ConnectTimeout / curl
      - secu nft counter 확인 (nft list table inet verify | grep counter)
      - 회고 markdown 1~2 단락
      - '**[OSS] nmap (Phase B trigger)** — `nmap -p 22,23,80 -Pn 10.20.30.1` 로 3 포트 동시 스캔. SYN 패킷이 nft 의 각 룰에 trigger 되어 counter 변동. 침투테스터 표준.'
      - '**[OSS] hping3 (정밀 trigger)** — `sudo hping3 -S -p 23 -c 3 10.20.30.1` SYN-only 패킷 정확히 3 개 = counter 정확히 +3. drop 룰 효과 정량 측정.'
      - '**[OSS] masscan** — 대규모 trigger 시 `masscan 10.20.30.1 -p 1-100 --rate 100` 으로 100 포트 스캔 → 차단·허용 룰 양쪽 동시 검증.'
      - '**[OSS] tcpdump (secu 측 wire-level)** — `sudo tcpdump -i any -nn "host 10.20.30.201"` 로 attacker 트래픽 wire-level 캡처. drop 룰이 *진짜로* 패킷을 떨어뜨리는지 패킷 부재로 검증.'
      - '**[OSS] wireshark/tshark** — `sudo tshark -i any -c 30 "tcp port 23"` telnet trigger 시 SYN 만 보이고 SYN-ACK 없으면 drop 정상.'
      - '**[OSS] iftop / nload (실시간 효과 시각화)** — Phase B trigger 중 secu 의 `iftop -i any` 에서 attacker 트래픽 표시. 차단된 트래픽은 0 bps.'
      - '**[OSS] conntrack -E (이벤트 모니터)** — `sudo conntrack -E -e new,destroy` 로 conntrack 이벤트 실시간 watch. accept 트래픽만 NEW 이벤트 발생, drop 은 부재.'
      negative_signs:
      - 룰 작성만 + trigger 시도 누락
      - trigger 실행했지만 counter 확인 안 함
      - 회고 누락 또는 1줄 미만
      - lab 테이블 (1~10번에서 삭제됨) 그대로 재사용 시도
  target_vm: bastion
  answer: "\U0001F916 프롬프트:\n\n[복합 시나리오] 이번 주 배운 nftables 룰의 *효과* 를 attacker→secu\
    \ trigger 로 검증한다.\n\nPhase A — secu 에 verify 테이블 다시 구성 (1~10번 lab 테이블은 삭제됐으니):\n\
    \  - nft add table inet verify\n  - nft add chain inet verify input { type filter\
    \ hook input priority 0 \\; policy accept \\; }\n  - nft add rule inet verify\
    \ input ct state established,related accept\n  - nft add rule inet verify input\
    \ iif lo accept\n  - nft add rule inet verify input tcp dport 22 accept\n  - nft\
    \ add rule inet verify input tcp dport 23 drop\n  - nft list table inet verify\n\
    \nPhase B — attacker(10.20.30.201) 에서 3 trigger:\n  1) nc -vz -w 3 10.20.30.1\
    \ 23                  # telnet drop 검증\n  2) ssh -o ConnectTimeout=3 ccc@10.20.30.1\
    \ 'echo ok'  # ssh accept 검증\n  3) (ssh 유지 상태에서) nft list table inet verify |\
    \ grep -A1 'ct state'  # established hit\n\n각 trigger 직후 secu 의 nft list table\
    \ inet verify | grep counter 로 변동 확인.\n\nPhase C — 회고 markdown 1~2 단락: 어느 룰이 어느\
    \ trigger 를 잡았나 / 예상 외 결과 /\n다음 룰 작성 시 주의점.\n\n종료 후 정리: nft delete table inet\
    \ verify.\n"
  answer_detail: '이 step 은 학습 목표 #2 ("input 체인 포트 허용/차단") 의 행동 증명. 1~10번 step 이

    syntax 익히기였다면 99 번은 효과 검증. attacker→secu trigger 로 nft counter 변동을 직접

    봐서 "내가 짠 룰이 진짜 일한다"는 경험을 얻는다.

    '
  risk_level: low
  bastion_prompt: 'nftables 룰의 효과 검증. secu 에 verify 테이블 + 4 룰 (established/lo/22accept/23drop)

    재구성 후, attacker(10.20.30.201) 에서 nc 23 / ssh 22 / established 3 trigger 실행.

    각 trigger 직후 secu 의 nft counter 변동 확인 후 1~2 단락 회고. 종료 시 verify 테이블 삭제.'
