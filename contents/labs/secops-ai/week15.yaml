lab_id: secops-ai-week15
title: 기말고사 — 보안 인프라 구축 종합 실기 (AI 지원)
version: ai
course: secops-ai
week: 15
description: nftables + Suricata + Wazuh + OpenCTI 연계를 활용해 공격 탐지·차단·기록 end-to-end 시나리오를 구성한다.
difficulty: hard
duration_minutes: 180
objectives:
- nftables 'final' 테이블로 화이트리스트·블랙리스트 구조를 설계한다
- Suricata 커스텀 룰로 특정 공격을 탐지한다
- Wazuh alerts.json 에서 공격 이벤트를 확인한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: secu VM에 inet 'final' 테이블을 만들고 input 체인(policy drop)을 생성하시오.
  hint: nft add table; nft add chain
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    📎 참고 (CLI): sudo nft add table inet final && sudo nft add chain inet final input ''{ type filter hook
    input priority 0 \; policy drop \; }'''
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: secu VM에 inet ''final'' 테이블을 만들고 input 체인(policy drop)을 생성하시오.'
  verify:
    type: output_contains
    expect: final
    field: stdout
    semantic:
      intent: Final 기말고사 - 보안 인프라 default-deny 시작 — sudo nft add table inet final && sudo nft add chain
        inet final input { type filter hook input priority 0 ; policy drop ; }. 종합 방어 설계의 기반 chain. final
        테이블 이름으로 기말 과제 전용. policy drop 으로 Zero Trust. 이후 whitelist + blacklist + CTI 통합 구조로 확장. priority
        0 filter 표준
      success_criteria:
      - nft add table + add chain 2 명령
      - policy drop 명시
      - type filter hook input priority 0
      acceptable_methods:
      - sudo nft add table inet final && sudo nft add chain inet final input {...}
      - /etc/nftables.conf 에 선언 후 nft -f
      - ansible nftables role
      - Terraform 으로 IaC 관리
      negative_signs:
      - policy accept(Zero Trust 위반)
      - hook 누락
      - priority 비정상 값
  target_vm: secu
  script: sudo nft add table inet final && sudo nft add chain inet final input '{ type filter hook input
    priority 0 \; policy drop \; }'
  risk_level: medium
  bastion_prompt: secu VM에 'inet final' 테이블과 policy drop의 input 체인을 만들어줘
- order: 2
  instruction: final 테이블에 whitelist_ips set 을 만들어 10.20.30.0/24 를 허용하시오.
  hint: nft add set + element
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept
    룰을 추가해줘

    📎 참고 (CLI): sudo nft ''add set inet final whitelist_ips { type ipv4_addr \; flags interval \; }''
    && sudo nft add element inet final whitelist_ips ''{ 10.20.30.0/24 }'' && sudo nft add rule inet final
    input ip saddr @whitelist_ips accept'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블에 whitelist_ips set 을 만들어 10.20.30.0/24 를 허용하시오.'
  verify:
    type: output_contains
    expect: whitelist_ips
    field: stdout
    semantic:
      intent: whitelist_ips CIDR set + accept 룰 — sudo nft add set inet final whitelist_ips { type ipv4_addr
        ; flags interval ; } && sudo nft add element inet final whitelist_ips { 10.20.30.0/24 } && sudo
        nft add rule inet final input ip saddr @whitelist_ips accept. flags interval 필수 - CIDR prefix
        저장/lookup 가능. 10.20.30.0/24 내부망 허용. 3-step 체인(set 생성 + element 추가 + rule 추가) 으로 완성. CIDR 외 IP
        는 drop(policy drop)
      success_criteria:
      - whitelist_ips set + flags interval
      - CIDR 10.20.30.0/24 element 추가
      - '@whitelist_ips accept 룰'
      acceptable_methods:
      - 3-step 체인(set+element+rule)
      - timeout + interval 조합
      - ipset hash:net 대체(legacy)
      - ansible nftables set module
      negative_signs:
      - flags interval 누락(CIDR 오류)
      - rule 없이 set+element 만
      - IP 오타
  target_vm: secu
  script: sudo nft 'add set inet final whitelist_ips { type ipv4_addr \; flags interval \; }' && sudo
    nft add element inet final whitelist_ips '{ 10.20.30.0/24 }' && sudo nft add rule inet final input
    ip saddr @whitelist_ips accept
  risk_level: medium
  bastion_prompt: secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept
    룰을 추가해줘
- order: 3
  instruction: final 테이블에 blacklist_ips set 을 만들고 input 체인 맨 앞에 drop 룰을 insert 하시오.
  hint: nft insert rule … @blacklist_ips drop
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    📎 참고 (CLI): sudo nft ''add set inet final blacklist_ips { type ipv4_addr \; }'' && sudo nft insert
    rule inet final input ip saddr @blacklist_ips drop'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블에 blacklist_ips set 을 만들고 input 체인 맨 앞에 drop 룰을 insert
    하시오.'
  verify:
    type: output_contains
    expect: blacklist_ips
    field: stdout
    semantic:
      intent: 'blacklist_ips set + drop 룰 prepend — sudo nft add set inet final blacklist_ips { type ipv4_addr
        ; } && sudo nft insert rule inet final input ip saddr @blacklist_ips drop. 블랙리스트는 화이트리스트보다 먼저
        평가되어야 함 - insert 로 맨 앞에 prepend. final chain 룰 순서: (1) @blacklist_ips drop (2) @whitelist_ips
        accept (3) 기본 drop. timeout 없으면 영구 차단 - CTI TTL 고려 timeout 30d 권장'
      success_criteria:
      - blacklist_ips set 생성
      - insert rule 로 맨 앞 삽입
      - '@blacklist_ips drop expression'
      acceptable_methods:
      - nft add set + nft insert rule 체인
      - timeout 30d 추가
      - index 0 명시
      - ipset hash:ip 대체
      negative_signs:
      - add rule(append) 로 순서 오류
      - accept 대신 drop
      - timeout 없이 영구
  target_vm: secu
  script: sudo nft 'add set inet final blacklist_ips { type ipv4_addr \; }' && sudo nft insert rule inet
    final input ip saddr @blacklist_ips drop
  risk_level: medium
  bastion_prompt: secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘
- order: 4
  instruction: Suricata local.rules 에 '.php?cmd=' 탐지 룰(sid:1000301) 을 추가하고 reload 하시오.
  hint: content:"cmd="; http_uri;
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata
    reload 해줘

    📎 참고 (CLI): echo ''alert http any any -> any any (msg:"Final exam - cmd injection"; content:"cmd=";
    http_uri; sid:1000301; rev:1;)'' | sudo tee -a /etc/suricata/rules/local.rules && sudo systemctl reload
    suricata'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: Suricata local.rules 에 ''.php?cmd='' 탐지 룰(sid:1000301) 을 추가하고
    reload 하시오.'
  verify:
    type: output_contains
    expect: '1000301'
    field: stdout
    semantic:
      intent: 'Suricata cmd injection 탐지 룰 — echo alert http any any -> any any (msg: Final exam - cmd
        injection ; content: cmd= ; http_uri; sid:1000301; rev:1;) | sudo tee -a /etc/suricata/rules/local.rules
        && sudo systemctl reload suricata. http_uri sticky buffer 에서 cmd= 패턴 매칭 - Command Injection 공격
        탐지(MITRE T1059). reload 로 즉시 적용. 이후 공격자가 ?cmd= 포함 URI 전송 시 alert 생성'
      success_criteria:
      - 'alert http + content: cmd= + http_uri'
      - sid:1000301 포함
      - tee -a + reload suricata
      acceptable_methods:
      - echo alert http ... | sudo tee -a local.rules && systemctl reload suricata
      - 'pcre: /cmd=.+?$/i 로 정교화'
      - 'content: cmd= ; pcre로 payload 구체 매칭'
      - ModSecurity CRS 932xxx(RCE) 대체
      - Sigma rule → Suricata 변환
      negative_signs:
      - http_uri 없이 raw match(FP 많음)
      - reload 누락
      - sid 중복
  target_vm: secu
  script: echo 'alert http any any -> any any (msg:"Final exam - cmd injection"; content:"cmd="; http_uri;
    sid:1000301; rev:1;)' | sudo tee -a /etc/suricata/rules/local.rules && sudo systemctl reload suricata
  risk_level: medium
  bastion_prompt: secu VM local.rules에 http_uri에 'cmd='가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload
    해줘
- order: 5
  instruction: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내 Suricata 탐지를 유도하시오.
  hint: curl /index.php?cmd=ls
  category: verify
  points: 10
  answer: '🤖 프롬프트: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘

    📎 참고 (CLI): curl -sS -o /dev/null -w ''%{http_code}\n'' ''http://10.20.30.80/index.php?cmd=ls'''
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내 Suricata 탐지를
    유도하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: cmd injection 트래픽 트리거 — curl -sS -o /dev/null -w %{http_code}\n http://10.20.30.80/index.php?cmd=ls.
        s4 의 sid 1000301 룰 구동 테스트. attacker → web 방향 HTTP GET, URI 에 cmd=ls(common RCE pattern). 응답 코드
        무시 - 룰 검증만 목적. 실 공격자는 encoding(?cmd=%6c%73), 쿼리 조합(?a=1&cmd=ls), POST body 등 다양한 우회 시도. 룰이 http_uri
        로 static content 만 잡아 우회 가능
      success_criteria:
      - curl 로 ?cmd= URI 전송
      - <IP>/index.php?cmd=ls 포함
      - 응답 코드 출력
      acceptable_methods:
      - curl -sS -o /dev/null -w %{http_code} http://<IP>/index.php?cmd=ls
      - '다양한 payload: ?cmd=cat /etc/passwd, ?cmd=whoami'
      - Burp + Intruder sniper mode
      - commix 자동 RCE 도구
      - 'Python requests.get(params={cmd: ls})'
      negative_signs:
      - cmd= 파라미터 생략
      - 응답 확인 없음
      - 잘못된 호스트/포트
  target_vm: attacker
  script: curl -sS -o /dev/null -w '%{http_code}\n' 'http://10.20.30.80/index.php?cmd=ls'
  risk_level: medium
  bastion_prompt: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘
- order: 6
  instruction: secu VM eve.json 에서 sid:1000301 알람을 확인하시오.
  hint: grep 1000301 eve.json
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 suricata eve.json에서 sid 1000301 알람을 찾아줘

    📎 참고 (CLI): sudo grep ''"signature_id":1000301'' /var/log/suricata/eve.json 2>/dev/null | tail -3
    || sudo grep 1000301 /var/log/suricata/eve.json | tail'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: secu VM eve.json 에서 sid:1000301 알람을 확인하시오.'
  verify:
    type: output_contains
    expect: '1000301'
    field: stdout
    semantic:
      intent: Suricata sid:1000301 alert 검증 — sudo grep signature_id:1000301 /var/log/suricata/eve.json
        | tail -3 || sudo grep 1000301 eve.json. 룰 매칭 시 eve.json event_type=alert + alert.signature_id=1000301.
        fallback 으로 단순 1000301 grep. 0 hits = (1) 룰 미로드 - suricata -T 로 재검증 (2) Suricata가 패킷 못 봄 - af-packet
        interface 확인 (3) http parse 실패
      success_criteria:
      - grep signature_id 1000301 실행
      - alert 매칭 또는 fallback
      - tail 결과
      acceptable_methods:
      - sudo grep signature_id:1000301 eve.json | tail
      - sudo jq -c select(.alert.signature_id==1000301) eve.json
      - sudo grep Final exam fast.log
      - suricatasc -c iface-stat 에서 alert 수 증가
      - Elastic 검색 alert.signature_id:1000301
      negative_signs:
      - 0 hits 정상 판정
      - 잘못된 sid
      - fast.log 만 확인
  target_vm: secu
  script: sudo grep '"signature_id":1000301' /var/log/suricata/eve.json 2>/dev/null | tail -3 || sudo
    grep 1000301 /var/log/suricata/eve.json | tail
  risk_level: low
  bastion_prompt: secu VM의 suricata eve.json에서 sid 1000301 알람을 찾아줘
- order: 7
  instruction: siem VM alerts.json 에서 sid 1000301 혹은 cmd injection 관련 이벤트를 검색하시오.
  hint: grep cmd alerts.json
  category: verify
  points: 10
  answer: '🤖 프롬프트: siem VM wazuh alerts.json에서 cmd injection 또는 sid 1000301 관련 이벤트를 찾아줘

    📎 참고 (CLI): sudo grep -i ''cmd injection\|1000301'' /var/ossec/logs/alerts/alerts.json 2>/dev/null
    | tail'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: siem VM alerts.json 에서 sid 1000301 혹은 cmd injection 관련 이벤트를
    검색하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: Wazuh alerts.json 에서 cmd injection 이벤트 — sudo grep -i cmd injection\|1000301 /var/ossec/logs/alerts/alerts.json
        | tail. Suricata eve.json → Wazuh agent 수집 → Manager 가 Suricata decoder 적용 → rule 매칭(rule.groups
        suricata) → alerts.json 에 record. 통합 파이프라인 종단 검증. Suricata signature 가 data.signature 또는 alert.signature
        필드에 복사됨. 0 hits = agent/pipeline 단절
      success_criteria:
      - grep cmd injection 또는 1000301 실행
      - Wazuh alerts.json 에서 tail
      - end-to-end 검증
      acceptable_methods:
      - sudo grep -iE "cmd injection|1000301" alerts.json | tail
      - sudo jq select(.data.alert.signature_id=="1000301") alerts.json
      - Wazuh Dashboard Security Events filter
      - curl Wazuh API /alerts?data.alert.signature_id=1000301
      - Kibana saved search suricata cmd injection
      negative_signs:
      - Suricata eve.json 만 확인(agent 체크 없음)
      - grep 필터 틀림
      - 0 hits 무시
  target_vm: siem
  script: sudo grep -i 'cmd injection\|1000301' /var/ossec/logs/alerts/alerts.json 2>/dev/null | tail
  risk_level: low
  bastion_prompt: siem VM wazuh alerts.json에서 cmd injection 또는 sid 1000301 관련 이벤트를 찾아줘
- order: 8
  instruction: CTI 결과를 가정해 blacklist_ips set에 203.0.113.77 을 주입하시오.
  hint: nft add element inet final blacklist_ips { 203.0.113.77 }
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블 blacklist_ips set에 203.0.113.77을 추가해줘

    📎 참고 (CLI): sudo nft add element inet final blacklist_ips ''{ 203.0.113.77 }'' && sudo nft list set
    inet final blacklist_ips'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: CTI 결과를 가정해 blacklist_ips set에 203.0.113.77 을 주입하시오.'
  verify:
    type: output_contains
    expect: 203.0.113.77
    field: stdout
    semantic:
      intent: CTI IOC 를 blacklist_ips set 에 주입 — sudo nft add element inet final blacklist_ips { 203.0.113.77
        } && sudo nft list set inet final blacklist_ips. CTI 연계 종단 검증 - OpenCTI 가 제공한 IOC(악성 IP) 를 방화벽
        enforcement 로 변환. OpenCTI connector pycti API 조회 결과 → nft add element 자동화 스크립트. 203.0.113.77 은
        TEST-NET-3 document IP. list set 으로 주입 확인
      success_criteria:
      - nft add element 실행
      - 203.0.113.77 추가
      - list set 으로 검증
      acceptable_methods:
      - sudo nft add element inet final blacklist_ips { 203.0.113.77 }
      - Python + pycti + subprocess 자동화
      - MISP + PyMISP 로 IOC feed 동기화
      - timeout 옵션으로 자동 만료
      - Wazuh Active Response custom script
      negative_signs:
      - list set 검증 누락
      - 잘못된 set 이름(whitelist 혼동)
      - 수동 IP 만 추가(CTI 연계 의미 없음)
  target_vm: secu
  script: sudo nft add element inet final blacklist_ips '{ 203.0.113.77 }' && sudo nft list set inet final
    blacklist_ips
  risk_level: medium
  bastion_prompt: secu VM final 테이블 blacklist_ips set에 203.0.113.77을 추가해줘
- order: 9
  instruction: final 테이블 전체 룰셋을 확인해 구성 무결성을 점검하시오.
  hint: nft list table inet final
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 final 테이블 전체 룰셋을 출력해줘

    📎 참고 (CLI): sudo nft list table inet final'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블 전체 룰셋을 확인해 구성 무결성을 점검하시오.'
  verify:
    type: output_contains
    expect: final
    field: stdout
    semantic:
      intent: 'final 테이블 구성 무결성 점검 — sudo nft list table inet final. 기말고사 최종 구성 검증 - 전체 구조 확인: (1) input
        chain policy drop (2) blacklist_ips set + drop 룰 (3) whitelist_ips set + accept 룰. 룰 순서가 올바른지
        확인 중요 - blacklist 가 whitelist 보다 먼저 평가. handle 번호로 룰 순서 추적. 의도와 실제 구성 diff 확인'
      success_criteria:
      - nft list table inet final 실행
      - chain input 구조 출력
      - blacklist+whitelist 모두 포함
      acceptable_methods:
      - sudo nft list table inet final
      - sudo nft -a list table inet final(handle)
      - sudo nft -j list table inet final | jq
      - 구성 스냅샷 저장 + diff
      - git-managed nftables.conf 와 대조
      negative_signs:
      - 구조 확인 없이 실행만
      - 다른 table 확인
      - handle 번호 무시
  target_vm: secu
  script: sudo nft list table inet final
  risk_level: low
  bastion_prompt: secu VM의 final 테이블 전체 룰셋을 출력해줘
- order: 10
  instruction: '실습 종료: final 테이블 삭제 + sid:1000301 룰 삭제 + suricata reload 를 수행하시오.'
  hint: nft delete table; sed; reload
  category: cleanup
  points: 10
  answer: '🤖 프롬프트: secu VM에서 final 테이블 삭제, sid 1000301 룰 삭제, suricata reload를 모두 수행해줘

    📎 참고 (CLI): sudo nft delete table inet final && sudo sed -i ''/sid:1000301/d'' /etc/suricata/rules/local.rules
    && sudo systemctl reload suricata'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: 실습 종료: final 테이블 삭제 + sid:1000301 룰 삭제 + suricata reload 를 수행하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 기말고사 cleanup 3단계 — sudo nft delete table inet final && sudo sed -i /sid:1000301/d /etc/suricata/rules/local.rules
        && sudo systemctl reload suricata. (1) final 테이블 cascade delete (2) Suricata 룰 sed 삭제 (3) Suricata
        reload 로 rule 재로드. && 순차 성공 체인 - 하나 실패 시 다음 skip. 실환경 에서는 git revert 로 Infrastructure-as-Code
        roll-back 패턴 권장. 자동화 pipeline 에 포함시 rollback 전용 job 필요
      success_criteria:
      - 3단계 cleanup 모두 실행
      - nft delete + sed + reload
      - '&& 순차'
      acceptable_methods:
      - sudo nft delete table && sudo sed && sudo systemctl reload suricata
      - git revert 로 IaC 롤백
      - 'ansible playbook tags: cleanup'
      - 3 명령 독립 실행(에러 유연 처리)
      - snapshot 복구
      negative_signs:
      - 1-2 단계 누락
      - reload 없이 룰 그대로
      - 잘못된 sid 삭제
  target_vm: secu
  script: sudo nft delete table inet final && sudo sed -i '/sid:1000301/d' /etc/suricata/rules/local.rules
    && sudo systemctl reload suricata
  risk_level: high
  bastion_prompt: secu VM에서 final 테이블 삭제, sid 1000301 룰 삭제, suricata reload를 모두 수행해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  hint: 복합 작업은 bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: secu
  answer: '🤖 프롬프트: secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘

    3) secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    4) secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘

    5) attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘'
  answer_detail: ''
  risk_level: low
  bastion_prompt: 'secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘

    3) secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    4) secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘

    5) attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘'
