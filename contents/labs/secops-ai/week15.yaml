lab_id: secops-ai-week15
title: 기말고사 — 보안 인프라 구축 종합 실기 (AI 지원)
version: ai
course: secops-ai
week: 15
description: nftables + Suricata + Wazuh + OpenCTI 연계를 활용해 공격 탐지·차단·기록 end-to-end 시나리오를 구성한다.
difficulty: hard
duration_minutes: 180
objectives:
- nftables 'final' 테이블로 화이트리스트·블랙리스트 구조를 설계한다
- Suricata 커스텀 룰로 특정 공격을 탐지한다
- Wazuh alerts.json 에서 공격 이벤트를 확인한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: secu VM에 inet 'final' 테이블을 만들고 input 체인(policy drop)을 생성하시오.
  hint: nft add table; nft add chain
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    📎 참고 (CLI): sudo nft add table inet final && sudo nft add chain inet final input ''{ type filter hook input priority 0
    \; policy drop \; }'''
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: secu VM에 inet ''final'' 테이블을 만들고 input 체인(policy drop)을 생성하시오.'
  verify:
    type: output_contains
    expect: final
    field: stdout
    semantic:
      intent: 'SecOps w15 s1 — secu VM에 inet ''final'' 테이블을 만들고 input 체인(policy drop)을 생성하시오. (cmd: sudo nft add table inet
        final && sudo nft add chain inet final input ''{ type fil)'
      success_criteria:
      - '명령 실행: sudo nft add table inet final && sudo nft add chain inet fin'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft add table inet final && sudo nft add chain inet final input '{ type filter hook input priority 0 \; policy
    drop \; }'
  risk_level: medium
  bastion_prompt: secu VM에 'inet final' 테이블과 policy drop의 input 체인을 만들어줘
- order: 2
  instruction: final 테이블에 whitelist_ips set 을 만들어 10.20.30.0/24 를 허용하시오.
  hint: nft add set + element
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘

    📎 참고 (CLI): sudo nft ''add set inet final whitelist_ips { type ipv4_addr \; flags interval \; }'' && sudo nft add element
    inet final whitelist_ips ''{ 10.20.30.0/24 }'' && sudo nft add rule inet final input ip saddr @whitelist_ips accept'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블에 whitelist_ips set 을 만들어 10.20.30.0/24 를 허용하시오.'
  verify:
    type: output_contains
    expect: whitelist_ips
    field: stdout
    semantic:
      intent: 'SecOps w15 s2 — final 테이블에 whitelist_ips set 을 만들어 10.20.30.0/24 를 허용하시오. (cmd: sudo nft ''add set inet final
        whitelist_ips { type ipv4_addr \; flags interval \;)'
      success_criteria:
      - '명령 실행: sudo nft ''add set inet final whitelist_ips { type ipv4_addr '
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft 'add set inet final whitelist_ips { type ipv4_addr \; flags interval \; }' && sudo nft add element inet
    final whitelist_ips '{ 10.20.30.0/24 }' && sudo nft add rule inet final input ip saddr @whitelist_ips accept
  risk_level: medium
  bastion_prompt: secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘
- order: 3
  instruction: final 테이블에 blacklist_ips set 을 만들고 input 체인 맨 앞에 drop 룰을 insert 하시오.
  hint: nft insert rule … @blacklist_ips drop
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    📎 참고 (CLI): sudo nft ''add set inet final blacklist_ips { type ipv4_addr \; }'' && sudo nft insert rule inet final input
    ip saddr @blacklist_ips drop'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블에 blacklist_ips set 을 만들고 input 체인 맨 앞에 drop 룰을 insert 하시오.'
  verify:
    type: output_contains
    expect: blacklist_ips
    field: stdout
    semantic:
      intent: 'SecOps w15 s3 — final 테이블에 blacklist_ips set 을 만들고 input 체인 맨 앞에 drop 룰을 insert 하시오. (cmd: sudo nft ''add set
        inet final blacklist_ips { type ipv4_addr \; }'' && sudo nft in)'
      success_criteria:
      - '명령 실행: sudo nft ''add set inet final blacklist_ips { type ipv4_addr '
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft 'add set inet final blacklist_ips { type ipv4_addr \; }' && sudo nft insert rule inet final input ip saddr
    @blacklist_ips drop
  risk_level: medium
  bastion_prompt: secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘
- order: 4
  instruction: Suricata local.rules 에 '.php?cmd=' 탐지 룰(sid:1000301) 을 추가하고 reload 하시오.
  hint: content:"cmd="; http_uri;
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘

    📎 참고 (CLI): echo ''alert http any any -> any any (msg:"Final exam - cmd injection"; content:"cmd="; http_uri; sid:1000301;
    rev:1;)'' | sudo tee -a /etc/suricata/rules/local.rules && sudo systemctl reload suricata'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: Suricata local.rules 에 ''.php?cmd='' 탐지 룰(sid:1000301) 을 추가하고 reload 하시오.'
  verify:
    type: output_contains
    expect: '1000301'
    field: stdout
    semantic:
      intent: 'SecOps w15 s4 — Suricata local.rules 에 ''.php?cmd='' 탐지 룰(sid:1000301) 을 추가하고 reload 하시오. (cmd: echo ''alert
        http any any -> any any (msg:"Final exam - cmd injection"; content:")'
      success_criteria:
      - '명령 실행: echo ''alert http any any -> any any (msg:"Final exam - cmd i'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: echo 'alert http any any -> any any (msg:"Final exam - cmd injection"; content:"cmd="; http_uri; sid:1000301; rev:1;)'
    | sudo tee -a /etc/suricata/rules/local.rules && sudo systemctl reload suricata
  risk_level: medium
  bastion_prompt: secu VM local.rules에 http_uri에 'cmd='가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘
- order: 5
  instruction: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내 Suricata 탐지를 유도하시오.
  hint: curl /index.php?cmd=ls
  category: verify
  points: 10
  answer: '🤖 프롬프트: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘

    📎 참고 (CLI): curl -sS -o /dev/null -w ''%{http_code}\n'' ''http://10.20.30.80/index.php?cmd=ls'''
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내 Suricata 탐지를 유도하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w15 s5 — attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내 Suricata 탐지를 유도하시오. (cmd: curl -sS -o /dev/null
        -w ''%{http_code}\n'' ''http://10.20.30.80/index.php?cmd=ls'')'
      success_criteria:
      - '명령 실행: curl -sS -o /dev/null -w ''%{http_code}\n'' ''http://10.20.30.8'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: attacker
  script: curl -sS -o /dev/null -w '%{http_code}\n' 'http://10.20.30.80/index.php?cmd=ls'
  risk_level: medium
  bastion_prompt: attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘
- order: 6
  instruction: secu VM eve.json 에서 sid:1000301 알람을 확인하시오.
  hint: grep 1000301 eve.json
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 suricata eve.json에서 sid 1000301 알람을 찾아줘

    📎 참고 (CLI): sudo grep ''"signature_id":1000301'' /var/log/suricata/eve.json 2>/dev/null | tail -3 || sudo grep 1000301
    /var/log/suricata/eve.json | tail'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: secu VM eve.json 에서 sid:1000301 알람을 확인하시오.'
  verify:
    type: output_contains
    expect: '1000301'
    field: stdout
    semantic:
      intent: 'SecOps w15 s6 — secu VM eve.json 에서 sid:1000301 알람을 확인하시오. (cmd: sudo grep ''"signature_id":1000301'' /var/log/suricata/eve.json
        2>/dev/null | tail)'
      success_criteria:
      - '명령 실행: sudo grep ''"signature_id":1000301'' /var/log/suricata/eve.jso'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo grep '"signature_id":1000301' /var/log/suricata/eve.json 2>/dev/null | tail -3 || sudo grep 1000301 /var/log/suricata/eve.json
    | tail
  risk_level: low
  bastion_prompt: secu VM의 suricata eve.json에서 sid 1000301 알람을 찾아줘
- order: 7
  instruction: siem VM alerts.json 에서 sid 1000301 혹은 cmd injection 관련 이벤트를 검색하시오.
  hint: grep cmd alerts.json
  category: verify
  points: 10
  answer: '🤖 프롬프트: siem VM wazuh alerts.json에서 cmd injection 또는 sid 1000301 관련 이벤트를 찾아줘

    📎 참고 (CLI): sudo grep -i ''cmd injection\|1000301'' /var/ossec/logs/alerts/alerts.json 2>/dev/null | tail'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: siem VM alerts.json 에서 sid 1000301 혹은 cmd injection 관련 이벤트를 검색하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w15 s7 — siem VM alerts.json 에서 sid 1000301 혹은 cmd injection 관련 이벤트를 검색하시오. (cmd: sudo grep -i ''cmd
        injection\|1000301'' /var/ossec/logs/alerts/alerts.json 2>/dev/)'
      success_criteria:
      - '명령 실행: sudo grep -i ''cmd injection\|1000301'' /var/ossec/logs/alerts'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo grep -i 'cmd injection\|1000301' /var/ossec/logs/alerts/alerts.json 2>/dev/null | tail
  risk_level: low
  bastion_prompt: siem VM wazuh alerts.json에서 cmd injection 또는 sid 1000301 관련 이벤트를 찾아줘
- order: 8
  instruction: CTI 결과를 가정해 blacklist_ips set에 203.0.113.77 을 주입하시오.
  hint: nft add element inet final blacklist_ips { 203.0.113.77 }
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM final 테이블 blacklist_ips set에 203.0.113.77을 추가해줘

    📎 참고 (CLI): sudo nft add element inet final blacklist_ips ''{ 203.0.113.77 }'' && sudo nft list set inet final blacklist_ips'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: CTI 결과를 가정해 blacklist_ips set에 203.0.113.77 을 주입하시오.'
  verify:
    type: output_contains
    expect: 203.0.113.77
    field: stdout
    semantic:
      intent: 'SecOps w15 s8 — CTI 결과를 가정해 blacklist_ips set에 203.0.113.77 을 주입하시오. (cmd: sudo nft add element inet final
        blacklist_ips ''{ 203.0.113.77 }'' && sudo nft lis)'
      success_criteria:
      - '명령 실행: sudo nft add element inet final blacklist_ips ''{ 203.0.113.7'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft add element inet final blacklist_ips '{ 203.0.113.77 }' && sudo nft list set inet final blacklist_ips
  risk_level: medium
  bastion_prompt: secu VM final 테이블 blacklist_ips set에 203.0.113.77을 추가해줘
- order: 9
  instruction: final 테이블 전체 룰셋을 확인해 구성 무결성을 점검하시오.
  hint: nft list table inet final
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM의 final 테이블 전체 룰셋을 출력해줘

    📎 참고 (CLI): sudo nft list table inet final'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: final 테이블 전체 룰셋을 확인해 구성 무결성을 점검하시오.'
  verify:
    type: output_contains
    expect: final
    field: stdout
    semantic:
      intent: 'SecOps w15 s9 — final 테이블 전체 룰셋을 확인해 구성 무결성을 점검하시오. (cmd: sudo nft list table inet final)'
      success_criteria:
      - '명령 실행: sudo nft list table inet final'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft list table inet final
  risk_level: low
  bastion_prompt: secu VM의 final 테이블 전체 룰셋을 출력해줘
- order: 10
  instruction: '실습 종료: final 테이블 삭제 + sid:1000301 룰 삭제 + suricata reload 를 수행하시오.'
  hint: nft delete table; sed; reload
  category: cleanup
  points: 10
  answer: '🤖 프롬프트: secu VM에서 final 테이블 삭제, sid 1000301 룰 삭제, suricata reload를 모두 수행해줘

    📎 참고 (CLI): sudo nft delete table inet final && sudo sed -i ''/sid:1000301/d'' /etc/suricata/rules/local.rules && sudo
    systemctl reload suricata'
  answer_detail: '기말고사 — 보안 인프라 구축 종합 실기: 실습 종료: final 테이블 삭제 + sid:1000301 룰 삭제 + suricata reload 를 수행하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w15 s10 — 실습 종료: final 테이블 삭제 + sid:1000301 룰 삭제 + suricata reload 를 수행하시오. (cmd: sudo nft delete table
        inet final && sudo sed -i ''/sid:1000301/d'' /etc/suricata/r)'
      success_criteria:
      - '명령 실행: sudo nft delete table inet final && sudo sed -i ''/sid:100030'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft delete table inet final && sudo sed -i '/sid:1000301/d' /etc/suricata/rules/local.rules && sudo systemctl
    reload suricata
  risk_level: high
  bastion_prompt: secu VM에서 final 테이블 삭제, sid 1000301 룰 삭제, suricata reload를 모두 수행해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  hint: 복합 작업은 bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: secu
  answer: '🤖 프롬프트: secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘

    3) secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    4) secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘

    5) attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘'
  answer_detail: ''
  risk_level: low
  bastion_prompt: 'secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) secu VM에 ''inet final'' 테이블과 policy drop의 input 체인을 만들어줘

    2) secu VM final 테이블에 whitelist_ips set(10.20.30.0/24)을 만들고 input 체인에 @whitelist_ips accept 룰을 추가해줘

    3) secu VM final 테이블에 blacklist_ips set을 만들고 input 체인 맨 앞에 drop 룰을 삽입해줘

    4) secu VM local.rules에 http_uri에 ''cmd=''가 포함된 요청을 탐지하는 룰(sid 1000301)을 추가하고 suricata reload 해줘

    5) attacker VM에서 web VM으로 /index.php?cmd=ls 요청을 보내줘'
