lab_id: secops-ai-week09
title: Wazuh SIEM 설치·구성 (AI 지원)
version: ai
course: secops-ai
week: 9
description: Wazuh Manager·Indexer·Dashboard 구성 상태를 확인하고 siem의 agent 목록을 조회한다.
difficulty: medium
duration_minutes: 120
objectives:
- Wazuh Manager/Indexer/Dashboard/agent 서비스 상태를 확인한다
- /var/ossec/etc/ossec.conf 의 주요 설정을 이해한다
- agent_control / wazuh-wui 로 에이전트 목록을 조회한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: siem VM에서 Wazuh Manager 서비스 상태를 확인하시오.
  hint: systemctl is-active wazuh-manager
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 wazuh-manager 서비스 상태를 확인해줘

    📎 참고 (CLI): systemctl is-active wazuh-manager'
  answer_detail: 'Wazuh SIEM 설치·구성: siem VM에서 Wazuh Manager 서비스 상태를 확인하시오.'
  verify:
    type: output_contains
    expect: active
    field: stdout
    semantic:
      intent: 'SecOps w9 s1 — siem VM에서 Wazuh Manager 서비스 상태를 확인하시오. (cmd: systemctl is-active wazuh-manager)'
      success_criteria:
      - '명령 실행: systemctl is-active wazuh-manager'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: systemctl is-active wazuh-manager
  risk_level: low
  bastion_prompt: siem VM의 wazuh-manager 서비스 상태를 확인해줘
- order: 2
  instruction: Wazuh Indexer 서비스 상태를 확인하시오.
  hint: systemctl is-active wazuh-indexer
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 wazuh-indexer 서비스 상태를 확인해줘

    📎 참고 (CLI): systemctl is-active wazuh-indexer || echo inactive'
  answer_detail: 'Wazuh SIEM 설치·구성: Wazuh Indexer 서비스 상태를 확인하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w9 s2 — Wazuh Indexer 서비스 상태를 확인하시오. (cmd: systemctl is-active wazuh-indexer || echo inactive)'
      success_criteria:
      - '명령 실행: systemctl is-active wazuh-indexer || echo inactive'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: systemctl is-active wazuh-indexer || echo inactive
  risk_level: low
  bastion_prompt: siem VM의 wazuh-indexer 서비스 상태를 확인해줘
- order: 3
  instruction: Wazuh Dashboard 서비스 상태를 확인하시오.
  hint: systemctl is-active wazuh-dashboard
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 wazuh-dashboard 서비스 상태를 확인해줘

    📎 참고 (CLI): systemctl is-active wazuh-dashboard || echo inactive'
  answer_detail: 'Wazuh SIEM 설치·구성: Wazuh Dashboard 서비스 상태를 확인하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w9 s3 — Wazuh Dashboard 서비스 상태를 확인하시오. (cmd: systemctl is-active wazuh-dashboard || echo inactive)'
      success_criteria:
      - '명령 실행: systemctl is-active wazuh-dashboard || echo inactive'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: systemctl is-active wazuh-dashboard || echo inactive
  risk_level: low
  bastion_prompt: siem VM의 wazuh-dashboard 서비스 상태를 확인해줘
- order: 4
  instruction: wazuh-control info 로 Wazuh 버전·설치 정보를 확인하시오.
  hint: /var/ossec/bin/wazuh-control info
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM에서 wazuh-control info 로 설치 정보를 확인해줘

    📎 참고 (CLI): sudo /var/ossec/bin/wazuh-control info 2>&1 | head'
  answer_detail: 'Wazuh SIEM 설치·구성: wazuh-control info 로 Wazuh 버전·설치 정보를 확인하시오.'
  verify:
    type: output_contains
    expect: WAZUH
    field: stdout
    semantic:
      intent: 'SecOps w9 s4 — wazuh-control info 로 Wazuh 버전·설치 정보를 확인하시오. (cmd: sudo /var/ossec/bin/wazuh-control info 2>&1
        | head)'
      success_criteria:
      - '명령 실행: sudo /var/ossec/bin/wazuh-control info 2>&1 | head'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo /var/ossec/bin/wazuh-control info 2>&1 | head
  risk_level: low
  bastion_prompt: siem VM에서 wazuh-control info 로 설치 정보를 확인해줘
- order: 5
  instruction: agent_control -l 로 등록된 에이전트 목록을 조회하시오.
  hint: /var/ossec/bin/agent_control -l
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM에서 agent_control -l 로 에이전트 목록을 보여줘

    📎 참고 (CLI): sudo /var/ossec/bin/agent_control -l 2>&1 | head'
  answer_detail: 'Wazuh SIEM 설치·구성: agent_control -l 로 등록된 에이전트 목록을 조회하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w9 s5 — agent_control -l 로 등록된 에이전트 목록을 조회하시오. (cmd: sudo /var/ossec/bin/agent_control -l 2>&1 | head)'
      success_criteria:
      - '명령 실행: sudo /var/ossec/bin/agent_control -l 2>&1 | head'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo /var/ossec/bin/agent_control -l 2>&1 | head
  risk_level: low
  bastion_prompt: siem VM에서 agent_control -l 로 에이전트 목록을 보여줘
- order: 6
  instruction: /var/ossec/etc/ossec.conf 에서 <global> 섹션을 확인하시오.
  hint: awk '/<global>/,/<\/global>/' …
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 ossec.conf <global> 섹션을 보여줘

    📎 참고 (CLI): sudo awk ''/<global>/,/<\/global>/'' /var/ossec/etc/ossec.conf | head -20'
  answer_detail: 'Wazuh SIEM 설치·구성: /var/ossec/etc/ossec.conf 에서 <global> 섹션을 확인하시오.'
  verify:
    type: output_contains
    expect: global
    field: stdout
    semantic:
      intent: 'SecOps w9 s6 — /var/ossec/etc/ossec.conf 에서 <global> 섹션을 확인하시오. (cmd: sudo awk ''/<global>/,/<\/global>/''
        /var/ossec/etc/ossec.conf | head -20)'
      success_criteria:
      - '명령 실행: sudo awk ''/<global>/,/<\/global>/'' /var/ossec/etc/ossec.conf'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo awk '/<global>/,/<\/global>/' /var/ossec/etc/ossec.conf | head -20
  risk_level: low
  bastion_prompt: siem VM의 ossec.conf <global> 섹션을 보여줘
- order: 7
  instruction: ossec.conf 에서 <alerts>/<log_alert_level> 값을 확인하시오.
  hint: grep log_alert_level …
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 ossec.conf에서 log_alert_level 값을 확인해줘

    📎 참고 (CLI): sudo grep -n log_alert_level /var/ossec/etc/ossec.conf'
  answer_detail: 'Wazuh SIEM 설치·구성: ossec.conf 에서 <alerts>/<log_alert_level> 값을 확인하시오.'
  verify:
    type: output_contains
    expect: log_alert_level
    field: stdout
    semantic:
      intent: 'SecOps w9 s7 — ossec.conf 에서 <alerts>/<log_alert_level> 값을 확인하시오. (cmd: sudo grep -n log_alert_level /var/ossec/etc/ossec.conf)'
      success_criteria:
      - '명령 실행: sudo grep -n log_alert_level /var/ossec/etc/ossec.conf'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo grep -n log_alert_level /var/ossec/etc/ossec.conf
  risk_level: low
  bastion_prompt: siem VM의 ossec.conf에서 log_alert_level 값을 확인해줘
- order: 8
  instruction: Wazuh Manager의 최근 alerts.json 2줄을 확인하시오.
  hint: tail -2 /var/ossec/logs/alerts/alerts.json
  category: verify
  points: 10
  answer: '🤖 프롬프트: siem VM의 wazuh alerts.json 최근 2줄을 보여줘

    📎 참고 (CLI): sudo tail -2 /var/ossec/logs/alerts/alerts.json 2>/dev/null'
  answer_detail: 'Wazuh SIEM 설치·구성: Wazuh Manager의 최근 alerts.json 2줄을 확인하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w9 s8 — Wazuh Manager의 최근 alerts.json 2줄을 확인하시오. (cmd: sudo tail -2 /var/ossec/logs/alerts/alerts.json
        2>/dev/null)'
      success_criteria:
      - '명령 실행: sudo tail -2 /var/ossec/logs/alerts/alerts.json 2>/dev/null'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo tail -2 /var/ossec/logs/alerts/alerts.json 2>/dev/null
  risk_level: low
  bastion_prompt: siem VM의 wazuh alerts.json 최근 2줄을 보여줘
- order: 9
  instruction: Wazuh Indexer API(9200) 가 응답하는지 확인하시오.
  hint: curl -k https://localhost:9200
  category: verify
  points: 10
  answer: '🤖 프롬프트: siem VM의 Wazuh Indexer(9200) 리스닝/응답 상태를 확인해줘

    📎 참고 (CLI): curl -sk -o /dev/null -w ''%{http_code}\n'' https://localhost:9200 -u admin:admin 2>/dev/null || ss -tlnp
    | grep 9200'
  answer_detail: 'Wazuh SIEM 설치·구성: Wazuh Indexer API(9200) 가 응답하는지 확인하시오.'
  verify:
    type: output_contains
    expect: '9200'
    field: stdout
    semantic:
      intent: 'SecOps w9 s9 — Wazuh Indexer API(9200) 가 응답하는지 확인하시오. (cmd: curl -sk -o /dev/null -w ''%{http_code}\n'' https://localhost:9200
        -u admin:admin )'
      success_criteria:
      - '명령 실행: curl -sk -o /dev/null -w ''%{http_code}\n'' https://localhost:'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: curl -sk -o /dev/null -w '%{http_code}\n' https://localhost:9200 -u admin:admin 2>/dev/null || ss -tlnp | grep 9200
  risk_level: low
  bastion_prompt: siem VM의 Wazuh Indexer(9200) 리스닝/응답 상태를 확인해줘
- order: 10
  instruction: 최근 1시간 alerts.json 내 alert 수를 집계하시오.
  hint: wc -l alerts.json
  category: analysis
  points: 10
  answer: '🤖 프롬프트: siem VM의 wazuh alerts.json 라인 수를 확인해줘

    📎 참고 (CLI): sudo wc -l /var/ossec/logs/alerts/alerts.json 2>/dev/null'
  answer_detail: 'Wazuh SIEM 설치·구성: 최근 1시간 alerts.json 내 alert 수를 집계하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w9 s10 — 최근 1시간 alerts.json 내 alert 수를 집계하시오. (cmd: sudo wc -l /var/ossec/logs/alerts/alerts.json 2>/dev/null)'
      success_criteria:
      - '명령 실행: sudo wc -l /var/ossec/logs/alerts/alerts.json 2>/dev/null'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: sudo wc -l /var/ossec/logs/alerts/alerts.json 2>/dev/null
  risk_level: low
  bastion_prompt: siem VM의 wazuh alerts.json 라인 수를 확인해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  hint: 복합 작업은 bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: siem
  answer: '🤖 프롬프트: secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) siem VM의 wazuh-manager 서비스 상태를 확인해줘

    2) siem VM의 wazuh-indexer 서비스 상태를 확인해줘

    3) siem VM의 wazuh-dashboard 서비스 상태를 확인해줘

    4) siem VM에서 wazuh-control info 로 설치 정보를 확인해줘

    5) siem VM에서 agent_control -l 로 에이전트 목록을 보여줘'
  answer_detail: ''
  risk_level: low
  bastion_prompt: 'secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) siem VM의 wazuh-manager 서비스 상태를 확인해줘

    2) siem VM의 wazuh-indexer 서비스 상태를 확인해줘

    3) siem VM의 wazuh-dashboard 서비스 상태를 확인해줘

    4) siem VM에서 wazuh-control info 로 설치 정보를 확인해줘

    5) siem VM에서 agent_control -l 로 에이전트 목록을 보여줘'
