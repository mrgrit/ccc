lab_id: secops-ai-week13
title: OpenCTI 위협 인텔리전스 활용 (AI 지원)
version: ai
course: secops-ai
week: 13
description: OpenCTI에 CTI feed를 연동하고 IOC(악성 IP)를 nftables blocklist 와 연계한다.
difficulty: medium
duration_minutes: 120
objectives:
- OpenCTI GraphQL API 구조를 이해한다
- IOC를 조회해 nftables set에 주입하는 파이프라인을 설계한다
- CTI 기반 차단 룰이 적용되는 것을 검증한다
pass_threshold: 0.6
steps:
- order: 1
  instruction: OpenCTI GraphQL 엔드포인트(/graphql) 가 응답하는지 확인하시오.
  hint: curl -X POST /graphql
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM의 OpenCTI /graphql 엔드포인트에 POST 테스트 요청을 보내 응답 코드를 보여줘

    📎 참고 (CLI): curl -sS -o /dev/null -w ''%{http_code}\n'' -X POST http://localhost:8080/graphql -H ''Content-Type: application/json''
    -d ''{"query":"{ about { version } }"}'''
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: OpenCTI GraphQL 엔드포인트(/graphql) 가 응답하는지 확인하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w13 s1 — OpenCTI GraphQL 엔드포인트(/graphql) 가 응답하는지 확인하시오. (cmd: curl -sS -o /dev/null -w ''%{http_code}\n''
        -X POST http://localhost:8080/graphql )'
      success_criteria:
      - '명령 실행: curl -sS -o /dev/null -w ''%{http_code}\n'' -X POST http://loc'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: 'curl -sS -o /dev/null -w ''%{http_code}\n'' -X POST http://localhost:8080/graphql -H ''Content-Type: application/json''
    -d ''{"query":"{ about { version } }"}'''
  risk_level: low
  bastion_prompt: siem VM의 OpenCTI /graphql 엔드포인트에 POST 테스트 요청을 보내 응답 코드를 보여줘
- order: 2
  instruction: secu VM에 cti 전용 blocklist set 'cti_ips'(filter 테이블) 를 만드시오.
  hint: nft add set inet filter cti_ips
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM에 filter.cti_ips ipv4 set을 생성해줘

    📎 참고 (CLI): sudo nft add table inet filter 2>/dev/null ; sudo nft ''add set inet filter cti_ips { type ipv4_addr \; }''
    2>/dev/null && sudo nft list set inet filter cti_ips'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: secu VM에 cti 전용 blocklist set ''cti_ips''(filter 테이블) 를 만드시오.'
  verify:
    type: output_contains
    expect: cti_ips
    field: stdout
    semantic:
      intent: 'SecOps w13 s2 — secu VM에 cti 전용 blocklist set ''cti_ips''(filter 테이블) 를 만드시오. (cmd: sudo nft add table inet
        filter 2>/dev/null ; sudo nft ''add set inet filter cti_i)'
      success_criteria:
      - '명령 실행: sudo nft add table inet filter 2>/dev/null ; sudo nft ''add s'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft add table inet filter 2>/dev/null ; sudo nft 'add set inet filter cti_ips { type ipv4_addr \; }' 2>/dev/null
    && sudo nft list set inet filter cti_ips
  risk_level: medium
  bastion_prompt: secu VM에 filter.cti_ips ipv4 set을 생성해줘
- order: 3
  instruction: filter.input 체인 초기에 ip saddr @cti_ips drop 룰을 추가하시오.
  hint: nft insert rule …
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM filter.input 체인 맨 앞에 @cti_ips set 기반 drop 룰을 추가해줘

    📎 참고 (CLI): sudo nft add chain inet filter input ''{ type filter hook input priority 0 \; }'' 2>/dev/null ; sudo nft insert
    rule inet filter input ip saddr @cti_ips drop'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: filter.input 체인 초기에 ip saddr @cti_ips drop 룰을 추가하시오.'
  verify:
    type: output_contains
    expect: cti_ips
    field: stdout
    semantic:
      intent: 'SecOps w13 s3 — filter.input 체인 초기에 ip saddr @cti_ips drop 룰을 추가하시오. (cmd: sudo nft add chain inet filter input
        ''{ type filter hook input priority 0 \; }'' )'
      success_criteria:
      - '명령 실행: sudo nft add chain inet filter input ''{ type filter hook inp'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft add chain inet filter input '{ type filter hook input priority 0 \; }' 2>/dev/null ; sudo nft insert rule
    inet filter input ip saddr @cti_ips drop
  risk_level: medium
  bastion_prompt: secu VM filter.input 체인 맨 앞에 @cti_ips set 기반 drop 룰을 추가해줘
- order: 4
  instruction: 작성한 cti_ips set의 현재 원소를 확인하시오.
  hint: nft list set inet filter cti_ips
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM filter.cti_ips set의 원소 목록을 확인해줘

    📎 참고 (CLI): sudo nft list set inet filter cti_ips'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: 작성한 cti_ips set의 현재 원소를 확인하시오.'
  verify:
    type: output_contains
    expect: cti_ips
    field: stdout
    semantic:
      intent: 'SecOps w13 s4 — 작성한 cti_ips set의 현재 원소를 확인하시오. (cmd: sudo nft list set inet filter cti_ips)'
      success_criteria:
      - '명령 실행: sudo nft list set inet filter cti_ips'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft list set inet filter cti_ips
  risk_level: low
  bastion_prompt: secu VM filter.cti_ips set의 원소 목록을 확인해줘
- order: 5
  instruction: 샘플 악성 IP 203.0.113.66 을 cti_ips 에 주입하시오(OpenCTI 조회 결과를 가정).
  hint: nft add element inet filter cti_ips { 203.0.113.66 }
  category: configure
  points: 10
  answer: '🤖 프롬프트: secu VM filter.cti_ips set에 203.0.113.66을 추가해줘

    📎 참고 (CLI): sudo nft add element inet filter cti_ips ''{ 203.0.113.66 }'' && sudo nft list set inet filter cti_ips'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: 샘플 악성 IP 203.0.113.66 을 cti_ips 에 주입하시오(OpenCTI 조회 결과를 가정).'
  verify:
    type: output_contains
    expect: 203.0.113.66
    field: stdout
    semantic:
      intent: 'SecOps w13 s5 — 샘플 악성 IP 203.0.113.66 을 cti_ips 에 주입하시오(OpenCTI 조회 결과를 가정). (cmd: sudo nft add element inet
        filter cti_ips ''{ 203.0.113.66 }'' && sudo nft list set)'
      success_criteria:
      - '명령 실행: sudo nft add element inet filter cti_ips ''{ 203.0.113.66 }'' '
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft add element inet filter cti_ips '{ 203.0.113.66 }' && sudo nft list set inet filter cti_ips
  risk_level: medium
  bastion_prompt: secu VM filter.cti_ips set에 203.0.113.66을 추가해줘
- order: 6
  instruction: 203.0.113.66 가 blocklist 로 등록됐는지 확인하시오.
  hint: nft list set inet filter cti_ips
  category: verify
  points: 10
  answer: '🤖 프롬프트: secu VM filter.cti_ips set의 내용을 다시 확인해줘

    📎 참고 (CLI): sudo nft list set inet filter cti_ips'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: 203.0.113.66 가 blocklist 로 등록됐는지 확인하시오.'
  verify:
    type: output_contains
    expect: 203.0.113.66
    field: stdout
    semantic:
      intent: 'SecOps w13 s6 — 203.0.113.66 가 blocklist 로 등록됐는지 확인하시오. (cmd: sudo nft list set inet filter cti_ips)'
      success_criteria:
      - '명령 실행: sudo nft list set inet filter cti_ips'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft list set inet filter cti_ips
  risk_level: low
  bastion_prompt: secu VM filter.cti_ips set의 내용을 다시 확인해줘
- order: 7
  instruction: OpenCTI 컨테이너에서 최근 5개 Indicator 생성 이벤트 로그를 조회하시오(platform 컨테이너 로그).
  hint: docker logs | grep Indicator
  category: analysis
  points: 10
  answer: '🤖 프롬프트: siem VM의 OpenCTI 플랫폼 로그에서 Indicator 관련 로그 최근 5건을 보여줘

    📎 참고 (CLI): docker logs --tail 200 $(docker ps --format ''{{.Names}}'' | grep -i ''^opencti$'' | head -1) 2>&1 | grep
    -i indicator | tail -5'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: OpenCTI 컨테이너에서 최근 5개 Indicator 생성 이벤트 로그를 조회하시오(platform 컨테이너 로그).'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w13 s7 — OpenCTI 컨테이너에서 최근 5개 Indicator 생성 이벤트 로그를 조회하시오(platform 컨테이너 로그). (cmd: docker logs --tail
        200 $(docker ps --format ''{{.Names}}'' | grep -i ''^opencti$'' |)'
      success_criteria:
      - '명령 실행: docker logs --tail 200 $(docker ps --format ''{{.Names}}'' | g'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: docker logs --tail 200 $(docker ps --format '{{.Names}}' | grep -i '^opencti$' | head -1) 2>&1 | grep -i indicator
    | tail -5
  risk_level: low
  bastion_prompt: siem VM의 OpenCTI 플랫폼 로그에서 Indicator 관련 로그 최근 5건을 보여줘
- order: 8
  instruction: OpenCTI connectors 컨테이너 목록을 확인하시오.
  hint: docker ps | grep connector
  category: survey
  points: 10
  answer: '🤖 프롬프트: siem VM에서 opencti connector 관련 컨테이너 목록을 보여줘

    📎 참고 (CLI): docker ps --format ''{{.Names}}\t{{.Status}}'' 2>/dev/null | grep -i connector | head'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: OpenCTI connectors 컨테이너 목록을 확인하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w13 s8 — OpenCTI connectors 컨테이너 목록을 확인하시오. (cmd: docker ps --format ''{{.Names}}\t{{.Status}}'' 2>/dev/null
        | grep -i connector | h)'
      success_criteria:
      - '명령 실행: docker ps --format ''{{.Names}}\t{{.Status}}'' 2>/dev/null | g'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: siem
  script: docker ps --format '{{.Names}}\t{{.Status}}' 2>/dev/null | grep -i connector | head
  risk_level: low
  bastion_prompt: siem VM에서 opencti connector 관련 컨테이너 목록을 보여줘
- order: 9
  instruction: 샘플 IP 203.0.113.66 을 cti_ips 에서 제거하시오.
  hint: nft delete element …
  category: cleanup
  points: 10
  answer: '🤖 프롬프트: secu VM filter.cti_ips set에서 203.0.113.66을 제거해줘

    📎 참고 (CLI): sudo nft delete element inet filter cti_ips ''{ 203.0.113.66 }'''
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: 샘플 IP 203.0.113.66 을 cti_ips 에서 제거하시오.'
  verify:
    type: output_contains
    expect: ''
    field: stdout
    semantic:
      intent: 'SecOps w13 s9 — 샘플 IP 203.0.113.66 을 cti_ips 에서 제거하시오. (cmd: sudo nft delete element inet filter cti_ips ''{
        203.0.113.66 }'')'
      success_criteria:
      - '명령 실행: sudo nft delete element inet filter cti_ips ''{ 203.0.113.66 '
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft delete element inet filter cti_ips '{ 203.0.113.66 }'
  risk_level: medium
  bastion_prompt: secu VM filter.cti_ips set에서 203.0.113.66을 제거해줘
- order: 10
  instruction: filter.input 체인의 현재 룰을 확인해 정리 상태를 점검하시오.
  hint: nft list chain inet filter input
  category: analysis
  points: 10
  answer: '🤖 프롬프트: secu VM filter.input 체인의 룰을 확인해줘

    📎 참고 (CLI): sudo nft list chain inet filter input'
  answer_detail: 'OpenCTI 위협 인텔리전스 활용: filter.input 체인의 현재 룰을 확인해 정리 상태를 점검하시오.'
  verify:
    type: output_contains
    expect: cti_ips
    field: stdout
    semantic:
      intent: 'SecOps w13 s10 — filter.input 체인의 현재 룰을 확인해 정리 상태를 점검하시오. (cmd: sudo nft list chain inet filter input)'
      success_criteria:
      - '명령 실행: sudo nft list chain inet filter input'
      - 결과 출력 또는 로그 확인
      - SecOps 실습 맥락 유지
      acceptable_methods:
      - 제시된 CLI 명령 그대로 실행
      - 동등한 대체 도구 사용
      - SecOps 관련 도구 체인
      - 결과를 파일/로그에 저장
      negative_signs:
      - 실행 없이 개념 설명만
      - 결과 확인 누락
      - SecOps와 무관한 기법
  target_vm: secu
  script: sudo nft list chain inet filter input
  risk_level: low
  bastion_prompt: secu VM filter.input 체인의 룰을 확인해줘
- order: 99
  instruction: '[Multi-task] 이번 주 핵심 작업 5개를 한 번에 수행하라.'
  hint: 복합 작업은 bastion이 순서대로 계획하고 실행해야 한다
  category: multi_task
  points: 50
  verify:
    type: output_contains
    expect: ''
    field: stdout
  target_vm: siem
  answer: '🤖 프롬프트: secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) siem VM의 OpenCTI /graphql 엔드포인트에 POST 테스트 요청을 보내 응답 코드를 보여줘

    2) secu VM에 filter.cti_ips ipv4 set을 생성해줘

    3) secu VM filter.input 체인 맨 앞에 @cti_ips set 기반 drop 룰을 추가해줘

    4) secu VM filter.cti_ips set의 원소 목록을 확인해줘

    5) secu VM filter.cti_ips set에 203.0.113.66을 추가해줘'
  answer_detail: ''
  risk_level: low
  bastion_prompt: 'secu/web/siem VM 전반에 대해 다음 작업들을 순서대로 수행해줘:

    1) siem VM의 OpenCTI /graphql 엔드포인트에 POST 테스트 요청을 보내 응답 코드를 보여줘

    2) secu VM에 filter.cti_ips ipv4 set을 생성해줘

    3) secu VM filter.input 체인 맨 앞에 @cti_ips set 기반 drop 룰을 추가해줘

    4) secu VM filter.cti_ips set의 원소 목록을 확인해줘

    5) secu VM filter.cti_ips set에 203.0.113.66을 추가해줘'
